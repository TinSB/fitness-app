// IronPathL10n — iOS-1 placeholder.
//
// This package will own translation tables, number / weight formatters,
// and the kg / lb display contract starting in iOS-2. iOS-1 carries no
// strings — the placeholder ContentView in the app target uses inline
// literals only.

public enum IronPathL10nVersion {
    public static let value = "0.0.1-bootstrap"
}
