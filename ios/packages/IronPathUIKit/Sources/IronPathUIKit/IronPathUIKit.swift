// IronPathUIKit — iOS-1 placeholder.
//
// Despite the name, this package vends SwiftUI primitives — buttons,
// cards, navigation containers — that the Focus Mode / Plan / History
// / Progress screens consume from iOS-5 onwards. The name is the
// SwiftPM package name; the underlying framework is SwiftUI. No
// UIKit-specific code is planned.

public enum IronPathUIKitVersion {
    public static let value = "0.0.1-bootstrap"
}
