# Rede Changelog

All notable native iOS product, architecture, repository-governance, and documentation changes should be recorded here.

> Backfill note (2026-06-13): the entries from M5-1 (2026-06-10) through wave-8 (2026-06-13) below were backfilled in one pass to restore per-PR traceability after the log fell ~3 days behind. Facts sourced from `DEV_LOG.md` same-day entries and `git log`; backfilled summaries are tighter than the live M1–M4 entries above. Citation convention: `#NNN` = a merged PR; `#<7-hex>` = a commit with no PR number (the late-period work was committed locally while the GitHub account was suspended, so those entries cite the commit hash instead).

> Backfill note (2026-06-23): the 2026-06-20 → 2026-06-23 sections below were backfilled in one pass — the log fell behind after 2026-06-18 (FR-NT + FR-PL3 slices 1/2). Covers FR-PL3/4 completion, the build bumps, the UX-polish program, notification fixes, FR-T5/FR-PR7 R1 wrap-up, the FR-EX2 exercise-detail content waves, the action-library expansion (123 → 165), and the FR-PL6/PL7 user-customizable-plan feature. Facts sourced from `DEV_LOG.md` + `git log` + PR titles; tighter than live entries.

## 2026-07-14

- **Today tab graphic language, round two (N3+N4)**: the status line gains a seven-dot week strip (trained = ember fill, today = stroke, rest = etch ticks) with "trained N days this week" — derived by a new pure `ContinuityCalendar.weekStatus` (TDD, calendar-week aligned with the continuity calendar); the post-workout summary card hangs a five-session volume sparkline next to the tonnage figure (shared `MiniSparkline`, extracted from Progress); and the reasons drawer drops its static "apply · hold · swap" capability row — real buttons already express control. Three-lens review caught the batch's own red line half-enforced: the light-day headline "weekly volume is in" and the coach volume card "N short of your weekly plan" still ran on rolling-7-day data next to the calendar-week strip — both reworded to "past 7 days" with anti-regression tests; date normalization aligned with the calendar precedent; count unit unified to days. Dead `signalLine` formatter removed.

## 2026-07-14

- **Milestones: same-lift rows merged** (owner-approved audit follow-up): a lift with both a measured and an estimated milestone now renders as one row — "Squat 135 lb · est 225 lb" (measured in ember, estimate muted) instead of two stacked rows repeating the name. Estimate-only lifts keep the "est" badge. Display-side grouping only — the engine already guarantees an estimate is emitted only above the measured tier, so nothing is lost. Combined VoiceOver line added with zh/en tests.

## 2026-07-13

- **Progress tab enterprise-grade audit** (owner: "review the whole Progress page as a top designer"): a nine-state screenshot matrix (three scales × rich/calibrating/empty/XXL/EN, unlocked by a new `-progressScrollTo` hook) surfaced one real engine bug and seven polish items, all fixed in place. The bug: the muscle-trend window counted the in-progress week as a full zero week — **every Monday morning all muscles showed ↘** (same disease as the comeback-protocol "mystery deload"); fixed TDD-first so the window only reads completed calendar weeks (repro test red → green, 182-test package regression clean). Polish: bars capped at 40pt and centered (brick wall → columns, Apple Health ratio), "orange marks…" legend lines retired (self-evident, a de-AI straggler), the cycle list's per-row "est 1RM" ×6 folded into a single list title, development rows raised to the 44pt HIG hit target, tonnage displays rounded to integers (reversing the earlier half-pound precision — noise, not precision, on five-digit aggregates), the zero-progress track made visible (18%→28%), and the EN empty-state got its missing period. Before/after evidence in docs/工作记录/2026-07-13-progress-audit-*.png.
- **Heatmap body v2 — full-silhouette redesign** (owner on v1: "too crude, no design sense"): the figure is rebuilt as a continuous human silhouette (head/neck/torso-with-trap-slope/pelvis-wedge/tapered-limb capsules, flood-filled as a union — per-part fill to dodge winding cancellation between mirrored paths) with curved muscle plates in a unified rounded-polygon language: paired pecs with a sternum gap, traps-diamond + lat-wings, abs column with segment etching + obliques, curved glutes/hams/calves. Semantics untouched: five-tier ember shading, calibrating = etched outline + untappable, button feedback, one VoiceOver announcement per muscle (a11yPrimary field replaces the -r suffix hack). Shapes iterated to final form in an offline CoreGraphics render loop before porting. Independent review: no MAJOR; verified by script that the 10-muscle a11y contract holds exactly. Evidence: docs/工作记录/2026-07-13-heatmap-v2*.png.
- **De-AI round two: muscle heatmap + hero exercise card + catalog dedup** (owner: "still not satisfied — keep going"): a nine-screen audit with rich seed data located the residual AI-feel — uniform text rows everywhere, zero graphic language. **N1**: the Development block gains front/back forged-steel-plate body figures — 10 muscle groups shaded across five ember opacity tiers by level, calibrating muscles drawn as etched outlines (unfilled, untappable — the detail sheet would render the placeholder Lv.1 as real, a fabricated-data red line caught independently by two review lenses), tap any plate to open that muscle's detail sheet. Hand-drawn SwiftUI paths, zero image assets. **N2**: today's next exercise becomes the page's hero — name at headline size, target "60 kg × 8" at scaled title size (review caught the initial fixed-30pt version inverting hierarchy at accessibility sizes), following the active exercise mid-workout. Still a list item, not a card. **N6**: arnold/landmine presses no longer double-count front delts (primary duplicated in secondary), with a catalog-wide contract test. Three-lens parallel review (correctness/design/regression) cleared 3 MAJOR + 5 MINOR + 5 NIT, all fixed in place.

## 2026-07-12

- **1.4 (build 21) archived — the full seven-pack, the ship-it build**: adds comeback reminders (#689) on top of build 20's six. PlistBuddy-verified 1.4/21 both targets; App Store submission planned once owner's 5-item device pass completes (store still ships 1.0).

## 2026-07-10

- **Comeback reminders — the app now reaches out when you stop training** (FR-NT3, owner-picked next feature after all four tabs went green): three tiers at 5/12/21 days since your last session, fired at your personal median training hour. The 21-day tier shares its threshold with the comeback protocol — the notification's promise ("your cycle has been reset, first session back starts light") is exactly what the engine does when you open the app. Copy went through three owner-review rounds (AI-flavored → coach-direct → **Apple-style**: observational full sentences, your own data, zero pressure). One session cancels and reschedules everything; after all three fire, permanent silence until you train. Settings gains a "Comeback reminders" toggle, **on by default** (opt-out — authorized users benefit by default; unauthorized devices never see anything). Expired tiers skip, never fire late. Review caught a main-thread redundant engine run for the day-name projection (fixed: reads the today-page's already-computed result). Scheduling proven by on-device probe: exact fireAt for all tiers, expiry-skip verified in an unexpected real timezone case, day-name projection correct.

## 2026-07-10

- **1.3 (build 20) archived — the full six-pack, entering device-acceptance phase**: 1.2(19) scrapped unsent (predates auto-balance); 1.3/20 carries mle-v2 calibration, comeback protocol, weekly cycle modes, muscle detail page, relative strength standards, and auto-balance. Device acceptance checklist authored (docs/工作记录/); store still ships 1.0 — submission planned once acceptance passes.

## 2026-07-10

- **Auto-balance — weak muscles get extra sets automatically, no proposal cards** (owner verdict: "don't suggest, just change the plan" + "no fine print either"): the prescription engine now consumes MLE's "building up" list — exercises targeting those muscles get +1 set on normal training days. Full yield discipline: light/deload/comeback days never boost (screenshot-verified), mesocycle overreach weeks don't stack, deload weeks aren't cancelled, and each session adds at most 2 sets total. The only explanation lives inside the tap-to-open reasoning drawer ("Building up Biceps. Extra sets added today") — zero persistent UI text. Feeds off the MLE memory single source (detraining muscles already excluded — they go through the comeback protocol instead), converges via natural negative feedback (extra volume → level catches up to median → list drops the muscle → boosting stops), and never writes back to plan customization (progression-drift red line). Review killed a false-positive phase-gate test (rewritten with an anti-vacuity assertion) and forced the PRD FR-PL5 rewrite from "proposal" to "automatic". FR-PL5 closed — the last piece of the plan tab.

## 2026-07-09

- **1.2 (build 19) archived — marketing version now bumps with every archive** (owner correction): 1.1→1.2 + build 19 both targets; unsent build 17/18 archives deleted; What's New doc retargeted to 1.2/19. Carries everything: mle-v2 calibration, comeback protocol, weekly cycle modes, muscle detail page, relative strength standards.

## 2026-07-09

- **Build 18 archived (1.1)**: bump 17→18 both targets; adds the bodyweight-relative strength standards batch (#683) on top of build 17's four features. PlistBuddy-verified 1.1/18; upload via Xcode Organizer (owner).

## 2026-07-09

- **Bodyweight-relative strength standards — a 60kg woman benching 36kg now ranks like the 100kg man benching 100kg** : owner confirmed levels should reflect actual ability and greenlit closing all three v1 gaps (no tiers below 60kg, no low-bar path for back/biceps, no bodyweight/sex adjustment). New RelativeStrengthStandards: best lift ÷ current bodyweight against five-grade tables (male/female × bench/squat/deadlift/OHP/**barbell row**), grade floors 2/6/10/16/19 aligned with absolute anchors, merged via max — absolute anchors untouched. Sex setting lands in Settings › Training background (male/female/not set, "only used for strength standards", single-purpose contract: never feeds prescriptions). Bodyweight: HealthKit latest (silent) → profile fallback → honest degradation. Review caught two serious issues, both fixed: dirty HealthKit weights (wrong-unit scales, family sharing) could permanently poison peak memory — 20-400kg plausibility guard in the tested engine layer; and a 3-session newcomer maxing one relative-elite single would jump straight to Lv.19 — low-confidence guard caps relative floors at intermediate (10) until confidence unlocks. New evidence line "Bodyweight-relative strength raised this level"; 15 new tests; simulator proof: female 60kg × 36kg bench → Chest Lv.10.

## 2026-07-09

- **Build 17 archived (1.1)**: version bump 16→17 both targets; carries mle-v2 level calibration, comeback protocol v1, weekly cycle modes, and the muscle detail page. PlistBuddy-verified 1.1/17 in the archive; upload to ASC via Xcode Organizer (owner).

- **Muscle detail page — tap Back, see Lats / Upper back / Traps each with its own level** (#681): owner wanted drill-down granularity without slapping new tags on every exercise — and none were needed: the catalog already carries fine-grained values (delt heads, traps); the real work was refining 26 back-family exercises from a blanket "back" into lats (12 vertical pulls — "pulldowns build width") vs upper-back (14 rows + rack pull — "rows build thickness"), plus 10 presses from "shoulder" to front-delt. Zero data migration (history re-derives from catalog values). Two-tier structure: the 10-block overview stays byte-identical (same-destination mapping keeps big-block levels mathematically unchanged); tapping a muscle row now opens a detail sheet — big-block header, a Breakdown section (per-sub-muscle level + sets/week + progress bar), and the reasoning section (inline expansion retired; future trend charts land here). Sub-levels are volume-led (12 effective sets/week full-score anchor) and inherit the parent's strength score **only when trained** — review caught that unconditional inheritance would show "Traps Lv.6 · 0 sets/week" (fixed, exact Lv.1 lock). Sub-tier is display-only (never feeds tier/balance/decisions/memory/share). Plan editor's muscle-frequency guardrail re-merges to big blocks ("Back 2×/week", regression-locked). New copy passes all four red-line suites.

## 2026-07-08

- **Weekly cycle mode — carry-over vs fresh-start, one settings toggle** (#680): owner's cadence question (planned push/pull/legs, only did push+pull — next week: finish the round, or start fresh?) resolved by shipping both mental models. Default = carry-over (science side: missed days come up next, coherent with MLE balance) now made transparent with a today-page subline ("Last week's Legs A carries over to today. Swap the day to start a fresh round"); new opt-in setting "Restart cycle each week" flips rotation to ISO-week-scoped counting (each new week starts the split from day one; day-swaps stop generating carry-back compensation, and the swap dialog/undo copy drops the "comes back next time" promise in that mode — honesty line). Review (regression-guard) caught two severes, both fixed: swap-dialog copy lied in weekly mode, and the Plan tab's four projection call sites still ran the legacy total-count formula (two tabs would visibly disagree) — rotation decision extracted into a single-source `rotationBase()` consumed by today/plan-projection/day-sequence-editor/proposal-preview, with an anti-divergence anchor test locking slot one to the today page in both modes. Also fixed en route: the "Today (swapped)" header false-positive under both new modes (app-layer recomputation drift, screenshot-caught). Three-shot evidence set on one seed (carry-over vs restart vs settings).

- **Comeback protocol v1 — a 3-week absence no longer pretends you trained yesterday** (#679): owner's real-device E3 (22 days away → still pushed the old rotation pointer, generic "one notch lighter" receipt, progression would even add a rung on top). Three-part fix: rotation restarts at the split's first day when today's gap ≥21 (= MLE detraining window; rotation thereafter counts from the most recent restart point — stateless history scan, no new fields; explicit day-swap still wins); load pulls back by tier (14-20d ×0.9 / 21-41d ×0.85 / ≥42d ×0.75 = MLE strength-window) with progression suppressed across external+assisted+bodyweight-plus; receipt copy goes tiered and human ("22 days away. Cycle restarts from day one, weights eased back"). Review (regression-guard grade) caught 2 severe + 1 moderate, all fixed in place; five legacy test seeds unintentionally contained ≥21-day gaps, dates pulled close with paper-trail comments. Sim repro of the exact owner scenario archived. Notification absence-variation deferred as its own slice.

- **Build 16 — mle-v2 calibration ships to TestFlight** (#676): `CURRENT_PROJECT_VERSION` 15 → 16 (app + widget, 4 sites; `MARKETING_VERSION` stays 1.1). Archived from up-to-date main (#675) after the pbxproj-first check; product verified via PlistBuddy at 1.1 (16) for both targets. On-device recheck: a fresh account after 3 sessions should show Lv.5 across the board with the "more training data unlocks higher levels" reasoning line.

- **MLE-8 first calibration pass (mle-v2) — "3 sessions = Lv.9" fixed to Lv.5** (#675): owner's real-device E3 feedback confirmed the level curve reads too fast. Three tightenings + one unification: no-baseline performance 15 → 0 (zero strength evidence earns zero — reverses the v1 "neutral base" ruling on device evidence); exposure full-score anchor 15 → 20 effective sets/week; NEW confidence level caps (low → Lv.5, beginner ceiling; medium → Lv.10; high uncapped) as the level-tier face of the §3.4 "low confidence → more conservative" red line, with progress pinned to 1 + a tenth evidence code (`confidenceLevelCapApplied` → "More training data unlocks higher levels") — and actual milestones still beat the cap (bench 100kg floors Lv.10 regardless, test-locked). Balance now always uses the pre-modifier curve level (cap flattened five muscles to Lv.5 → variance zero → fake Balance 100, screenshot-caught; same "no forced beautification" clause as the milestone floor). modelVersion mle-v1 → mle-v2; static config renamed `.v1` → `.current`. Before/after shots on the same 3-session seed: Lv.9-12 → Lv.5 across the board, Balance 100 → 91. New scenario anchors: 3 sessions ≈ Lv.5, 6 steady weeks ≈ Lv.10, 12+ high-confidence weeks let the score speak.

## 2026-07-07

- **Build 15 — MLE ships to TestFlight** (#673): `CURRENT_PROJECT_VERSION` 14 → 15 (app + widget, 4 sites; `MARKETING_VERSION` stays 1.1 — TestFlight iteration build). Archived from up-to-date main (#672) after the pbxproj-first check (build-13 lesson); product verified via PlistBuddy at 1.1 (15) for both app and widget extension. Owner uploads via Organizer.

- **MLE batch B close-out — spec write-back + logs** (#672): system-logic updated in 7 places (peak persistence contract §6.5.4, cold-start render semantics §6.5.9, Progress-landed + FR-PL5 layering §6.5.10, three B3 convergences §6.5.11, Muscle Level card live + confidence structural-absence §6.5.12, contract-slice vs actual-batch mapping §6.5.13, share status note + card table §9.2); PRD FR-PR6 → implemented, FR-PL5 → partially landed (display-tier). DEV_LOG battle report; level-curve pacing flagged as calibration-pending (3 sessions → Lv.9-10 reads fast; MLE-8 refinement after real-device data).

- **MLE-B5: Development share card — third card kind** (#671): `ShareSnapshot.Content.muscleLevel` + `SharePrivacyFilter.muscleLevel` factory (level-desc sort / cap 6 / balance clamp 0-100 / calibrating tier → nil); privacy via structural absence — confidence doesn't exist on the card type (harder than the contract's `String?` nil, per §3.4); card face reuses the 360×450 offscreen pipeline (REDE mark + tier + ≤6 muscle rows + trend arrows + balance); entry on the Development block (unlocked-only); `-sharePreviewIndex` screenshot hook. Review: zero MAJOR/MODERATE; clamp-boundary test + comment wording fixed in place. zh/en shots archived.

- **MLE-B3: Development block unsealed — muscle levels on Progress (FR-PR6 + FR-PL5 display-tier)** (#670): the block the original header sealed as "no fabricated data" goes live now that the engine eats real history. Calibrating/unlocked/collapsed three-state render, per-muscle Lv + progress + trend arrow + decision label (·Building up / ·Ease back in), overall tier + balance line, tap-to-expand evidence in plain words (all nine engine codes mapped — review MAJOR: the ninth, `exposureRecentSets`, was missing and screenshot-proven silently dropped), settings row relabeled 自报背景/Self-reported (§6.5.14 legacy-merge). Review 4 MAJOR + 4 MODERATE all cleared: calibrating copy hit the no-period/no-dash red line (rewritten two-beat), two "rulings" had no paper trail (handoff doc amended in place), a11y hint, en singular/plural, recover×detraining relabel. 5 shots archived (zh/expanded/en/XXL).

- **MLE-B2: cross-run level memory — derived-only persistence + peak write-merge** (#669): `MuscleLevelMemory` (schema-versioned, unknown-version rejected, decode-failure = honest "no memory", atomic) at `muscle-level-memory.json` next to canonical, never touching it; extract records unlocked muscles only. Review MODERATE: two concurrent `loadOutcome` writers could clobber a fresher peak — write path now re-reads disk and max-merges peaks per key (also preserves peaks for muscles that regress to calibrating), levels/tier stay last-writer-wins snapshot semantics. `shouldPersist` sunk into the package with three-branch tests. 151 package tests green.

- **MLE-B1: feed pipeline — clean history → MuscleDevelopmentProfile** (#668): `MuscleProfileComposer` (RedeLocalSnapshot) chains rows/touches/e1rm-rows → aggregation → 10-muscle observations (dataless muscles emit calibrating) → estimator → milestones → assembler, fully package-tested; `MuscleGroupMapping.primaryGroup` routes e1RM to primary muscle only (ruling ③); ProgressModel glue kept to ~38 lines, same statsRecords lens as FR-PR7 (suspect sets excluded). Review: zero MAJOR; floor test rebuilt as with/without-milestone contrast (seed's natural level was already above the floor — the assertion had zero discriminating power), unique-session-count test rebuilt at a boundary where row-counting inverts the outcome.

- **MLE batch B handoff (B0)** (#667): execution prompt with five slices, six batch-level rulings (FR-PL5 display-tier demotion, single share card, e1RM primary-muscle-only, movement family = substitution group, local derived-only persistence, milestone e1RM same-source-same-trust) and a file:line-verified terrain map from two read-only scout agents.

- **MLE batch A close-out — spec write-back + logs** (#666): system-logic §6.5 updated in 8 places with batch-A rulings (package placement §6.5.1, 17→10 muscle mapping + fractional counting §6.5.2, `balanceScore: Double?` §6.5.3, level-curve/sub-score anchors + tier semantics §6.5.4, MLE-4 milestone rulings §6.5.5, trend/balance/peak assembly semantics §6.5.6). DEV_LOG battle report; CHANGELOG backfilled for #655-#658 (missed at mid-batch close-out).

- **MLE-4: contract milestone catalog (nine §6.5.5 anchors) + level floor + tier progress signal** (#665): new `MuscleMilestoneCatalog` (RedeLocalSnapshot) with the nine ruled milestones (bench 60/80/100/120/140 kg, squat 140, deadlift 180, OHP 60, weighted pull-up +20; separate lb ladder 135-315/405/+45 — never unit-converted, same FR-PR7 discipline), each carrying linkedMuscles/levelFloor/tierCandidate + catalogVersion. Achievements: actual completed set → high confidence, e1RM-only → estimated/medium, never both for one milestone (estimates don't impersonate). Assembler wiring: single `flooredLevel` helper feeds median/tier/estimate/strongest (no consumer drift); floors only lift unlocked muscles (calibrating stays gray); floor hits zero out `levelProgress` + emit `milestoneFloorApplied` evidence (curve progress is meaningless against a lifted level); **balanceScore uses un-floored curve levels** — a milestone is strength evidence, not coverage evidence, and flooring all linked muscles to one value would beautify "bench-only" into "perfectly balanced" (contract: no forced beautification). Review: zero MAJOR; 4 MODERATE + 3 MINOR all fixed in place (progress semantics, balance beautification, two test gaps, full nine-anchor table test, helper extraction, catalog version). Coexists with FR-PR7's simplified `StrengthMilestoneCatalog` (file untouched). 136 package tests green.

- **MLE-3: trend / peakLevel / balanceScore / TrainingTier assembly** (#664): `MuscleProfileAssembler` builds the full `MuscleDevelopmentProfile` from per-muscle computations. Trend is calendar-anchored (3 ISO weeks back from nowISO, missing weeks count 0 — review MAJOR: data-keyed windows drifted and made detraining unreachable); insufficient data = stable, never a scare-label. peakLevel is max-monotone with previous values supplied by the caller (no canonical writes; storage contract = batch B). balanceScore = inverse CV of unlocked levels, nil under 3 unlocked (no fake balance on tiny samples). overallTier: median unlocked level into 5/8/12/16 bands, capped at novicePlus without a strength-progress signal, one band down when balance < 40, calibrating fallback via **median confidence** (review MAJOR: "any high" let a single specialized muscle bypass the safety net). First unlock is an appearance, not a breakthrough.

- **MLE-2: developmentScore + Lv.1-20 curve + calibration gate** (#663): `MuscleLevelEstimator.compute` scores exposure (max 60, effective sets × frequency factor `min(recordedWeeks/6,1)` — review MAJOR: one monster week could farm levels) + performance (base 15, +7.5 per +10% e1RM, max 30; no baseline = base + noBaselineWindow limitation, out-of-window = 0 + noRecentWindow). Threshold curve `T(n)=n+0.2n²` (fast early, slow late); bottom-bucket floor pinned to 0 (review: low-score progress regressed as score grew). Calibration unlock: 3 sessions or 8 working sets.

- **MLE-1: fractional set aggregation** (#662): `MuscleContributionTable` (primary 1.0 / secondary 0.5, Pelland 2025 meta best-fit; same-group collisions take max, not sum) + `MuscleVolumeAggregator.weeklyFractionalSets` bucketing contribution rows into per-muscle ISO weeks via rawValue bridging (Master §5: no cross-package dependency).

- **MLE-0: type skeleton + MuscleLevelModelConfig** (#661): 14 contract types for the muscle-level engine land in RedeLocalSnapshot (Foundation-only, widget-shareable — placement ruling written back to §6.5.1) with every model constant centralized in `MuscleLevelModelConfig.v1` (modelVersion `mle-v1`): calibration/confidence/window thresholds, scoring anchors, curve params, assembly bands.

- **MLE-D1: 17→10 muscle-group mapping** (#660): `MuscleGroupID` (10 values, two same-value enums with anchor comments + rawValue contract tests on both sides) + `MuscleGroupMapping.group(forCatalogMuscle:)` as the single translation point: 4 delts→shoulders, traps/upper-back→back, lower-back→core, adductors→glutes (review MAJOR: hip-extension function makes glutes less misleading than quads), forearm honestly excluded (nil, not force-bucketed).

- **MLE batch A handoff + research artifacts** (#659): execution prompt (docs/工作记录/2026-07-07-MLE批次A-执行prompt.md, 7 tasks + discipline + close-out checklist) and EVIDENCE_LEDGER.md (16 research findings with IDs: fractional counting, Pelland efficiency tiers, licensing analysis of strength-standard tables, cold-start patterns, competitor complaint taxonomy). D2 (backfill 60 secondary-muscle lists) was downgraded to a verification close: all 60 are isolation movements where empty = correct.

- **Build 14 — rebuilt archive after the stale-main incident** (#658): owner's build-13 archive was cut from a local main missing #654, producing a 0.2.3 (12) artifact; main fast-forwarded, build bumped 13 → 14 (app + widget, 8 sites), archive re-cut clean at 1.1 (14). Lesson recorded: verify pbxproj version numbers before archiving.

- **De-AI-feel mid-batch close-out — spec write-back + dev-log report** (#657): PRD/system-logic/copy-baseline write-backs for M1+M2; DEV_LOG battle report for the mid batch.

- **Empty states unified: Progress preview bars + carry-over CTA, Plan rot-promise removed (batch M2)** (#656): Progress pre-data state now shows a schematic preview of the coming bar chart (ghost bars + axis, clearly marked sample) with a carry-over button into today's session instead of a bare "no data yet" line; Plan page's stale "系统会随训练自动调整" promise (referencing behavior that doesn't exist) replaced with honest copy. Review MAJOR: the en screenshot was actually zh (missing `-locale en`) — re-shot; schematic bars restyled away from skeleton-screen lookalike.

- **Primary button promoted: full-width default + one-step brighter + 50pt + 3px ember edge + hair outline (batch M1)** (#655): `EmbButton` primary variant gains real visual weight so the screen's one main action reads as *the* action (was visually peer to secondary chips). Review MAJOR: rest-bar hug regression fixed (full-width default needed an opt-out for inline contexts). Owner eyeballed before merge.

## 2026-07-05

- **Version 1.1 (build 13) — engineering marketing version aligned with ASC** (#654): App Store shipped as 1.0 while the project's `MARKETING_VERSION` stayed 0.2.3 (deliberate carry-over noted at 1.0 close-out: align on next bump). This de-AI-feel batch ships user-visible features (completed-day summary, plan-page collapse), so minor bump to 1.1; `CURRENT_PROJECT_VERSION` 12 → 13 (next upload must be ≥13). App + widget targets × Debug/Release, 8 sites; Info.plists reference build settings, untouched. Built product verified via PlistBuddy: 1.1 (13).

- **De-AI-feel batch close-out — spec write-back + dev-log battle report** (#653): PRD ×6 (FR-T2 first-badge, new FR-T6 completed-day summary, FR-PR3 chart slots, FR-PL2 collapsed schedule, FR-SH1 stat-row no-wrap, FR-SH3 entry continuation), system-logic ×3 (completed-state render contract, coach-hint two kill-switches, schedule-digest layer), copy-baseline ×2 (completed-state bilingual strings, one-shot teaching-hint policy). Six evidence screenshots archived under docs/工作记录/.

- **Completed-day summary block + share entry on Today (batch T1, new FR-T6)** (#652): the "already trained today" state used to be two lines + 85% empty screen — the highest-emotional-value moment paid zero. Now a surface-primitive block (no ForgedCard; Today stays at 0 nameplates) renders volume as the hero number (thousands-grouped), exercises · sets · duration-band meta line, PR badge, training-day name, and a lightweight "Share this workout" entry into the existing share-card preview (FR-SH3 entry continuation). Data derives from **persisted** history via new pure `TodayCompletedDigestBuilder` (RedeLocalSnapshot, 6 tests): sets/volume from the snapshot chain (same lens as Progress), dayCode/duration read-only from canonical (`loadTodayCompletedFacts` — the clean chain doesn't carry those two), share snapshots exclusively through `SharePrivacyFilter` (structural-omission privacy inherited). Honest edges: latest-session-not-today / missing record → nil → original two lines; missing duration never fabricates a band (workout card skipped, PR card kept). Sim run exposed a real bug in the first cut — one-shot `.task` missed the "view alive, state flips" path (`-autoCompleteSession`) — fixed as `.task(id: isCompletedToday)` and re-verified. Kill-and-relaunch keeps the block (persistence proven in zh shot). Review: zero MAJOR; 3 of 4 MINORs settled in-place (count-semantics comment, input-contract comment, setCount=0 boundary test).

- **Plan page schedule collapsed: day-types expand once + segment sequences (batch T2)** (#651): the day-by-day projection render repeated each day-type's full pattern list verbatim (upper/lower ×2 segments = 4 dupes in 8 rows; a 5-day split = 10 rows of copy-paste) — the archetypal "internal data structure dumped as UI". New pure `PlanScheduleDigestBuilder` (RedeTrainingDecision, 4 tests, failing-first) dedupes types by first occurrence and emits per-segment dayCode sequences; the view renders 「接下来/再往后 · Coming up/After that」 as compact sequence lines (§3.4 anti-regression tests stay green — ordinal words, never calendar weeks) plus a 「训练日构成 / Day types」 section where each type expands once and the row is still the FR-PL6 drill-down editor (verified by an actual computer-use tap: editor opened with swap/remove/add/apply intact). FR-PL7 reorder entry untouched; 0-ForgedCard axiom kept. A 5-day PPL+UL mixed split now fits one screen (was scroll-through repetition), zh/en shots archived.

- **Coach hint gets a session cap: stops teaching after 3 sessions (batch T6)** (#650): "Tap the weight to adjust…" already had one kill-switch (`@AppStorage hasUsedQuickAdjust` — used once, gone forever), but a user who never taps the weight saw the manual every single session. Scope honestly narrowed from the handoff plan (no new persistence key needed): second kill-switch = cleaned completed-session count ≥ 3, reusing the exact `CoachActionEngine.totalSessionCount` lens (no separate counter to drift; nil model → 0 → conservatively still teaches). "Starting at plan target" was adjudicated a **state line**, not teaching copy — it's the first-set branch of the why-line that flips to "last 80×6 hold" after a set — deliberately kept (the verification shot happens to prove the polymorphism). Verified across the boundary with the seed-history trick (complete one → shift dates back 2 days → repeat): hint present on session 1, gone on session 4, state line intact. Review: zero code defects; both MAJORs were evidence-packaging gaps (no-test-target disclosure + deviation rationale), both now in the PR body.

- **Progress bar chart ghost slots: a single bar no longer fills the whole chart (batch T4)** (#649): with one week of data the equal-split `HStack` gave the lone bar the entire chart width — a giant orange rectangle (every new user's week one). Shared constant `barChartWindow = 5` now sources both `prefix(5)` call sites (session/week) and the ghost-slot fill: `max(0, window - bars.count)` transparent slots (`Color.clear`, `accessibilityHidden`) keep real bars at full-window width, left-aligned, blank space where future bars will land. Full-window layout is point-identical (ghost count 0); cycle scale (trend lines) untouched. Reviewer verified all seven concerns including VoiceOver order purity and the frame-locked height; zero MAJOR.

- **Share card duration split into value + unit segments, fixing the three-line wrap (batch T5)** (#648): "60–90 min" at 30pt bold in an equal-width stat column broke into three lines (60–/90/min) on the poster. RedeL10n now exposes `shareCardDurationBandValue` ("60–90") + `shareCardDurationUnit` ("min/分"), old combined API deleted (single consumer migrated); the card renders 30pt digits + 13pt unit at first-text-baseline, unit-bearing column hugs content (`fixedSize`) while numeric columns split the rest — digits never scale down (the min-scale-factor route was tried and measurably still truncated English). Review MAJOR handled by its own recommendation: a width-contract test locks every band's value segment at ≤5 characters — an independently-built probe rig proved a hypothetical "120–180" band would silently crush neighbor columns ("999" → "9…"), so the hidden assumption is now an explicit, breaking-test contract. Both locales render one full-size line (zh/en shots).

- **First-session "first" badges deduplicated on Today (batch T3)** (#647): on a fresh plan the subtitle already says "First session, every lift starts light", yet all 8 rows carried a trailing 「首练/first」 badge — the same fact ×9. The row badge column now hides only when the header sentence is actually on screen: gating on `callCode == "train" && reasonCode == "noHistoryCalibration"`, the exact case that produces the subtitle. The first cut gated on "all prescriptions are .start" — review MAJOR: an experienced user swapping in an all-new day (FR-PL6/PL7/TR7) also yields all-.start with no header sentence, where the badges are accurate and necessary; rewritten to the same-source signal so that path keeps its badges. increase/ease/hold rows byte-identical.

## 2026-07-04

- **Dynamic Type support for all eight font tokens (audit MAJOR #2, NFR-7 MVP baseline)**: the entire type scale was fixed-size — system text-size setting had zero effect. Each token now anchors to a semantically-close system text style (redeDisplay→largeTitle, redeTitle→title1, redeHeadline→title3, redeSubhead→headline, redeBody→body, redeCallout→callout, redeCaption→caption1, redeOverline→caption2) via `UIFontMetrics.scaledValue(for:)` applied to the size, still emitting `Font.system` — implementation notes: the planned `Font.system(size:weight:relativeTo:)` does not exist in the SDK (custom fonts only), and wrapping `Font(UIFont)` would break the 6 `.weight()` derivations + `.monospacedDigit()` call sites, so scaling the numeric size preserves every modifier. `RedeTracking` deliberately stays fixed (brand engraving, not a readability parameter). Default size category: `scaledValue` is identity — pixel-identical UI. No `minimumScaleFactor` guards added: XXXL across 7 screens (5 screens × zh/en sampling) showed zero truncation — the feared 54pt train hero renders the weight digits only (suffix is a separate callout line), so no speculative defensive branches. Known honest limits (in-code comment + DEV_LOG): mid-run text-size changes need a screen re-entry/relaunch (computed-var reads category at body evaluation); AX sizes not per-screen validated (FF). Spec write-back: PRD NFR-7 status. Quality gate PASS (ForgedCard budget = regression card); 14 before/after screenshots archived. **Merged after the owner reviewed the two-size screenshot set (per handoff: no auto-merge for this one).**

- **Restore-to-resting now re-schedules the rest-end notification (+ discard cancels stale ones)**: `restorePendingDraft()` restarted the rest countdown (`restCountdown.begin`) but never called `scheduleRestNotification` — kill the app mid-rest, resume, lock the phone: no rest-end ping. Now aligned line-for-line with the `apply(.logSet)` path (seconds = `restSecondsPlanned`, same source as the countdown — restore semantics restart the full planned rest, an existing deliberate simplification). Preference-off → policy returns nil → nothing scheduled (package-tested). Review caught the sibling gap, fixed in the same PR: `discardPendingDraft()` now calls `cancelRestNotification()` — previously a rest-end notification scheduled by the killed process could still fire after the user abandoned the draft. "Not now" deliberately left un-cancelled (draft persists, user may resume immediately; semantics for the owner to call). Honest evidence limits: no app-target test bundle exists (gate runs package tests + build only) so the two wiring lines aren't auto-covered; notification-permission dialog can't be tapped unattended — end-to-end lock-screen delivery is a one-minute manual device check (steps in DEV_LOG/PR). Fixed id "rest-end" replaces rather than stacks (UNUserNotificationCenter semantics); reviewer verified no double-schedule/stale-fire path via draft event-replay reasoning. Quality gate PASS.

- **Resume-draft prompt goes brand-styled (last native action popup closed out)**: the FR-TR9 resume prompt (「继续进行中的训练？ / Resume your session?」) was the one remaining native `.alert` after #634 brand-converted the three swap flows — now it renders as the house `RedeChoiceSheet` (component untouched; its `role: .destructive → redeRisk` support, laid down in #634, gets its first user). Three actions map one-to-one to the old alert: Resume (ember emphasis) → `restorePendingDraft()` + jump to Train; Not now (neutral) → clear prompt, keep draft file, re-ask next launch; Discard (red destructive row, **bottom-placed with the neutral row between it and the primary** — mis-tap guard) → existing `discardPendingDraft()`. Swipe-down = Not now (same semantics as the old system Cancel). Draft read/write paths untouched, no new deletion surface. New sim-verification hook `-autoResumeAction resume|discard|later` (repo hook convention; simctl can't tap UI). Verified: quality gate PASS; 5 screenshots (zh/en panel, resume lands mid-session at set 2/3 with set 1 still checked, discard leaves today clean, not-now normal) + file-level proof (draft json gone after discard, kept after not-now). Presentation layer only; spec write-back none (FR-TR9 contract doesn't pin presentation form).

- **Plan page: schedule section labels say sequence, not calendar (audit MINOR)**: `PlanWeekProjection` blocks future sessions by days-per-week starting from the next session — it is a rolling forecast, not calendar weeks — yet the section labels said 「本周/下周 · This week / Next week」, so a user who already finished this week's sessions reads "4 more this week". Labels only, zero projection/rotation logic touched: `planScheduleThisWeek` → 「接下来 / Coming up」, `planScheduleNextWeek` → 「再往后 / After that」, and the adjustment-proposal card's `planAdjustAfterLabel` (same projection source, same misread) → 「调整后 / After the change」 (review correction: the draft "each week" overpromised — block composition drifts when frequency doesn't divide the rotation length). Spec write-back in the same PR: PRD FR-PL2 acceptance wording + system-logic §8.1 preview line now state the non-calendar-week projection semantics. TDD: bilingual goldens + anti-regression (no 本周/下周/this week/next week in schedule labels) + labels added to the no-trailing-period guard. RedeL10n 98 green; quality gate PASS; zh/en plan-page sim screenshots archived (content and order byte-identical). Note for future seeding: canonical `programTemplate` is an object (`splitType`/`daysPerWeek`) — seeding it as a string drops the plan page to its empty state.

- **Progress page readability: thousands-grouped volume + plain-color legends (audit MINORs)**: new `RedeStrings.formatVolumeKg` wraps `formatKg` output with thousands grouping (21,325; no separator under 1,000; lb mode keeps the single conversion point so 0.5-lb decimals survive — 2,204.5). Swapped in at the 8 volume call sites in `ProgressTabView` (week/session tonnage subs, history rows, chart a11y) **plus the post-session summary volume stat in `TrainTabView`** (same tonnage semantic — review catch, fix-all-affected-surfaces rule); prescription-weight `formatKg` untouched everywhere (train page / adjust sheet / share card — layout-regression guard), including the suspect-set weight line which is a weight, not a volume. Implementation uses `NumberFormatter` (en_US_POSIX + explicit grouping) after independent review caught the hand-rolled grouping loop emitting misplaced commas on negatives ("-,100" — domain is non-negative, defensive fix, failing test first). All three progress legends drop the internal design term 「铁火线 / single ember」 for plain color wording: 「橙色标出本周/新纪录/最高点 / Orange marks this week / the PR / the peak」. TDD: 12 formatter assertions (lb half-pound, sub-1000, millions, negatives, quarter decimals from loadFactor) + bilingual legend goldens + anti-regression (no 铁火线/ember anywhere in legends). RedeL10n 97 green; quality gate PASS; zh/en sim screenshots archived (summary-sheet shot not capturable via auto hooks — flow is in-memory; covered by unit tests). Display layer only.

- **Progress page: suppress mid-week "vs last week" verdict (post-launch audit MAJOR #3)**: the Week scale compared the in-progress current week against the full previous week, so mid-week it naturally showed "Volume −N% vs last week" + the "A lighter week" headline — reading like regression while the user is on pace. `WeeklyComparison` gains `.currentWeekInProgress`; `WeeklyInsight.compare(latest:weeks:todayISO:)` now returns it when the would-be `vsPreviousWeek` verdict lands inside the latest week's Mon–Sun window (Sunday itself still counts as in-progress — rule locked in tests; comparison resumes the following Monday). `firstWeek` / `previousWeekMissing` are not preempted (their copy is already neutral), and an unparseable `todayISO` falls back to the old comparison (defensive). New copy `weekVerdict("inProgress")` 「本周进行中 / Week in progress」 + `weekSubInProgress` 「本周至今 X 组 · Y kg　周结束后显现对比 / X sets · Y kg so far this week. Comparison appears when the week ends」; the ±N% tag on the current-week bar disappears while in progress. No same-days-elapsed precise comparison (explicit non-goal). TDD: 6 new snapshot tests + neutral-copy goldens with anti-regression assertions (no 「较上周」/"vs last week"/% in in-progress lines). Read-only derived layer + copy; canonical write paths untouched. Quality gate PASS; 3-lens independent review; seeded sim screenshots (zh/en, 4-session prev week vs 2-session current).

## 2026-07-03

- **Fix EN today-page signal line week wording ("this week" → "in the past 7 days")**: `TodayEngineCopy.signalLine` renders `sessionsLast7` (rolling 7-day count) but the EN string said "sessions this week" — misleading whenever the rolling window crosses an ISO week boundary (post-launch audit MAJOR #1; zh 「近 7 天」 was already correct and is untouched). Now reads `"\(gapDays)d since last · X/Y sessions in the past 7 days"` ("past" avoids colliding with "since last"). TDD: golden updated + zh-unchanged assertion + anti-regression assertion (EN signalLine must never contain "this week"). Audited all other EN "this week" strings — notifications (calendar weekday reminders), progress page (ISO-week aggregation), volume-boost card (`weekStartISO`), deload (program week) are genuinely weekly, left as-is. Copy-only, no data-path impact. Quality gate PASS; independent review clean; sim screenshots (en/zh) archived.

## 2026-07-01

- **🎉 Rede Strength 1.0 approved & live on the App Store**: Apple review completed (submitted 2026-06-27 16:33 PT, approved in ~4 days, first submission, zero rejections). Store version 1.0 = build 12 (0.2.3). Store URL: https://apps.apple.com/app/rede-strength/id6780301633 (auto-release; up to 24h for worldwide storefront propagation). Listing shipped bilingual (en-US + zh-Hans): name/subtitle/keywords/description/promo text, Health & Fitness category, privacy questionnaire "Data Not Collected", privacy-policy URL both locales, 4+ rating, free.

- **Share card now carries the real App Store link**: `ShareLinks.appStoreURL` (ShareCard.swift) filled with the Apple-provided URL — the pre-launch TODO agreed when FR-SH1 shipped (#611/#612). Share text = tagline + store link instead of the "Find Rede on the App Store" fallback; the rendered card poster keeps the neutral download hint. App-layer constant only, no engine/persistence impact.

## 2026-06-27

- **Version bump 0.2.2 (11) → 0.2.3 (12)**: per the owner's version policy (bump marketing + build together), `MARKETING_VERSION` 0.2.2 → 0.2.3 and `CURRENT_PROJECT_VERSION` 11 → 12 across all 4 configs. 0.2.3 adds the brand-styled choice sheets (native `confirmationDialog` removed from the swap flows) on top of 0.2.2. Still not uploaded (10/11/12 were sim iterations). `xcodebuild` BUILD SUCCEEDED; bundle reports 0.2.3 (12). Owner re-archives 0.2.3 (12) → Distribute.

- **Brand-styled choice sheets (replace native confirmationDialog in swap flows)**: per owner directive ("the switch-day UI must be our brand design, not Apple's native popup"), introduced a reusable `RedeChoiceSheet` (+ `RedeChoiceOption`) component in `RedeComponents.swift` that renders the house bottom-sheet idiom (redeBase surface, drag indicator, Overline header, optional message, 44pt action rows with hairline dividers, ember-emphasized primary, EngraveDivider + Cancel) — distilled from the existing `TrainTabView.moreSheet`/`sheetActionRow` pattern. Replaced all three native `.confirmationDialog`s in `TodayTabView`: FR-TR7 day picker, FR-TR7 scope (just-today / change-rotation-order), and FR-TR6 exercise-swap scope (just-this-time / from-now-on). The two FR-TR7 steps now swap content in place within a single `.sheet` (no stacked popups); `pendingDayOverride` drives the step and `onDismiss` resets it. **Decision logic unchanged** (applyDayOverride/applySwap/rotationOffset/oneTimeDayOverride/engine untouched — presentation layer only). The permanent branch opens the day-sequence editor via an `onDismiss` relay (`pendingSequenceEditor` signal) to avoid same-batch dismiss+present races. Independent code review: 1 MAJOR (resolved via the onDismiss relay) + 2 MINOR (stable ForEach id; required `onCancel`) all fixed. Verified end-to-end in the simulator (both swap flows render in brand style, apply correctly, undo banner shows, sequence editor opens). Remaining native surface: the resume-draft `.alert` (left as-is — destructive draft-recovery confirm; flagged to owner). `xcodebuild` BUILD SUCCEEDED.

- **Version bump 0.2.1 (10) → 0.2.2 (11)**: per the owner's version policy (bump marketing + build together), `MARKETING_VERSION` 0.2.1 → 0.2.2 and `CURRENT_PROJECT_VERSION` 10 → 11 across all 4 configs (app + widget × Debug/Release). 0.2.2 bundles the exercise-swap scope choice (just-this-time / always, #630), the swap-candidate fix (#631), and FR-TR7 "Switch today's session". Still not uploaded (10/11 were sim iterations). `xcodebuild` BUILD SUCCEEDED; bundle reports 0.2.2 (11). Owner re-archives 0.2.2 (11) → Distribute.

- **FR-TR7 "Switch today's session" (one-time training-day override)**: the today page can now temporarily swap today's training day for another in the split ("don't want legs, train chest today") via a today-page entry → pick day → **"Just today (X moves to next session) / Change my rotation order"** (default just-today; permanent opens the day-sequence editor). Header tag "Today (swapped): X" + undo toast "Switched to X today · Y next time". **Engine core only changes when new params are non-default** (golden 311 green): `plan()` gains `dayCodeOverride: String? = nil` (today forced day; invalid → falls back to rotation) + `rotationOffset: Int = 0` (rotation = `sequence[(sessions.count + offset) % len]`). **"Y comes back next session" via the offset**: at session completion (`appendCompletedSession`, same atomic write, if override `dateISO == session.date`) `rotationOffset -= 1` + clear override — cancelling that session's rotation advance so the skipped day is next. Offset is consumed only on completion (no-train = rotation untouched; undo just clears override; `count+offset ≥ 0` never underflows). Both fields open-bag additive (no schema bump); temporary swap doesn't touch the sequence or weekly frequency (no A/B guardrail trigger). Backed by deep research (flexible periodization: choosing the session order day-of doesn't hurt gains and improves adherence). 10 new unit tests (5 engine + 5 writer incl. double-completion); reviewer-audited (core math verified; 2 MINORs fixed); full UI flow simulator-verified (entry → picker → just-today → day switches with engine-computed loads + tag + toast). Spec write-back: PRD FR-TR7 + system-logic §6.0.1a.

## 2026-06-26

- **Fix swap offering exercises the engine can't place (made one-time swap look "unimplemented")**: the today-page swap list filtered only by substitution-group + global allowed equipment, missing the **slot's own equipment preference** (e.g. the lower-day compound squat slot requires `machineClasses`) — so it offered barbell/dumbbell/bodyweight squats that `plan()` rejects, silently reverting (honest no-fake-success) and looking like nothing happened. Added a read-only engine seam `TodayPrescriptionEngine.swapCandidates(for:dayCode:currentIds:equipmentScenario:)` returning the actual slot candidates (same pattern + **slot equipment preference** + scenario + excluding used); the today swap now uses it, so every offered alternative actually applies. `plan()` untouched (golden green). Diagnosed by direct-seeding a valid one-time sub (it applied with the TODAY badge — proving the mechanism was already correct) then driving the full UI in the simulator (alternatives now show only Pendulum/Smith squat → tap → "just this time" → swaps with TODAY badge + engine-computed load + undo toast). 2 new engine unit tests; quality gate PASS. Exposes `equipmentScenario` on `TodayModel`. Engine/data/rotation untouched.

- **FR-TR6 one-time exercise swap ("just this time / always")**: tapping a replacement now asks **"just for today, or always?"** — *always* writes the persistent `exerciseSubstitutions` (existing path); *just this time* writes a date-stamped `oneTimeSubstitutions[original]={actual,dateISO}` that **applies only today and auto-reverts the next day**. Both are open-bag additive (**no schema bump**). **Engine untouched**: `TodayModel` merges persistent + today's one-time subs (one-time wins) into the single `substitutions` map before calling `plan()`, so the engine can't tell them apart → 309 golden engine tests stay green (zero regression); one-time entries never leak into the permanent map. Writer `applyOneTimeSubstitution` prunes stale non-today entries (auto-GC). UI: prescription-row badge "今天换"/"Today" (vs permanent "已换"/"Swapped"); detail-sheet revert text notes the next-day auto-restore; undo routes to `removeOneTimeSubstitution`; honest no-fake-success preserved (dead overrides cleared if `plan()` falls back). 7 new writer/getter unit tests; quality gate PASS; simulator-verified the choice dialog + one-time routing (added `-autoOpenSwap` hook). Spec write-back: PRD FR-TR6 + system-logic. Rotation/engine/data untouched.

## 2026-06-25

- **Version bump 0.2.0 (9) → 0.2.1 (10)**: owner confirmed the whole-row drag (#628) feels good on-device. Per the owner's version policy (bump the marketing version, not just build), raised `MARKETING_VERSION` 0.2.0 → 0.2.1 and `CURRENT_PROJECT_VERSION` 9 → 10 across all 4 configs. 0.2.1 caps this UX-polish wave: custom-plan editing fully drag-based (both editors, whole-row long-press, jitter eliminated) + HealthKit read-only bodyweight + the 90683 upload fix + the pre-launch audit fixes. First upload since build 7. `xcodebuild` BUILD SUCCEEDED; bundle reports 0.2.1 (10). Re-archive 0.2.1 (10) → Distribute.

## 2026-06-24

- **Whole-row drag-to-reorder for both plan editors; exercise editor gains drag** (`PlanDayEditorView` + `PlanDaySequenceEditorView`): per owner request — (1) the within-day exercise editor now reorders via drag (removed the per-row ∧∨ arrows), and (2) both editors are now **whole-row draggable** (day-seq editor's grip handle removed; the whole row is the target). Both reuse the proven gap + `.global` + hysteresis no-jitter logic. Initiation is now **long-press (0.2s) anywhere on the row** — required so a quick swipe still scrolls the (scrollable) exercise list and still triggers sheet-dismiss; only a hold-then-drag reorders (iOS-standard). The earlier "long-press feels bad" was really the coordinate-space jitter (since fixed), not the hold. Swap/remove (⇄/⊖) buttons remain tappable; up/down move actions retained as VoiceOver `accessibilityActions`; exercise reorder recomputes muscle-frequency impact on drop. Build + quality gate PASS; exercise-editor **layout** verified via simulator screenshot (arrows gone, ⇄/⊖ present, subtitle updated). **Drag gesture not self-verified this round** — a macOS Notification Center overlay blocked computer-use control of the simulator; the gesture is byte-identical to the already-verified day-seq drag plus the long-press wrapper. Held unmerged pending owner tactile confirmation (long-press feel + buttons + scroll). Rotation/engine/data untouched.

- **Drag-reorder jitter, root cause found — gesture coordinate space** (`PlanDaySequenceEditorView`): the real cause (missed in the prior two attempts) was that the `DragGesture` is attached to the grip handle, which is moved by `.offset(dragTranslation)`, while the gesture measured translation in the default `.local` coordinate space — so as the row moves, the measurement reference moves with it, creating a self-feeding wobble that twitches *even while the pointer is held still* (this matches the recording: each "jitter" lasted ~2s of sustained motion = a feedback loop, not input). Fix: `DragGesture(minimumDistance: 6, coordinateSpace: .global)` — screen-absolute reference, stable translation. (The hysteresis dead-band from the prior attempt is kept as complementary input-noise protection.) **Verified via computer-use real-drag in the simulator** (not shipped blind): holding the pointer still mid-drag is now completely static (the former feedback source); boundary wiggle is clean; crossing swaps cleanly; release settles and commits with the impact preview updating; both drag directions checked. Rotation/engine/data untouched. Quality gate PASS. Rides build 9.

- **Drag-reorder jitter, take 2 — switch to gap architecture (no array mutation during drag)**: the `.transaction` fix below did NOT cure the jitter, because a row's re-position is a parent-VStack layout animation that a child `.transaction` can't suppress; as long as the array is mutated mid-drag, the dragged row's slot gets parent-animated and fights the finger offset. Rewrote `PlanDaySequenceEditorView` drag internals to the standard jitter-free pattern: **the array is not mutated during the drag** — the dragged row follows the finger via a plain `dragTranslation` offset (never inside any `withAnimation`, so nothing can fight it), the other rows open a one-row gap via `gapOffset` driven by `dropTargetIndex` (updated inside `withAnimation` → smooth), and the order is committed exactly once in `onEnded` (gap offsets equal post-commit slot deltas, so displaced rows don't visually move on commit; only the dragged row springs into its slot). Removed `dragOffset`/`consumedShift`/`.transaction`; added `dragStartIndex`/`dropTargetIndex`/`dragTranslation`. Rotation/engine/data untouched. Independent reviewer verified the gap math + no-jitter mechanism; one drop-landing timing note flagged for real-device check. Quality gate PASS; final build reinstalled to simulator. Rides build 9 (no upload yet); jitter is a temporal artifact — real-drag verification required.

- **Fix drag-reorder jitter on swap** (`PlanDaySequenceEditorView`): when two rows got close and a swap fired, the dragged row twitched. Root cause: the array reorder ran inside `withAnimation(.spring)`, so the dragged row's *layout slot* animated while its finger-follow `.offset` was compensated instantly — the two fought at the swap instant. Fix: `.transaction { if isDragging { txn.animation = nil } }` on the row — the dragged row moves instantly (offset-driven, no jitter) while displaced rows still spring-slide. Moved the `onEnded` state reset (`draggingCode`/`consumedShift`) inside the settle `withAnimation` so the drop keeps its spring (reviewer MAJOR: otherwise `isDragging` flips false early and the settle snaps). Crisper displaced-row spring (damping 0.9). Rotation/engine/data untouched. Rides build 9 (sim-testable; no upload yet). Reviewer-audited; jitter is a temporal artifact — real-drag verification required.

- **Build bump 0.2.0 (8) → (9)**: `CURRENT_PROJECT_VERSION` 8 → 9 (all 4 configs); `MARKETING_VERSION` stays 0.2.0. Build 8 (long-press drag) was the device-tested build; this build carries the drag-feel fix + Health-caption removal, so it needs a unique higher number. `xcodebuild` BUILD SUCCEEDED; bundle 0.2.0 (9).

- **Day-sequence reorder: drop long-press, drag via handle** (real-device feel fix): the #622 long-press-then-drag felt laggy on device (had to hold ~0.22s; early finger movement aborted the recognizer). Replaced with an immediate `DragGesture(minimumDistance: 6)` attached **only to the per-row grip handle** (44pt touch target). Review caught that a whole-row `highPriorityGesture` swallows the sheet's swipe-to-dismiss — handle-only scoping fixes that (drag handle = reorder; drag elsewhere = dismiss/scroll). Restored `dragOffset = 0` on grab init. Rotation/engine/data untouched. Subtitle → "Drag the handle on each row to reorder". One independent reviewer; MAJOR (sheet dismiss) + MINOR (init) fixed. Quality gate PASS; simulator-verified layout (drag feel pending real-device).

- **Remove Apple Health section caption in Settings**: deleted the `healthSectionNote` line ("reads your latest weight for on-device display only…") under the Apple Health section in Settings, per owner request, and removed the now-unused string. **In-app display text only** — the Info.plist HealthKit usage strings (the system authorization dialog) are untouched and remain compliant.

- **Build bump 0.2.0 (7) → (8)**: `CURRENT_PROJECT_VERSION` 7 → 8 across all 4 build configs (app + widget × Debug/Release); `MARKETING_VERSION` stays 0.2.0 (still the beta cycle, not an App Store release). Build 7 (the 90683-fixed archive) already went up to TestFlight, so the next upload — carrying the day-sequence drag-reorder (#622) — needs a unique higher build number. Re-archive at build 8 before Distribute. `xcodebuild` BUILD SUCCEEDED; bundle reports 0.2.0 (8).

- **FR-PL7② day-sequence editor: arrows → long-press drag-to-reorder** (real-device UX polish): replaced the per-row ↑/↓ move buttons in `PlanDaySequenceEditorView` with a custom long-press-then-drag reorder (0.22s lift + haptic, live follow-the-finger with a half-row dead-zone `while` loop that consumes multi-slot fast drags without jumping or oscillating, shadow lift, grip-handle affordance). Rotation logic, engine, and data are untouched — interaction layer only (no `List`/`.onMove`, per the open-row axiom). Accessibility: VoiceOver can't drag, so each row carries equivalent "move up/down" `accessibilityActions` (boundary actions gated by `if` conditions, not `.disabled()` which is a no-op inside the accessibility action builder); the move helper resolves the live index by `code` to avoid stale captured `idx`. Subtitle reworded ("Touch and hold a row to drag and reorder"), day-name `lineLimit(1)` against the fixed row height. Three independent reviewers (reorder math / gesture-conflict+axiom / a11y regression) audited; all real findings fixed. Quality gate PASS; simulator-verified layout (drag *feel* pending real-device acceptance — simctl can't synthesize drag gestures). Spec write-back: system-logic §「日序编辑器 UI」.

- **Fix App Store upload rejection (HealthKit, error 90683)**: re-added `NSHealthUpdateUsageDescription` to `Info.plist` (Base) + `en.lproj`/`zh-Hans.lproj` `InfoPlist.strings`. Apple's ITMS upload validator requires **both** HealthKit purpose strings (`NSHealthShareUsageDescription` *and* `NSHealthUpdateUsageDescription`) whenever the HealthKit entitlement is present — regardless of whether the app writes. The audit had removed the Update string ("read-only ⇒ no write string"), which is correct app-behavior reasoning but fails Apple's static check, so `Distribute → Upload` was rejected with code 90683. The re-added string honestly states Rede never writes to Health and only reads body weight for on-device display; it is never displayed (the app never requests write authorization), it only satisfies the validator. **Entitlement untouched** — no signing change. Verified by unpacking the built `Rede.app` (both keys present; version 0.2.0 (7), MinimumOSVersion 17.0 unchanged) + quality gate PASS. Re-archive 0.2.0 (7) and re-upload.

## 2026-06-23

- **Marketing version bump 0.1.0 → 0.2.0 (build 7)**: `MARKETING_VERSION` 0.1.0 → 0.2.0 across all 4 build configs (app + widget × Debug/Release); `CURRENT_PROJECT_VERSION` stays 7. **New owner policy**: from now on version bumps raise the marketing version (not just the build number, which had been frozen at 0.1.0 since launch). Applied to the pending build 7 — the FR-PL6/PL7 user-customizable-plan release is the natural 0.2.0. (Supersedes the "MARKETING stays 0.1.0" note on the build-7 bump below.) Build number still increments per upload (Apple uniqueness). `xcodebuild` BUILD SUCCEEDED.
- **Build bump 0.1.0 (6) → (7)**: `CURRENT_PROJECT_VERSION` 6 → 7 across all 4 build configs (app + widget × Debug/Release); `MARKETING_VERSION` stays 0.1.0 (later raised to 0.2.0 — see entry above). Build 6 (#588) was the last uploaded build; everything merged since needs a unique higher build number for a fresh TestFlight upload — FR-T5 fix-data wiring (#589), FR-PR7 e1RM milestones (#590), the FR-EX2 exercise-detail content waves (#591-594, #598, #599), the action-library expansion 123 → 165 (#595-597), and the FR-PL6/PL7 user-customizable-plan feature (#600-607). Re-archive at build 7 before Distribute.
- **FR-PL6/PL7 spec write-back** (#607): wrote the user-customizable-plan contracts back into the canonical specs after the feature shipped — PRD §5.4 (new FR-PL6/PL7/PL6.1/PL6.2-6.3 rows + non-goals), Master architecture (added `plan customization edit` + `plan-frequency adjustment` writer mutation classes), system-logic §8.2 (new "用户自定义训练计划" system contract: data/writer/editor/day-sequence/impact-guardrail/honesty/0-card/screenshot hooks). Docs-only.
- **FR-PL6 slice S9b — add-exercise picker** (#606): the editor's "添加动作" affordance + picker sheet. New engine seam `TodayPrescriptionEngine.addCandidates(dayCode:currentIds:equipmentScenario:)` (same-pattern-family, equipment-whitelisted FR-EQ1, excludes used, no new pattern slots); tapping appends to the working list and the picker stays open for multi-add. Added exercises still get engine-computed load/progression (userPinned path). +3 engine tests. Closes the literal "what each day trains" half (add/remove/swap). Independent review (no blocker; 3 MINOR + 1 NIT fixed). Quality gate PASS (308 RedeTrainingDecision tests); simulator-verified.
- **FR-PL7② slice S10 — day-sequence editor** (#605): `PlanDaySequenceEditorView` — reorder the week's training days (permutation-only, no add/remove, preserves A/B split + 2×/week). New public engine seams `defaultDaySequence` / `nextDayCode` / `resolvedDaySequence`; live "下一个训练日将变为 X" preview (rotation anchors to completed sessions). Adversarial 3-dimension review confirmed 7/0-dismissed findings, all fixed (MAJOR: `isCustomized` must exclude an override that equals the default order; MINOR: `nextDayCode` negative-guard, ppl-ul/nil test coverage, a11y hint + move-button labels). Quality gate PASS; simulator-verified.
- **FR-PL6/PL7① slice S9 — training-day editor** (#604): `PlanDayEditorView` reached from a tappable Plan-page day row — reorder (up/down) + same-family swap (equipment-whitelisted) + remove, with a muscle-frequency impact preview (neutral guardrail, never blocks). Apply/restore-default via the gated writer. Independent review fixed 1 BLOCKER (swap candidates missing `allowedEquipment` → FR-EQ1) + 3 MAJOR (off-main impact compute, clear stale error on reopen, dedup list) + a stale-result race. Quality gate PASS; simulator-verified end-to-end (data → engine → both Plan + Today render custom plan with engine-computed loads).
- **FR-PL6/PL7 slice S6+S8 — app wiring + copy + editor seed** (#603): Today + Plan now feed `appData.planCustomization` through `PlanCustomizationBridge` into the engine (no customization = unchanged); 4 `SessionStore` write wrappers (gated write + reload, isolated `planSaveErrorText`); engine helper `defaultDayExerciseIds` (editor start = coach plan, shares `slotCandidates` with `plan()`); bilingual editor copy (`PlanCustomizationCopy`). Dormant until S9 UI → zero regression. New app files registered in `project.pbxproj` (explicit file lists). Quality gate PASS.
- **FR-PL6.1 slice S7 — impact/guardrail data** (#602): pure `PlanCustomizationImpact.compute` — per-muscle weekly-frequency delta + `droppedBelowTwice` (the core A/B-split-erosion guardrail), from a one-week projection. Honest limitation: `primaryMuscle` single-value frequency approximation (no contribution weights yet). Zero engine coupling; no caller yet. Quality gate PASS (297 tests).
- **FR-PL6/PL7 slice S2+S3 — engine consumption** (#601): `plan()` / `PlanWeekProjection.weeks()` gained a default-empty `customization` param (empty ≡ current, golden zero-change). Custom day = `userPinned` slots (user picks exercise + order; engine still computes load/reps/progression/verdict — decision-first preserved); day-sequence override must be a permutation (graceful fallback). Golden tests unchanged. Quality gate PASS.
- **FR-PL6/PL7 slice S1+S5 — data foundation + writer** (#600): open-bag `AppData.planCustomization` read view (`PlanCustomization{dayPlans, daySequence}`, defensive read, absent = nil) + 4 `CanonicalSessionWriter` methods (apply/remove day-plan + day-sequence, idempotent, open-bag-preserving). No schema bump (additive). Dormant → zero regression. Quality gate PASS.

## 2026-06-22

- **FR-EX2 — progressions/regressions + §7.1 safety notes** (#599): added 退阶/进阶 (easier/harder variants) to the 93 exercises that already have technique cues, plus conservative §7.1 safety notes to 60 of them. Additive content fields (absent = not shown, zero regression). Real-evidence discipline maintained; independent review. Quality gate PASS.

## 2026-06-21

- **FR-EX2 — cues + evidence for the 42 new exercises** (#598): bilingual technique cues + adversarially-verified real citations for the wave-16/17/18 additions (brings the with-cues set to 93). Additive. Quality gate PASS.
- **Action-library expansion wave-18** (#597): +11 standard exercises (additive, zero behavior change) → 165 total.
- **Action-library expansion wave-17** (#596): +13 standard exercises (additive) → 154.
- **Action-library expansion wave-16** (#595): +18 standard exercises (additive, 123 → 141).
- **FR-EX2 batch 4 (B-tier)** (#594): technique cues + real evidence for 16 high-frequency isolation exercises.
- **FR-EX2 batch 3 (A-tier)** (#593): technique cues + real evidence for 16 high-frequency compound + machine exercises.
- **FR-EX2 batch 2** (#592): technique cues + real evidence for 12 high-frequency compound exercises.
- **FR-EX2 R1 wrap-up ③ — exercise-detail framework + first content** (#591): exercise detail page gains 技术要点 (bilingual, 4 cues) + 循证依据 (real source + tappable "view source") for the 7 main lifts. Owner chose **real evidence** — a research workflow (14 agents) web-verified every citation, each adversarially WebFetch-checked for existence/authority/support; front-squat downgraded to cues-only (only a commercial source found, not evidence-grade). Cues are accepted safe movement form, not medical advice. Additive fields (123 exercises zero regression). Independent review (a11y + test coverage fixed). Quality gate PASS (app 274 + 8 packages).

## 2026-06-20

- **FR-PR7 R1 wrap-up ② — e1RM estimated milestones** (#590): Progress milestones now also surface estimated-1RM milestones (e.g. "Bench 140kg · estimated") when the estimated 1RM crosses a *higher* tier than the measured one — explicitly labeled "estimated", color de-emphasized, never impersonating a real lift; estimate uses the suspicious-set-excluded snapshot (bad data can't false-trigger); named milestones stay barbell-only (FR-EQ2). +9 engine tests; milestone engine touched but old behavior unchanged (empty estimate = old logic, pinned by tests). Quality gate PASS.
- **FR-T5 R1 wrap-up ① — "fix data" coach-card wiring** (#589): the coach card's "修数据" path (previously hard-stubbed to 0) is wired — when history has suspicious sets, Today shows a "有记录待核对" card → navigates to the Progress data-quality section. Single composition source `DataQualityComposer` keeps the Today count and the Progress section in lockstep; navigation only (no fake "fixed"). Independent review fixed 2 MAJOR (overflow "+N more" reconciliation; cost-of-count comment). Deferred: auto-scroll to the data-quality section. Quality gate PASS.
- **Notifications → Time Sensitive (+ build 6)** (#588): rest + weekly reminders raised to the Time Sensitive interruption level so they banner even when another app is foreground or Focus is on (default `.active` was being silently downgraded to notification-center-only). `CURRENT_PROJECT_VERSION` → 6.
- **Build bump 0.1.0 (4) → (5)** (#587): unique build number for the TestFlight archive carrying the notification fix + the 4-phase UX polish.
- **Fix: rest-end notification not firing on device** (#586): diagnosed + fixed the rest-timer notification not delivering on real hardware.
- **Settings notification toggle — drop two sub-copy lines** (#585): removed redundant explanatory subtext under the notification toggles.
- **UX polish Phase 3 — clarity** (#584): tappable hints + de-duplication + final close-out across the app.
- **UX polish Phase 2 — visual correction** (#583): de-"AI-card" pass, ember restraint, dead-space cleanup.
- **UX polish Phase 1 — motion/transitions** (#582): fixed "hard jumps" with eased transitions (reduce-motion guarded).
- **UX polish Phase 0 — app-wide feel foundation** (#581): press feedback + haptics across the app.
- **Build bump 0.1.0 (3) → (4)** (#580): unique build number for the TestFlight archive carrying FR-PL3/4 plan adjustment.
- **FR-PL3/4 slice 4 — frequency-proposal spec write-back** (#579): wrote the FR-PL3/4 frequency-adjustment contract into the specs (system-logic §8.1, PRD FR-PL3/4), closing the FR-PL frequency feature on the doc side.
- **FR-PL3/4 slice 3 — Plan-page adjustment card (first user-visible)** (#578): the Plan page surfaces the frequency proposal as a preview→adopt/snooze card + an adopted/rollback state (mutually exclusive, single record no-stack), reusing the 0-card surface; honest `planSaveErrorText`. Quality gate PASS.
- **Feedback inbox → hello@rede.fit** (#575): changed the feedback recipient email from a personal Gmail to `hello@rede.fit` (stop exposing the personal address).

## 2026-06-18

- **FR-PL3/4 slice 2 — adopt / rollback write path (dormant)**: the data layer for adopting + rolling back a plan-frequency adjustment. `CanonicalSessionWriter.applyFrequencyAdjustment(from:to:)` writes `programTemplate.daysPerWeek = to` (the approved "program config scalar edit" category) **plus** an open-bag `planAdjustment {kind, fromDaysPerWeek, toDaysPerWeek}` rollback record — **no schema bump**; `rollbackPlanAdjustment()` restores `fromDaysPerWeek` + clears the record (single-step reverse, idempotent when absent). New `AppData.planAdjustment` read view (`PlanAdjustmentRecord`, absent = nil). `SessionStore.applyFrequencyAdjustment` / `rollbackPlanAdjustment` wrappers (gated write + reload, so the plan projection + today prescription pick up the new `daysPerWeek`). +3 write tests (apply persists days+record & preserves history/profile/unknown keys & schema; rollback restores+clears; no-record idempotent). **No UI caller yet** (Plan adjustment card = slice 3) → dormant, zero behavior change. Quality gate PASS (8 packages + `xcodebuild`).

- **FR-PL3 slice 1 — PlanAdjustmentEngine (frequency proposal, pure brain)**: starts the Plan-page "调整建议 → 采纳 → 可回滚" feature. **Route B** (owner-approved): bootstrap the preview/apply/rollback framework with a **non-MLE proposal source**, since the spec's only adjustment source — MLE balance recommendations (FR-PL5) — isn't built and is the most expensive XL block. New `PlanAdjustmentEngine.frequencyProposal` + `PlanAdjustmentProposal` (`RedeTrainingDecision`, pure, code-not-copy): from a flattened adherence signal (`plannedDaysPerWeek` + recent weekly session counts) it proposes **reducing the weekly plan to a sustainable frequency when the user has sustainedly trained below plan** (supportive friction-reduction, §7.3 no shaming). Conservative guards (owner chose "conservative", MVP starting values pending calibration): ≥4 weeks of data, median ≤ planned−1, floor 2 days/week, only when it actually reduces. +8 host tests (proposes / on-track / insufficient-data / at-min / floor / median odd-even / determinism). **No caller yet** (preview + apply/rollback write = slice 2; Plan UI = slice 3) → zero regression. Increase-frequency + MLE balance (FR-PL5) deferred to the same framework. Adopt may change program structure (`daysPerWeek`) = existing "program config scalar edit" category + an open-bag rollback record → **no schema bump** (slice 2). Quality gate PASS (8 packages + `xcodebuild`).

- **Build bump 0.1.0 (2) → (3)** (repo governance): `CURRENT_PROJECT_VERSION` 2 → 3 across all 4 build configs (app + widget × Debug/Release). 0.1.0 (2) was already uploaded to Apple (Jun 15), so the R1 TestFlight archive (FR-T5 + FR-TR10 + FR-NT + the 5 prior FFs) needs a unique, higher build number to upload. `MARKETING_VERSION` stays 0.1.0. Re-archive at build 3 before Distribute.

- **FR-NT2 slice 4 — weekly training reminder + notification spec write-back**: completes the notifications feature. Settings gains a "每周训练提醒 / Weekly training reminder" toggle (same value-first opt-in + serialized `isNotifBusy` pattern as rest-end, DRY'd into a shared `notifToggleRow`). `SessionStore.syncWeeklyReminders` (run on `loadToday` + after pref save, idempotent) builds the policy's reminders → resolves codes via RedeL10n → `replaceWeekly`, and cancels `managedWeeklyIds` no longer active (so turning weekly off / reducing clears stale registrations). `WeeklyTrainingReminderPolicy` exposes `managedWeeklyIds`; weekly = 2 fixed reminders (Mon 09:00 "new week" / Thu 18:00 "keep pace", `UNCalendarNotificationTrigger(repeats:)`). New `RedeL10n` weekly copy (per-`messageCode` title/body + settings label/hint, §7.3 neutral — no streak/shame/pressure, +tests). **Spec write-back**: system-logic §5 gains the notification-preference write category + a Notifications contract note (ephemeral, never canonical, no remote, code-not-copy, rest-end lifecycle + weekly fixed-2, value-first auth, MVP timing pending calibration); the §0 package inventory corrected (RedeNotifications created → **8 packages**, 3 reserved); Master §7 + PRD FR-NT1/FR-NT2 marked `🟡 R1 partial`. Quality gate PASS (8 packages + `xcodebuild`; SettingsSheet 2/2 ForgedCard). Completes the FR-NT chain (slices 1–4). **Notification delivery / weekly trigger not host-verifiable — owner TestFlight acceptance pending** (weekly timing/frequency are MVP starting values, calibrate on device).

- **FR-NT1 slice 3 — rest-end notification goes live (Settings toggle + permission + persistence + wiring)**: FR-NT1 is functional end-to-end. Settings gains a "通知 / Notifications" section with a "休息结束提醒 / Rest-timer alert" toggle (mirrors the periodization toggle): turning it on requests system authorization **value-first** (only on first opt-in, not at launch); if granted, the pref persists; if denied, it rolls back and shows an honest "enable in iOS Settings" hint. The pref is **open-bag additive** in canonical AppData (`notifications.restEndEnabled`, absent = off) via a new gated `CanonicalSessionWriter.applyNotificationPreferences` — **no schema bump**. `SessionStore` schedules the rest-end local notification at rest-begin (when the pref is on) and cancels it on rest-finished / session-end / flow-clear, via `RestNotificationPolicy` + `UNUserNotificationCenterScheduler`; copy resolved from new `RedeL10n/NotificationCopy` (neutral, §7.3: no hype / streak-pressure). Prefs + locale are cached (refreshed on `loadToday` + save) so rest-begin never reads disk. Weekly toggle/scheduling (FR-NT2) + spec write-back = slice 4. +6 tests (4 write-gate: default-off / persist+readback / preserve-other-keys / disable; 2 copy: bilingual + §7.3 no-forbidden-tone). Quality gate PASS (8 packages + `xcodebuild`; SettingsSheet 2/2 ForgedCard). Independent code-review: no blockers; 2 MAJOR fixed on-branch — (1) `endSession()` now cancels the pending rest notification so abandoning/ending mid-rest can't fire a stale "next set" alert; (2) the Settings toggle is serialized (`isNotifBusy` + `.disabled` + UI state set only after the persist succeeds) so an auth-then-quick-flip can no longer desync disk vs UI. Plus MINOR fixes (refresh the notification locale cache on language change; `masterEnabled` MVP-assumption comment; reset the denied hint on sheet open). **Notification delivery / permission prompt not host-verifiable — owner TestFlight acceptance pending.**

- **FR-NT1/2 slice 2 — platform adapter + app linking (dormant)**: adds the `#if os(iOS)` `UNUserNotificationCenterScheduler` (the only `UserNotifications` importer) implementing `NotificationScheduling` — rest = `UNTimeIntervalNotificationTrigger` (one-shot), weekly = `UNCalendarNotificationTrigger(repeats:)`, idempotent cancel/replace, alert+sound (no badge). The protocol now takes **resolved title/body** (code→copy resolved by the app via RedeL10n before the call), so the adapter is zero-copy / zero-RedeL10n-dependency / trivially Sendable (no stored state, `.current()` per call); added `ResolvedWeeklyReminder`. Linked `RedeNotifications` into the **app target** (`project.pbxproj`: XCLocalSwiftPackageReference + XCSwiftPackageProductDependency + PBXBuildFile + Frameworks, mirroring RedeTrainingDecision). `SessionStore` imports it and holds the `notificationScheduler` seam (default adapter) — **not called yet** (rest-lifecycle schedule/cancel + prefs + copy + permission = slice 3), so this slice is **dormant, zero behavior change**. Host `swift test` excludes the iOS adapter (the `#if os(iOS)` seam pattern); `xcodebuild` verifies the adapter conforms + the package links into the app. Quality gate PASS (8 packages + `xcodebuild` BUILD SUCCEEDED). No CI-config change (package was registered in slice 1).

## 2026-06-17

- **FR-NT1/2 slice 1 — RedeNotifications package + pure notification policies (owner-approved package creation)**: creates the reserved `RedeNotifications` package (Master §5) for local notifications — FR-NT1 rest-end reminder + FR-NT2 weekly training reminder. Mirrors the RedeWidgetShared boundary: a Foundation-only **pure policy layer** (host-tested) + a future (slice 2) `#if os(iOS)` UNUserNotificationCenter adapter behind a `NotificationScheduling` seam. This slice ships only the **pure brain**: `RestNotificationPolicy.scheduleOnRestBegin` (gated by master/rest-end prefs + valid duration → a typed `ScheduledRestNotification`, **code-not-copy**) + `WeeklyTrainingReminderPolicy.weeklyReminders` (gated → 2 fixed reminders: Mon 09:00 "new week" / Thu 18:00 "progress check" — **MVP starting constants pending calibration**) + typed plans (`ScheduledRestNotification` / `WeeklyTrainingReminder` / `NotificationPreferences`) + the `NotificationScheduling` protocol seam. **Local-only** (no remote/push per §9 → no aps entitlement, no Info.plist key — zero signing/CI-capability risk). Notifications are derived/transient — never canonical, never a source of truth, no network. +8 host tests (pref gating master/rest-end/weekly + zero-duration, plan fields, weekly fixed-2 weekday/code, determinism). Registered in `quality-gate.cmd` + `rede-ci.yml` EXPECTED_PACKAGES + Master §5 (moved reserved → present, **8 packages**). **No app caller yet** (adapter + SessionStore wiring = slice 2; Settings/permission + FR-NT1 live = slice 3; FR-NT2 = slice 4) → zero runtime impact. Notification prefs will persist as **open-bag additive** booleans (absent = off) — no schema bump. Quality gate PASS (8 packages + `xcodebuild`). **Contains a CI-config change (`rede-ci.yml` EXPECTED_PACKAGES) — high-risk class per §3.5, merged manually by the owner, not auto-merged.**

- **FR-TR10 slice 4 — spec write-back (warmup), docs-only**: §6.3's warmup logic goes from "deferred" to the implemented contract — warmup is **flow-only ephemeral guidance**: `WarmupLadderEngine` (pure, `RedeTrainingDecision`) → `WarmupStep` LoadGrid-snapped conservative ladder; `TrainFlowState` models it as a `.activeSet` overlay (`warmupPointer`/`isWarmingUp`, **no new Phase**); warmup never enters `events`/`observationsByExercise`/persistence → no NextSetEngine poisoning, no PR/volume/e1RM/level pollution, no schema; restore regenerates it from the working-set pointer. Two-layer contract: warmup = transient guidance (not persisted), working sets = the only canonical truth. Added a dated status note (engine #566 / flow #567 / UI #568; ladder values MVP pending calibration; **skip-learning deferred** — current = static conservative + skippable). The §6 status table's "warm-up generator 未做" → shipped; the §6.0.2 "热身生成后置" reference corrected; PRD FR-TR10 marked `🟡 R1 partial`. Completes the FR-TR10 chain (slices 1–4). §9 discipline (timeless contract + dated status). No code change.

- **FR-TR10 slice 3 — warmup card UI (first user-visible)**: the focus-training hero card gains a warmup state. When `flow.isWarmingUp` (the start of an exercise that has a warmup ladder), the hero morphs to a warmup card — a "热身 N/M" overline + the current step (空杆 / 动作模式预热 / weight × reps) + a "完成热身组" primary action (→ `advanceWarmupStep`) and a "跳过热身" secondary (→ `skipAllWarmup`); the working-set table + next-up line are hidden until warmups finish/skip, then the card morphs to the first working set. New `SessionStore.advanceWarmupStep()`/`skipAllWarmup()` (in-memory flow mutations — **not** events/draft, so warmup stays ephemeral). New `RedeL10n/WarmupCopy` bilingual strings (+3 tests). Warmup renders **inside the existing hero `ForgedCard`** (no new card — TrainTabView stays 2/2 budget). Additive overlay: setState/restState/setTable logic is unchanged (only gated by `!isWarmingUp`), so the shipped flow is byte-identical when there's no warmup. Quality gate PASS (7 packages + `xcodebuild`; TrainTabView 2/2 ForgedCard). **Visual/interaction not yet screenshot-verified — owner TestFlight acceptance pending** (manual-acceptance steps provided). Ladder values remain MVP starting values (engine, slice 1). Spec write-back = next slice.

- **FR-TR10 slice 2 — warmup wired into the focus-training flow (dormant overlay, zero state-machine change)**: `TrainFlowState` gains a warmup overlay — in-memory `warmupPointer` + derived `warmupStepsForCurrentExercise` (calls `WarmupLadderEngine` on the current exercise's planned work weight + catalog facts) + `isWarmingUp` + `currentWarmupStep` + `advanceWarmupStep()`/`skipAllWarmup()`. The key design choice: warmup is a **UI overlay on `.activeSet`, NOT a new `Phase`** — so the reducer's existing transitions, guards, event log, draft replay, and persistence are byte-for-byte unchanged, which avoids every regression the understand workflow flagged (NextSetEngine poisoning, index drift, draft same-form). Warmup ticks/skips never append events and never touch `observationsByExercise`/`completedInCurrentExercise`/`exerciseIndex` → they cannot poison NextSetEngine or pollute PR/volume/e1RM/level, and draft restore is unaffected (warmup is transient, regenerated from the working-set pointer; `advanceExercise` resets it). **No UI consumes `isWarmingUp` yet** (UI = next slice), so this slice is dormant — zero visible behavior change. +8 flow tests, incl. the code-regression-guard core: *the first working-set recommendation/target is identical whether or not warmup methods were called*. The existing 252 RedeTrainingDecision tests (the no-warmup baseline) stay green → zero regression. The originally-planned separate "zero-regression proof" slice is subsumed (the design changes no existing reducer/persistence/stats path). Quality gate PASS. No schema, no persistence change, no new package.

- **FR-TR10 slice 1 — WarmupLadderEngine (pure brain, no UI/flow/persistence)**: new `WarmupLadderEngine` + `WarmupStep` in `RedeTrainingDecision` turns a working set's top weight + exercise facts (loadType/equipment/kind/startWeight + unit) into a deterministic, conservative warmup ladder. **Ephemeral by design** (the key FR-TR10 decision, validated by a 4-lane understand workflow): warmup is flow-only guidance — it will **never** enter `observationsByExercise`/canonical, so it cannot pollute PR/volume/e1RM/level, and needs **no schema bump**. Per-loadType: external = empty-bar prep (barbell/smith) + 50/70/90% (compound) or 60/80% (isolation/accessory) reps-descending ladder, LoadGrid-snapped, strictly `< work`, monotonic, deduped; bodyweight/band = one no-weight movement-prep step (compound only); bodyweight-plus = pure-bodyweight then ramped added load; assisted = **reversed** (more assist = lighter/safer, ramping toward the work assist); unknown loadType = no warmup (safe degrade). Values are **conservative MVP starting values pending owner TestFlight calibration** (empty bar 20kg→45lb, ladder %, levels). +13 host tests (six loadTypes, light-load collapse, lb-grid snap, monotonic-below-work, determinism). Pure function, **no caller yet** (flow wiring = slice 2, UI = slice 4) → zero regression surface, zero stats impact, zero schema. Quality gate PASS.

- **FR-T5 audit NIT cleanup**: follow-up to the full-chain audit, low-risk polish. (1) Extracted the ISO-week anchor into a tested `WeekAnchor.isoWeekStart(_:timeZone:)` in `RedeTrainingDecision` (was an untested `static` on the app-layer `TodayModel`) — the volume-boost weekly suppression/adopt/dismiss key now has SPM coverage for the year-end week-53/cross-year boundary, leap day, Sunday-membership, and same-week-stability edges (+6 tests); `timeZone` is an explicit param (default `.current`) so tests pin it deterministically and the timezone dependency is on the surface. Both call sites (`TodayModel.loadOutcome`, the volume-adopt button) now call `WeekAnchor`. (2) The swap coach-card body no longer repeats the exercise name already shown in its title (readability). (3) `CanonicalSessionWriter.removeCoachActionDismissal` is now documented as intentionally not surfaced in the UI (dismiss is a one-way throttle signal) rather than reading as missing wiring. Quality gate PASS (7 packages + `xcodebuild`; TodayTabView 0/1 ForgedCard). No behavior change. Remaining lower-value NITs (runtime timezone-switch snapshot, `SchemaMigrator.plan()` dry-run fields with no consumer) left documented per YAGNI.

- **FR-T5 full-chain audit hardening**: post-merge comprehensive review of the whole coach-action write path (slices 0–7) via an 8-dimension audit + adversarial-verify workflow (32 raised → 23 confirmed, **0 blockers**; verdict GO_WITH_FIXES). Fixed **2 MAJOR** + a cluster of MINOR: (1) **[MAJOR · UI]** the undo banner's 5s self-destruct timer raced the undo gated write — a failed undo could clear the banner before the user could retry, and volume-undo has no persistent fallback; the timer now skips auto-dismiss while a coach write is in flight (`!isSaving`). (2) **[MAJOR · e2e]** swap-adopt's post-write `stillHasOriginal` check defaulted to `true` when the reloaded `model` was nil, which would delete the just-written override (silent no-op swap); now defaults to `false` (keep the override when it can't be confirmed). (3) **[error isolation]** coach-write failures now surface via a dedicated `SessionStore.coachSaveErrorText` (cleared on Today appear) instead of the global `saveErrorText`, so a training/settings save failure can no longer be mis-attributed under the coach card; the `coachWriteAttempted` flag is removed (net simplification). (4) **[honesty]** volume adopt computes the week anchor from `Date()` (not the stale `model.now`), so a cross-midnight/cross-week adopt writes the current week's key and the card doesn't immediately reappear. (5) **[chained swap]** adopting a swap on an already-swapped exercise now writes the override on the **root** originalId (root-collapse), avoiding inconsistent A→B / B→C keys. (6) **[docs]** SchemaMigrator `downStep(11)` + system-logic now state the 11→10 downgrade is **best-effort lossy** (drops populated coach containers, non-production only); §FR-PL2's stale `current=9` corrected to `11` with 9→10/10→11 migration notes; §6.4a throttle wording `连续`→`累计` (cumulative, no reset). (7) **[DRY/safety]** extracted `PrescriptionReason.isCeilingOrGraduationMilestone` (exhaustive switch, no `default`) shared by the engine's stalled-exercise filter and the milestone notes — the compiler now forces both to stay in sync when a ceiling/graduation case is added. +3 tests (down-migrate lossy lock; `weekStartISO=""` degradation ×2). Quality gate PASS (7 packages + `xcodebuild`; TodayTabView 0/1 ForgedCard). No change to the shipped happy path. Remaining NITs (move `isoWeekStart` into a tested package, timezone snapshot, `removeCoachActionDismissal` visibility, etc.) recorded for future.

- **FR-T5 slice 7 — spec write-back (coach-action full chain), docs-only**: reconciles the FR-T5 6a–6c reality + contract into the canonical specs (§9 discipline: timeless contract prose, erodable status in dated notes). System-logic adds a new **§6.4a CoachActionEngine contract** (clean-input primitives, priority dataReview>swap>volume, the three action kinds with exact trigger reasons, gentle throttle ≥2/≥1 with rationale, gated adopt + single-step undo, red lines + a dated implementation-status note); **§5 write contract** gains the volume-boost intent class + a note that adopt/dismiss/undo flow through the one gated writer (with dismiss having a writer-level reverse but no surfaced UI undo); **§6.5.10** clarifies that muscle-level set-count coaching ("2–4 sets of X", with muscle name + sets) is the deferred MLE-gated capability vs the shipped **frequency-only** engine; **§8.0.1** notes dataReview as a future `DataQualityReport` consumer (deferred — `dataFindingCount` held at 0). **Master §7** adds the volume-boost intent write class + adopt/dismiss/undo-through-the-writer note. **PRD FR-T5** marked `🟡 R1 partial` (#558–562; data-review trigger deferred), contract sources updated to §6.4a/§6.5.10/§5/Master §7. Verified by a 3-lane spec-vs-code reconciliation workflow (15 claims cross-checked): **1 WRONG fixed** (spec implied dismiss had a surfaced UI undo — it does not), 3 imprecise tightened (exact swap-trigger reason set excluding `repCeilingReached`; swap's three undo entries incl. the transient toast; volume-undo no-persistent-entry limitation), 2 completeness gaps closed (the `sessionsLast7≥0` guard; the ≥1-vs-≥2 throttle rationale); the rest verified to match code. No code change, no schema, no gate (the reconciliation IS the gate). Completes the FR-T5 full write-path chain (slices 0–7).

- **FR-T5 slice 6c — coach card adopt + single-step undo**: the coach card gains adopt actions and undo. **Exercise swap**: card "换个动作/Swap exercise" CTA → opens the topped-out exercise's FR-EX2 detail sheet in swap mode (`ExerciseDetailTarget.swapIntent`), where the same-family alternatives list (reusing `alternatives(for:)`) upgrades from read-only Text to selectable rows; tapping one writes `applyExerciseSubstitution(originalId:actualId:)` (gated, merged #557) → plan() (#556) swaps it into the prescription. The swapped-in row shows a persistent "已换/Swapped" badge (reads the persisted `TodayModel.substitutions` map — survives card reload); tapping that row opens the sheet with a "换回原动作/Restore" entry. **Volume**: card "知道了/Got it" CTA → `applyVolumeBoost(weekStartISO:)` (merged #559) using `TodayModel.isoWeekStart(now)` (single source, no view-local recompute). **Honesty red line**: volume adopt only records "acknowledged, stop nudging this week" — it does **not** add a session or change the plan; copy carries no "added/scheduled/workout/已加/已补" implying-write words (+test). **Undo**: a transient bottom banner ("已换成 XX·撤销" / "本周不再提醒补量·撤销", ~5s, on the Today root overlay so it survives the card disappearing) reverses via `removeExerciseSubstitution`/`removeVolumeBoost`; swap also has the persistent detail-sheet undo. Single-step reverse gated write, no undo stack (owner-decided). All adopt/undo route through a new shared `SessionStore.performCoachWrite` helper (isSaving mutex + clears `saveErrorText` on attempt + gated writer + reload); `dismissCoachAction` (6b) folded into it (de-dupe). **Red-line fix**: Today had no `saveErrorText` surface — adopt/undo write failures would be silent; added an inline error row under the context line (reuses `saveFailedLine`, `redeRisk`, same as TrainTabView). **dataReview cross-tab nav deferred** — `dataFindingCount` is held at 0 so the card never renders; wiring nav now = dead code (YAGNI / unverifiable), it ships with the future DataQualityReport slice. Design chosen via a 3-approach design-review workflow (minimal/honest-first won 23/25). Pure UI + already-merged writers/engine/wrappers — no schema, no new package, low-risk. Independent multi-dimension review (4 lenses, adversarially verified: 15 raised → 12 confirmed) fixed on-branch: (1) **MAJOR** — undo write-failure unconditionally cleared the undo banner, locking the user out of retrying a volume undo (it's the only entry); now the banner survives a failed undo so the user can retry. (2) **MAJOR** — `saveErrorText` is global, so a training/preferences save failure would surface under the coach card with wrong semantics; Today now only shows the error after a coach write was attempted *from this screen* (`coachWriteAttempted`, reset on tab re-entry). (3) **MINOR/honesty** — on plan()'s graceful fallback (chosen alternative not a valid slot candidate) the toast claimed "Swapped to X" while the prescription was unchanged; now the success toast only fires if the original exercise actually left the prescription, and the dead override is cleaned up otherwise. (4) +banned-word test coverage for the new swap-UI copy keys; NIT (exerciseId-nil half-card — engine-guaranteed unreachable) documented. Quality gate PASS (7 packages + `xcodebuild` BUILD SUCCEEDED; TodayTabView 0/1 ForgedCard). Adopt/undo interaction not yet screenshot-verified (owner TestFlight acceptance pending; manual-acceptance steps provided). Spec contract write-back batched to slice 7.

- **FR-T5 slice 6b — Today coach card UI (first user-visible) + dismiss wiring**: the coach actions become visible. `TodayModel.loadOutcome` now flattens the verdict signals (`sessionsInLast7Days` / `plannedDaysPerWeek`), the day's topped-out/graduated prescription reasons (stalled exercise ids), and the persisted throttle state (`AppData.coachDismissals` / `volumeBoostWeeks` + the **real** local ISO-Monday week anchor) into `CoachActionInput`, runs `CoachActionEngine`, and exposes `coachActions: [CoachAction]`. `dataFindingCount` is held at 0 (the Today-side DataQualityReport is deferred), so v1 surfaces **exerciseSwap** (topped-out) + **volumeBoost** (frequency) only. `TodayTabView` renders the top-priority action (≤1 per screen, between the verdict line and the exercise list) as a **card-free** redeSurface panel — icon + bilingual title/body + a "Not now" dismiss button — keeping the 1-card ForgedCard budget for the hero. Dismiss calls new `SessionStore.dismissCoachAction(actionKey:)` → gated `applyCoachActionDismissal` (#559) → reload, so the throttle (slice 6a) closes the loop: the UI dismisses the **engine-produced** `action.actionKey` (no hand-wired keys). New `RedeL10n/CoachActionCopy` renders signal+impact decision-receipt copy per reasonCode (+9 host tests incl. red-line guards: **no shaming, no algorithm/AI/"best" banned words, volume copy carries no muscle name or set count** per §6.5.2). Adopt (swap→alternatives picker / volume→applyVolumeBoost) + undo + dataReview cross-tab nav = slice 6c. Read/throttle path only — no schema, no new package, low-risk class. Quality gate PASS (7 packages + `xcodebuild` BUILD SUCCEEDED; TodayTabView 0/1 ForgedCard). Independent code-review: no blockers; 1 MAJOR fixed on-branch (`isoWeekStart` silently fell back to `now` on the never-hit calendar-failure path — a per-day-varying non-Monday string would misalign the weekly throttle and, in slice 6c, pollute `volumeBoostWeeks` on write; now `assertionFailure` + empty-string safe-degrade so it can only over-show, never query the wrong week) + 2 copy-guard blind spots tightened (English banned-word / "set" checks now word-boundary token-matched). Card visual not yet screenshot-verified (owner TestFlight acceptance pending). Also fixes a stale example dismissal key in `VolumeBoostAndDismissWriteTests` (`volumeBoost:belowWeeklyPlan` → `volumeBoost:<weekISO>`, doc clarity only).

- **FR-T5 slice 6a — coach-action throttle (gentle policy, pure engine) + actionKey (#560)**: `CoachActionEngine` now throttles its output by the persisted dismissal counts / adoption state (consumption policy; owner delegated the "gentle" tuning). exerciseSwap/dataReview: hidden after **≥2** consecutive dismissals of the same `actionKey`; volumeBoost: hidden for the week once adopted (`volumeBoostAdoptedThisWeek`) or dismissed (≥1) that week. `CoachAction` gains `actionKey` (`dataReview` / `exerciseSwap:<id>` / `volumeBoost:<weekISO>`) — the UI dismisses that key, and the engine reads `dismissals[key]` next time (closed loop). `CoachActionInput` gains `weekStartISO` / `dismissals` / `volumeBoostAdoptedThisWeek` (**defaults = no throttle, zero regression**); the app (slice 6b) injects them from `AppData.coachDismissals`/`volumeBoostWeeks` + the current ISO week. +4 tests (stable actionKeys / swap+data suppressed after 2 / volume suppressed after adopt-or-dismiss-this-week). Pure engine, no UI/write; the existing 227 tests stay green (231 total). Quality gate PASS.

- **FR-T5 slice 5 — volume-boost & dismiss write entries + undo (#559)**: persists coach-action adoption/dismissal intents to the schema-11 containers (data foundation; consumption policy is slice 6). `CanonicalSessionWriter` gains, all through the single gated orchestration (load → mutate → DataHealth gate → pre-write backup → atomic save): `applyVolumeBoost(weekStartISO:)` (dedupes one boost per ISO week into `coachAdjustments.volumeBoosts`) + `removeVolumeBoost`; `applyCoachActionDismissal(actionKey:)` (accumulates a per-key `count` in `coachState.dismissed`, feeding the future frequency throttle) + `removeCoachActionDismissal`; empty-key guard (`.emptyKey`). New defensive read views `AppData.volumeBoostWeeks` / `coachDismissals` (`?? []`/`[:]` for missing inner — **resolves review M-1**: no crash when the inner array is absent). +8 tests (boost dedup/undo/remove-absent idempotent, dismiss accumulate/undo, empty-key throws, open-bag preservation). Structural writes only — whether a boost should surface is the engine's call (#558), not this layer; nested open-bag merge keeps sibling/top-level keys intact. No UI caller yet (slice 6), so merging adds no automatic writes. Quality gate PASS.

- **FR-T5 slice 4 — CoachActionEngine (pure brain, no UI/write) (#558)**: new `CoachActionEngine` in `RedeTrainingDecision` (Master §5: coach actions live here) turns app-flattened primitives (`CoachActionInput`) into a priority-ordered list of typed coach actions — **dataReview > exerciseSwap (topped-out) > volumeBoost (frequency)**. Pure function, zero copy, no engine-output persisted. **volumeBoost is frequency-only** (`count = planned − last7`, **no muscle name, no set count** per §6.5.2: never guess muscles without contribution weights), with four guards: active day only (`train`/`light`; never on rest/deload), not a brand-new user (`totalSessionCount>0`), `plannedDaysPerWeek>0`, and behind plan (`last7<planned`). exerciseSwap hints come from the day's topped-out/graduated prescription reasons; dataReview from the data-quality finding count. +8 host tests (priority order / four volume guards / data / swap order / empty). Read-only brain — no UI (slice 6), no write (volume/dismiss writers = slice 5), no schema; no caller yet, so merging is zero-runtime-impact. Quality gate PASS.

- **FR-T5 slice 3 — exercise-swap override write entry + single-step undo (#557)**: write path for adopting a coach swap. `CanonicalSessionWriter.applyExerciseSubstitution(originalId:actualId:)` open-bag-merges `storage["exerciseSubstitutions"][originalId]=actualId` through the existing single gated orchestration (load → mutate → DataHealth gate → pre-write backup → atomic save; honest throw, disk untouched on failure); structural guards reject empty ids and self-substitution. `removeExerciseSubstitution(originalId:)` is the single-step undo (reverse gated write, key removal, idempotent — no separate undo stack). Realizes the §5 "saved-session exercise replacement" category. Layering: this package (RedePersistence → RedeDomain only) does only the structural write; whether `actualId` is a valid catalog entry / same substitution family is validated by the catalog-holding app layer (slice 6's adoption UI). Safety net: even an invalid `actualId` is harmless because the engine override (#556) only activates when the target is a valid slot candidate, else it gracefully falls back. +6 tests (persist+reload / open-bag preservation / multi-write+undo / remove-absent idempotent / empty-id throws / self-substitution throws). Quality gate PASS. Write entry has no UI caller yet (slice 6), so merging adds no automatic writes.

## 2026-06-16

- **FR-T5 slice 2 — prescription engine consumes exercise-swap overrides (#556)**: the Today prescription engine now applies forward exercise-swap overrides (schema-11 `exerciseSubstitutions`, originalId→actualId), priority **override > sticky > preferredId > rank**. After the default pick, if its id has an override whose target is still a valid candidate for that slot (same pattern + passes FR-EQ1/slot filters + not already used), the target replaces it; otherwise it gracefully falls back. New read-only `AppData.exerciseSubstitutions` view (parses the schema-11 container, skips non-strings, empty when absent). `TodayModel` injects it; `TodayPrescriptionEngine.plan` gains a defaulted `substitutions:` param. **Read-only consumption — no canonical write (write entry is slice 3); the override table is always empty pre-adoption, so plan() output is byte-identical (existing golden/prescription tests stay green = zero-regression baseline)**. +3 engine tests (override applies / invalid target falls back / empty map == no-arg) + AppData view test. code-regression-guard. Quality gate PASS.

- **FR-T5 slice 1 — schema 10→11 for coach actions (owner-signed)**: data foundation for the full write-path FR-T5 (coach actions). `SchemaVersion.current` 10→11 (owner explicitly approved the bump, Master §7). `SchemaMigrator` gains an additive `upStep(10→11)` that seeds three canonical open-bag containers when absent — `exerciseSubstitutions{}` (forward exercise-swap overrides), `coachAdjustments.volumeBoosts[]` (volume-boost intents), `coachState.dismissed[]` (dismiss intents + frequency throttle) — without overwriting existing ones (idempotent, mesocycle precedent); `downStep(11→10)` removes exactly those three keys (round-trip identity with up). **Containers are seeded empty only — no writes, no engine consumption, no read-path change**, so existing schema-10 data upgrades with zero behavior change. +4 migration tests (seed / additive / no-overwrite / 10↔11 round-trip); 6 hardcoded schema-10 assertions updated for the bump (SchemaVersionGuard / RealExport / 4 RedePersistence write tests, mostly to `SchemaVersion.current`). schema-migration-guard: purely additive + reversible + migrate stays in-memory at decode (disk only via the backed-up gated writer). Quality gate PASS. **High-risk schema class — not auto-merged; owner merges manually.** Subsequent FR-T5 slices (plan() override consumption, write entries, engine, UI) follow per the signed write-path plan.

- **Strength milestones (FR-PR7, R1, #554)**: the Progress tab gains a "Milestones" section — achieved plate thresholds for the barbell big lifts (press / squat / hinge / overhead), e.g. "Bench press 100 kg". New pure `StrengthMilestoneCatalog` in `RedeLocalSnapshot`: kg ladder 60/100/140/180, lb ladder 135/225/315/405, **stored separately, no interconversion** (evaluated against the user's unit). **Actual-only** — uses `ProgressSnapshot.bestWeightKg` (real completed top set), and deliberately does **not** emit e1RM-estimated milestones (credibility red line: never pass an estimate off as an achieved lift). Eligibility (barbell + big-lift patterns) is injected from the catalog at the `ProgressModel` layer (package stays catalog-decoupled); best weights come from the `statsRecords`-based snapshot, so suspect data can't trigger a milestone. +7 host tests (dual-unit, no-interconvert, below-threshold, determinism, edges). `ProgressTabView` renders it card-free (0-card budget). Thresholds are industry-standard plate milestones, **MVP starting values pending calibration**. v1 scope: barbell big-lifts + actual-only; e1RM-estimated and machine/dumbbell milestones deferred (machines can't impersonate barbell since eligibility is barbell-only). Read-only — no canonical write, no schema/new package, no gate. Quality gate PASS. PRD §5.3 FR-PR7 marked partially implemented. Logic host-tested; visual not yet screenshot-verified.

- **Plan tab weekly schedule (FR-PL2, R1, #553)**: the Plan tab goes from "cycle bar + placeholder" to a real schedule — this week / next week, each `daysPerWeek` training days, every day showing its training-day name + exercise count + movement-pattern composition. New pure `PlanWeekProjection` in `RedeTrainingDecision` reuses the prescription engine's existing `daySequence` (rotation) + `slots` (per-day) — **no change to any prescription logic** (zero behavior regression; the existing 215 engine tests stay green). The first projected day equals what Today shows right now (same `sessions.count % sequence.count` rotation — the two pages can't diverge); +5 host tests (rotation offset, dedup, same-source, edges). `SessionStore.loadPlanProjection` runs the same clean pipeline as Today. `PlanTabView` renders it card-free (0-card budget); the placeholder note narrows to "adjustment previews & rollback arrive later" (FR-PL3/4 still pending). `RedeL10n` adds the schedule copy; reuses `trainingDayName` + `movementPatternLabel`. Read-only projection — no canonical write, no schema/new package, no gate. Quality gate PASS. PRD §5.4 FR-PL2 marked implemented. Logic/same-source host-tested; visual layout not yet screenshot-verified.

- **Readiness Widget write-side wired (R1 slice 1, #550)**: the widget extension + `RedeWidgetShared` types/seam shipped in #431, but the app never wrote the App Group snapshot, so at runtime the widget only ever showed the honest placeholder (PRD's "delivered" mark was spec drift). This wires the write side: `SessionStore.loadToday()` projects the day's verdict into a derived read-only `ReadinessWidgetSnapshot` (off-main), writes it to the App Group, and triggers a WidgetKit reload — `.ready` only (no fake/overwrite when unreadable; silent on failure). Copy comes from new pure `RedeStrings.widgetHeadline` / `widgetAdvice` composers reusing the Today page's `verdictStatus` / `receiptConclusion` / `verdictHeadline` source (+6 host tests). `completeOnboarding` / `completeAndPersistSession` already call `loadToday`, so the widget refreshes after a workout/onboarding. Derived snapshot never touches canonical AppData, no network (master §12 W-1). PRD §0 / §5.9 FR-WD1 corrected from "✅ delivered" to "🟡 write-side wired, pending real-device acceptance"; lock-screen family noted as not yet implemented. Quality gate PASS (7 packages swift test + `xcodebuild` BUILD SUCCEEDED). Independent code-review: no blockers; 2 majors (Sendable capture, locale window) fixed on-branch. **Runtime widget refresh needs owner real-device acceptance before merge.**

- **Exercise detail sheet (FR-EX2, R1, #552)**: Today's prescription rows are now tappable → a read-only detail sheet showing the lift's metadata — type (compound/isolation/accessory), movement pattern, primary + secondary muscles, equipment, and same-family alternatives (entries sharing a substitution group, excluding self/deprecated, first 6). Moves metadata off the main surface into detail (PRD "no metadata pile-up on main screens"). New `RedeL10n/ExerciseDetailCopy` maps the catalog's internal codes (17 patterns / 17 muscles / 8 equipment / 3 kinds) to bilingual labels with raw-code fallback; +6 host tests (full-code coverage, fallback, bilingual). Honest scope: the catalog's `contraindicationHint`/`evidenceTag` are empty, so v1 shows structured metadata only — no technique cues/evidence prose/progressions, and muscles are listed without fabricated contribution weights (catalog limitation). Read-only, reuses the catalog — no engine/schema/new package, no gate. Quality gate PASS (7 packages + xcodebuild); TodayTabView still within its 1-card budget. PRD §5.7 FR-EX2 marked partially implemented. Logic/copy host-tested; sheet visual layout not yet screenshot-verified.

- **Progress consistency calendar (FR-PR5, R1, #551)**: the Progress tab gains a current-month training calendar between the chart and history — days with a logged session are marked with an ember dot, answering "is my training frequency steady?". Neutral by design (no streak, no missed-day shaming, per copy baseline §7.3). New pure `ContinuityCalendar` in `RedeLocalSnapshot` builds a Monday-start month grid from the same module's `SnapshotDayMath` (Hinnant civil-days — no Calendar/timezone), with +5 host tests (month length, leading pad, today marking, out-of-month dates ignored, invalid anchor). `ProgressModel.loadOutcome` takes an injected `now`, derives trained days from the unfiltered `records` (a day with any logged session counts, even if some sets were quality-flagged), and computes the month. `ProgressTabView` renders it card-free (respects the 0-card budget) — ember fill = trained, ring = today, each cell VoiceOver-reads "date[, trained]". `RedeL10n` adds the calendar copy (+5 host tests incl. a no-pressure-words guard). Read-only projection — no engine/schema/new package, no gate. Quality gate PASS (7 packages + xcodebuild). Logic host-tested; pure visual layout not yet screenshot-verified.

- **🎉 MVP achieved — R0 shipped to TestFlight (internal testing), version `0.1.0` (#548)**: the clean iOS rewrite's minimum training loop is complete and live on TestFlight. All MVP slices done — M0–M5 ✅ (four-tab IA, today verdict + prescription engine, focus-training flow, progress surface, cold-start onboarding, settings) and M6-1/2/3 ✅ (app icon/meta, local-first privacy + disclaimer, feedback channel); **M6-4 (TestFlight build distribution) completed** — real-device signing on the paid Apple team `47K95GV3X5` + bundle id `com.tinsab.rede` (#546), widget bundle id `com.tinsab.rede.RedeWidget` (#546), Archive → App Store Connect → internal test group. The owner confirmed the §1 eight-point real-device acceptance passes. 7 clean Swift packages with golden/contract tests, exercise catalog at 123 entries (wave-15). Per the MVP implementation plan's §11 termination flow, that bounded-lifecycle doc is now retired (long-term conclusions absorbed into the roadmap, system-logic §10 gap index, and the PRD; this entry is the completion-of-record). Successor planning: `docs/REDE_PRD.md` (R0 = the shipped scope) + roadmap P0 validation / R1 fast-follow.

## 2026-06-13

- **Content wave-8 — Smith machine class (#48cddd6, 91→97)**: new `smith` equipment class + 6 exercises (smith squat / bench / incline press / overhead press / calf raise / shrug) for the previously-uncovered commercial guided-rail rack. Rank 920-970 tail-appended (zero prescription change; substitution candidates only). `smith` joins `machineClasses` (guided ⟹ fixed-equipment, self-consistent); load grid same as barbell (2.5 kg / 5 lb); scenario gate unchanged (home/minimal auto-exclude, commercial passes). `CatalogContractTests` asserts catalogVersion=wave-8 and count 97. Owner-reviewed.
- **Content wave-7 — plate-loaded Hammer machines (#e339d05, 85→91)**: 6 commercial-gym plate-loaded Hammer machines (chest / incline / row / pulldown / shoulder press, pendulum squat), rank 860-910 tail-appended (zero prescription change); enriches commercial users' swap lists. 128 decision-package tests green.
- **Abandon active session exit (#022c12c, owner feedback)**: the "End training?" confirm sheet gains a red "Abandon this session" option (discards the in-progress session, writes nothing, returns to Today) alongside "End training" (saves completed sets). `SessionStore.abandonActiveSession` = endSession (clears flow + draft, no canonical write). Simulator-verified.
- **Bodyweight set-table weight column removed (#1fc9a70, owner visual feedback)**: for bodyweight exercises the set-row table drops the Weight column entirely (header + rows), leaving Sets | Reps | RIR. Simulator-verified.
- **Bodyweight rep-adjust interaction (#1be3568, wave-6 finish)**: the bodyweight training flow hides the weight rail and makes reps the primary control ("adjust by reps").
- **Bodyweight loadType engine (#b0a85c6 — largest single engine change)**: `loadType=bodyweight` ungated — progression by reps (push-ups/pull-ups), prompt to raise difficulty at 25 reps, weight stays 0; +6 calisthenics exercises; bodyweight added to home/minimal scenarios; Today shows reps. Remaining: full adjust-by-reps train interaction (later UI slice); bodyweight-plus/assisted/band still gated.
- **Content wave-5 — new movement classes (#876f3fc, 73→79, behavior change)**: knee-flexion / shrug / abs as new patterns + new slots (leg-extension + crunch on leg day, shrug on pull/upper days); goldens updated to reflect the longer training days.
- **Content wave-4 (#febb6a5, 61→73)**: 12 common variants for family thickness, rank tail-appended (zero prescription change).
- **Content wave-3 (#e5f0231, 49→61)**: 12 exercises filling weak spots across families, rank tail-appended (zero prescription change).
- **LoadGrid real equipment load grid (#2c9a0aa)**: per-equipment realistic load steps, unit-native "round up not down" so lb users land on real 5 lb grid stops and are never shown an unachievable weight.

## 2026-06-11

- **Machine merged-class split (#95fe952, wave-2.1)**: 9 exercises' original ids re-valued in place — plate-loaded 3 / selectorized 6; slot equipment preference upgraded to set-membership matching; zero behavior change (golden-proven), per the §6.1 id-immortality rule.
- **Content wave-2 — four equipment families (#b7fecef, 34→49)**: 15 exercises (barbell 6 / dumbbell 1 / cable 4 / machine 4), rank tail-appended.
- **§6.2 Should batch 2 (#158aeb6, owner call B, wave-1.1)**: `loadFactor` tonnage denomination + seven engineering debts cleared in one pass.
- **Content wave-1 — gap-clearing batch (#22de895, 31→34)**: 3 dumbbell exercises clear all 4 known coverage-matrix gaps (content pipeline's first run).
- **Content-system Blocker schema PR (#6047c2a, mvp-2)**: the six §6.1 blocker items landed — `loadType` safety gate + per-entry step + `kind` axis split + equipment registry.
- **Exercise content system P0 (#3fb73f3, owner call)**: catalog JSON-ization (single source `Resources/exercises.json`, 31 entries migrated from the hardcoded array, golden-proven zero behavior change), order-independent matching (first-match → (rank,id) ascending), display names moved into the catalog, id immortality, and `CatalogContractTests` (the coverage-matrix golden caught 2 real gaps human review had missed twice). New doc `docs/REDE_EXERCISE_CONTENT_SYSTEM.md` registered. (Includes the second-review schema revision #0100d74 and the assisted-equipment recording decision #d208d25 as design trace.)
- **FR-EQ1 equipment awareness + craft-spec promotion (#6162f4f, owner ACD)**: the equipment answer goes from "stored only" to "actually consumed" — home-dumbbell/minimal users no longer get barbell/cable prescriptions; `EquipmentAccess` scenario whitelist + slot hard-filter; +7 dumbbell catalog entries; fixed across all affected surfaces (TrainFlowState / draft restore / swap candidates); `CleanProfile.equipmentScenario`. Separately, `REDE_CRAFT_SPEC.md` was promoted from its `.ai-tmp` working draft into `docs/` and registered in DOCS_MANIFEST (Canonical "工艺规格" row).
- **One-panel axiom — whole-app de-carding (#e3e8d30, owner directive "everything's a card, very AI")**: a two-agent container audit reversed the diagnosis — the AI-feel came from sheet surfaces, scattered bordered frames, and empty-state hero overuse, not from per-screen nameplates. Three slices: sheets back to forged base + grabber; scattered frames cleared to text-level + engrave dividers; the "one-panel axiom" added to design-language §12 plus a per-view ForgedCard budget check in `quality-gate.cmd`. Build SUCCEEDED; simulator-verified.

## 2026-06-10

- **M6-3 feedback channel finalized (#b2f66a4)**: in-app feedback to xuhaochen122@gmail.com — subject carries the version (for triage), body pre-fills a guide line + a context line (version/build/iOS/device/language/unit, visible and deletable in compose, transparent), and an honest fallback shows the address when mailto can't open. `URLComponents` construction prevents injection/encoding errors; L10n +3 strings + anchor test. Simulator-verified fallback path (no-mail-app device) + URL round-trip script.
- **Settings page craft rework (#966b812, owner directive — direction B "signal keys")**: the Settings page (the app's only screen previously built with no design-truth source — tokens right, craft layer missing) reworked into an "instrument/inspection panel": panel nameplate header, machined segmented switches, a device spec-plate as the single hero, S2 engrave-divider groups, and progressive disclosure. Design truth written back to rede-app.html + the rede-12 direction. zh/en simulator full-flow verified; gate 7 packages 290 tests PASS.
- **Onboarding four-question polish (#9c6fe63, owner directive — direction A "inline single-question edit")**: editing an answer changes from "re-answer all four" to "tap a row to edit one" with a change receipt; first-launch fix (four answers → one-tap return to the result card, no redundant re-write); the success haptic moved to after the write actually succeeds (honesty); VoiceOver completed. Design truth written back to rede-app.html + rede-11b. zh/en full-flow simulator-verified; gate 7 packages 290 tests PASS.
- **FR-PL1 honest Plan placeholder (#540, user real-device feedback)**: the Plan tab's fake timeline and non-persisting toggles were taken down entirely and replaced with an honest placeholder — the user caught a fake switch not persisting, and the correct fix was an honest empty state, not making the fake real.
- **Chinese-UI English-popup fix (#543)**: a launch preference timing gate + an explicit localized Cancel button — Chinese-system users no longer saw English system dialogs.
- **M6-2 local-first privacy + disclaimer (#542)**: Settings gains a Privacy section stating only code-provable facts (verified zero network code / zero network entitlement before writing): stored on-device by default, no network, no account, no third-party analytics, deleting the app deletes local data; HealthKit not mentioned (not shipped). L10n +2 + §7.4 contract test (must contain "本机/on-device"; bans absolutes — never/100%/anonymous/HIPAA). FR-SE4 disclaimer reconciled with the README (fitness not medical). zh/en simulator-verified; L10n 56 tests PASS.
- **M6-1 app metadata (#541)**: app icon direction A "load-grid rail" (owner call; B wordmark / C plate lost) — forged dark base + engraved rail + ember main stop, single generation source `assets/app-icon/make_icon.swift` (zero-dependency CoreGraphics, change the icon = change the script); launch screen → LaunchBackground colorset (#15130F, kills the light-mode launch flash); CFBundleDisplayName=Rede; version 0.0.1 → 0.1.0 (pbxproj MARKETING_VERSION ×4, widget follows). Simulator home-screen verified. (iOS 18 dark/tinted icon variants deferred — not required for TestFlight.)
- **M5-2 Profile/Settings minimal shell — M5 milestone close (#539)**: a Settings sheet (low-frequency entry, not a tab) — FR-SE1 kg/lb global switch (storage/engine always kg; lb is a render-layer ×2.2046, 0.5 lb step; ~22 kg literals → unitLabel; exact input parses in the display unit with a round-trip drift guard); FR-SE3 language+unit persisted through the write gate (`applyPreferences`, closing the M0-3 loop); FR-SE2 background four-answer view+edit = re-run onboarding; FR-SE4 disclaimer / FR-SE5 feedback mailto / FR-SE6 export statement. L10n +16, write gate +3; gate 287 tests PASS. Review 3 MAJOR (write-gate mutual exclusion / saveErrorText cross-talk / a View-layer kg leak) + 3 MINOR fixed.
- **M5-1 first-launch onboarding — two parts**: **M5-1b UI (#536, direction B "instrument-card stepper", design truth rede-11b)** — `OnboardingView` four-question stepper card + a result card reading the real engine prescription + honest retry on write failure; the needsOnboarding rule treats unreadable data as never-onboard; launch hooks `-forceOnboarding`/`-skipOnboarding`. **M5-1a engine (#535)** — `ColdStartPrior` first-exposure scaling (novice 0.5 / some 0.75 / proficient 1.0, firstExposure only, overridden once a record exists) + `OnboardingPlanInit` (4 answers → template; ≥5 days ppl else upper-lower, clamp 2..6) + write-gate `applyOnboarding` entry, 13 tests. Simulator full-flow verified (novice DB bench 15 kg = 30×0.5 → Today same → no re-prompt on relaunch); gate 279 → 290 tests PASS.
- **Real-device fix — set table follows engine truth (#538)**: the set table and the hero card now share one source (active/future rows follow the engine's real values).
- **Real-device fix — quick-adjust no longer reverts on panel close (#537)**: an adjusted weight is retained with the current set.
- **M5-3 quick-adjust load-grid into iOS (#534, owner-approved prototype #533)**: the quick-adjust panel reworked to a load-grid — in-package `AdjustOptionsBuilder` (stop generation: follow / last-set / plan / light-heavy, value-deduped fixed priority + ascending, 8 tests) and `AdjustPreview` (preview = the same engine function as persistence, with fatigue / below-floor / RIR-nil contract tests); the UI load-grid (real-ratio stop placement + min spacing, single ember pointer, reps/RIR direct-select bands, RIR "—" = not recorded → nil, preview row, haptic vocabulary, reduced-motion fallback). Simulator full-flow verified; gate 7 packages 262 tests PASS.
- **Quick-adjust craft upgrade — load-grid direction (#533, owner feedback "not premium enough, a bit rigid")**: the load-grid (scale-rail) direction won and was written back to rede-app.html.
- **M5 design-first — quick-adjust redesigned as a "semantic load-grid bar" (#532)**: prototype write-back + design-research trace ahead of the M5-3 implementation.

- **M4-4 in-session adjustment control (user-directed UX hardening)**: two user-reported issues from real use. (1) The Progress segmented control only responded to taps on the label text — the `.plain` button style hit-tests opaque pixels only, so the transparent segment area was dead; fixed with `.contentShape(Rectangle())` (benefits every SegControl use). (2) "The prescription is a hard template" — diagnosis: the engine already follows actual execution (adjust set 1 and the next-set recommendation plus the next session's prescription both follow), but the quick-adjust entry was invisible (a bare tappable number with no affordance) and weight only moved in ±2.5 kg steps. Fixes, with zero new component surfaces: a small slider glyph next to the weight so it reads as tappable; a one-time hint line ("Tap the weight to adjust — suggestions follow your change") that disappears permanently after first use; and the adjust row's weight value became a decimal-pad text field (any precision, clamped ≥0, unparseable input reverts — never guessed) alongside the ±2.5 quick steppers. The flow-back is now pinned as an explicit contract test (`AdjustmentFlowBackTests`): adjusted set → next-set recommendation follows the actual weight → completed-session write records actuals only → next push-day prescription seeds from the adjusted baseline and never pulls back to the catalog start. Root-cause work for first-exposure start weights (training-level scaling) remains queued in M5-1 onboarding. Verified: quality gate PASS — 7 packages 253 tests green; zh/en train-screen screenshots with the new affordance archived; segmented-control hit area and text-field entry are on the manual acceptance list (tap interactions are not automatable via simctl). Independent review (2 MAJOR + 4 MINOR/NIT, all fixed in-slice): the real catch — tapping "Log set" while the weight text field still had uncommitted input silently logged the stale value (the decimal pad has no return key, so `onSubmit` was dead code and blur ordering is not guaranteed); logging now force-commits the field first, and the keyboard gained an explicit Done toolbar button. The flow-back contract test was also strengthened from "≥ baseline" to the exact double-progression outcome (3×8 at rep ceiling with RIR 2 → 52.5, change == increase), and the one-time hint is explicitly pinned to "entry discovered" semantics.
- **M4-3 Progress page on real data — M4 milestone complete**: the Progress tab now answers "did training work" from canonical history. The prototype skeleton is unchanged (Session/Week/Cycle segmented scales, verdict-first hero, monochrome chart with the single-ember mark, caption) but every word and number is engine-derived. Session scale: latest session by-lift volume bars, ember + PR tag on the record-setting lift, verdict/top-set/e1RM sub-line. Week scale: weekly-volume bars (honesty adaptation: the catalog has no muscle-attribution weights yet, so by-muscle bars and the body map cannot be rendered truthfully — bars are by ISO week instead, serving FR-PR3's "vs last week" directly), ember on the current week with a delta tag. Cycle scale: real e1RM trend line for the most-trained lift with the ember on the peak (no fabricated deload point — MVP has no cycle model), verdict in the PRD §5.3 voice ("Bench press is still trending up. Estimated 1RM is up 6 kg over the last 4 sessions."). The FF Development block (muscle levels, FR-PR6) is omitted rather than faked. New conservative-styled sections the prototype never drew: History (FR-PR1 — newest-first rows with PR badges, tap opens a set-by-set detail sheet) and Data (FR-PR4 — behavioral suspect lines like "lat pulldown set 1 at 227 kg — probably a typo" plus a dropped-records count; correction entry deferred to M5 with the edit-class write gate). Trust expressed through behavior (§3.4): suspect sets are excluded from all stats — trends, PRs, verdicts, history volumes — while staying listed in Data and untouched in canonical; simulator proof shows a 227 kg typo with prior 55 kg history losing its fake PR/top-set to the honest verdict. New package plumbing: `RedeLocalSnapshot` gains TrendInsight (4-point window, flat band ±2.5 kg, calibrating under 2 points, key-exercise pick) and WeeklyInsight (adjacent-ISO-week comparison, no forced comparison across gap weeks) — 12 new tests; app links the package (ProgressModel maps clean view → snapshot records). `RedeL10n` gains the Progress engine-copy layer (verdict/sub/caption/history/data-quality lines, `formatE1Rm` rounding to 0.5 kg) with the forbidden-words regression extended to ban "confidence/置信度" — 7 new tests; the M0-2 static placeholder keys (fake muscle bars, Lv.15 development rows, planned-deload copy) are retired. Real bugs caught on simulator this slice: raw Epley floats leaked into UI ("est 1RM 69.66666666666666 kg" → formatE1Rm), and a typo'd set could claim the PR ember and the verdict line (→ suspect-set exclusion). Launch-arg scaffolding: `-progressScale session|week|cycle`. Independent review (2 MAJOR + 5 MINOR + 2 NIT, all fixed in-slice): insight calls moved out of the view into ProgressModel (thin-app-layer contract restored); gap weeks no longer read "Week one on the books" — `WeeklyComparison` distinguishes `firstWeek` from `previousWeekMissing` with honest copy for each; default scale restored to Week (prototype spec); the set-by-set detail sheet now shows the unfiltered records (what the user actually logged) so data-quality set numbers always match; a missing e1RM point degrades to the volume sub-line instead of "est 1RM 0 kg". Verified: quality gate PASS — 7 packages 250 tests green; `xcodebuild build` SUCCEEDED; zh/en screenshots of all three scales, empty state, data-quality section, and the default-Week regression archived.
- **M4-2 data-quality signal (FR-PR4 data layer)**: `RedeDataHealth` now answers "is this data trustworthy" with a minimal typed signal — `DataQualityReportBuilder.build(view:)` produces a `DataQualityReport` from the clean view. Two signal classes: (1) purification drop statistics aggregated by category from the existing `DataHealthIssue` trail (sessions / exercises / sets dropped, fields ignored); (2) silent suspect-set flagging for numbers that passed validity guards but defy plausibility (the PRD's "this 500 lb set is probably a typo"). Three deterministic rules, thresholds as MVP starting values locked by tests: weight strictly above 1.5× the user's own best top set for that exercise from strictly earlier sessions (baseline ≥30 kg to ignore small-weight noise; no flag on first exposure — calibration philosophy; flagged sets never enter the baseline so a typo can't poison the reference), absolute ceiling >400 kg (catches unit/typo errors with no history), reps >50. One reason per set, priority ceiling > relative > reps. Hard lines held by structure: flagging never drops or mutates user data (tested: flagged set remains verbatim in the clean view); the report is typed codes with zero copy, so "confidence" wording cannot exist (copy baseline §3.4 — trust is expressed through behavior, not readouts); missing-RIR-style gaps deliberately excluded (§3.4 folds them into Train, not Progress). Chronology uses plain string ordering — the clean view guarantees strict `yyyy-MM-dd`, so lexical order is chronological with no date math. TDD: 16 tests (12 red→green + 4 added by independent review: explicit reason-priority assertion, independent reps-rule firing, baseline-floor boundary at exactly 30 kg, and a lock on the accepted known boundary — flagged sets never evolve the baseline, so a persistent anomaly keeps getting flagged until handled via the M4-3 correction entry; honest by design, thresholds await real-user calibration). UI marking + correction entry land in M4-3. Verified: quality gate PASS — 7 packages 231 tests green (RedeDataHealth 48); `xcodebuild build` SUCCEEDED.
- **M4-1 progress projections (`RedeLocalSnapshot` reborn)**: the data layer for the Progress page (FR-PR1/2/3). The package returns as a Foundation-only library, enforcing the Master contract that it stays decoupled from `RedeDomain` and never reads or writes canonical AppData — it defines its own input value types (`SnapshotSessionRecord`: id, local-day date, exercises/sets, duration), and the app composition layer will map the DataHealth clean view into them when the UI lands (M4-3). `ProgressSnapshotBuilder.build(sessions:)` is a pure function producing three read-only projections: training history (newest-first; volume, set count, top set, PR exercise ids, duration), per-exercise e1RM trend (oldest-first point series + latest/best), and weekly volume (ISO week starting Monday; tonnage/sets/sessions). All semantics are locked to what's already shipped: e1RM = Epley `w×(1+r/30)` on the session top set (same as `SessionSummary`), PR = top-set weight strictly greater than every earlier session's top set for that exercise with no award on first exposure (M3's conservative call), volume = Σ weight×reps. Week bucketing uses pure integer date math (Hinnant civil-days) on the date string — no Calendar/timezone dependency, so fixed history yields deterministic output (the slice's acceptance), covered by leap-year/year-boundary tests. Invalid-date records are skipped defensively (clean view guarantees validity upstream). Legacy's median-based e1RM with confidence gating is deliberately out of scope (confidence belongs to M4-2's behavioral expression; copy baseline §3.4 bans confidence readouts). TDD: 17 tests red→green (15 red→green + 2 added by independent review: duplicate same-exercise entries within one session now merge before PR judgment — no award from intra-session self-comparison on first exposure, one e1RM point per exercise per session; same-day later session can PR over the earlier one). Registered in CI allowlist, quality gate, and workspace. Verified: quality gate PASS — 7 packages 215 tests green; `xcodebuild build` SUCCEEDED.
- **M3-4 resume in-progress session (FR-TR9) — M3 milestone complete**: an interrupted session can now survive a process kill. Design: the flow reducer records every *accepted* event (rejected ones never enter the log), and the draft is simply prescription + event log (`TrainSessionDraft`, Codable end-to-end); restore replays the events through the reducer, so "restored state ≡ interrupted state" is guaranteed by reducer determinism and asserted literally with Equatable in tests. The draft is not canonical truth (Master: draft restore is an in-memory-draft cache): a separate `active-session-draft.json`, written best-effort + atomically after every action (via new `SessionStore.apply(event)` wrappers — the view no longer mutates the flow directly), never through the write gate, same-day-only (stale drafts silently cleared), and deleted on successful persist or discard. On launch, a restorable draft raises a bilingual resume prompt (Resume / Discard; system Cancel = ask again next launch, loop-free dismissal). Simulator proof: `-autoPartialSession` logs one set → kill → clean relaunch → resume prompt screenshot archived; logged-set survival is proven by the replay-equivalence package tests. Independent review (2 MAJOR + 5 MINOR, all fixed in-slice): restore is now fail-safe — any event rejected on replay (e.g. catalog drift) returns nil and the stale draft is discarded rather than restoring a wrong state; the auto-complete verification hook now goes through apply() like production; startSession explicitly clears the pending prompt. Verified: quality gate PASS — 6 packages 198 tests green (RedeTrainingDecision 82, +7 draft tests); `xcodebuild build` SUCCEEDED.

## 2026-06-09

- **M3-3 completed-session write — the training loop closes**: a finished session now persists through the single write gate, and the kill-relaunch acceptance is proven on simulator (relaunch with no flags shows "Already trained today · Rest day" — the verdict read back from disk). `CompletedSessionBuilder` (RedeTrainingDecision) turns the flow end-state into a canonical `TrainingSession` using the legacy vocabulary: only user facts are recorded — actual sets (weight/reps/RIR/pain flag), skip records with reasons, replacement audit (`originalExerciseId`/`actualExerciseId`), end reason, `templateId` — engine outputs (prescribed targets) are never written back (system logic §5). The M1-2 gate seam finally gets its real implementation: `RedeDataHealth.CanonicalWriteValidation` — a write may not lose any clean-view session, and a newly appended raw session must survive purification (write-time interception beats read-time scrubbing); garbage rejection leaves the file byte-identical (tested). `SessionStore.completeAndPersistSession` wires builder → real gate → `CanonicalSessionWriter` off the main thread; the summary sheet's button became "Save & finish" with an honest failure state (FR-TR8: error text + retry, sets kept, never fake success). End-to-end package test (test-only dependency on RedePersistence) drives the full cycle: empty store → calibration prescription → train → persist → reload → verdict flips to rest, prescription nil, backup created on second write. FR-TR9 (resume in-progress session) decided: new slice M3-4 (draft persistence is not canonical truth — kept out of this slice). Verification scaffold: `-autoCompleteSession` launch arg (iteration-capped); simulator test data cleaned afterwards. Independent review (3 MAJOR + 5 MINOR, all fixed in-slice): double-tap on Save can no longer double-write (isSaving guard + disabled button), skip-then-replace records now align to the final exercise id (tested), the verification hook cannot spin forever, and the gate rejects newly introduced nil-id garbage entries (tested). Verified: quality gate PASS — 6 packages 191 tests green (RedeTrainingDecision 75, RedeDataHealth 32); `xcodebuild build` SUCCEEDED; kill-relaunch screenshot archived.
- **M3-2 focused training flow UI — the Train tab is alive**: full in-session interaction wired to the engines (per-set plan, NextSetEngine, skip/swap/pain intent models), with the session state machine extracted into the package as a pure, fully-tested reducer (`TrainFlowState`, 11 tests) so the app layer only renders and runs the rest timer. Flow: current-set card (exercise / target kg / reps / RIR + engine why-line) → Log set → in-place set⇄rest morph (fixed-frame cross-fade per prototype P2-1) with M:SS countdown, +30s / Pause / Next set → next set or next exercise → … → Finish (risk-red confirm sheet) or last set → summary sheet (volume / sets / top set / conservative PR via `SessionSummaryBuilder`, 4 tests — no PR on first exposure). Independent review (2 MAJOR + 4 MINOR/NIT, all fixed in-slice): the set table no longer mis-attributes data after a skipped set (per-row status mapping with an explicit skipped marker), the discomfort toast clears on skip paths, hold became a reducer event, and Plan-tab start now loads today first. FR coverage: TR1 one-screen current set; TR2 quick-adjust (tap the number → ± steppers, 2 taps); TR3 rest timer with add/pause/skip; TR4 recommendations honor execution facts with a persistent Hold toggle (plan weight, engine paused, resets across exercises); TR5 skip set/exercise with typed reasons; TR6 swap from the substitution group excluding today's list; TR7 one-tap discomfort logging → conservative next set + compliant advisory copy (§7.1 approved shape, forbidden-words tested); TR8 first half (summary confirmation; persistence is M3-3). `RedeL10n` gains `TrainEngineCopy` (36 pkg tests). New `SessionStore` (today outcome + active flow); session state is in-memory only — FR-TR9 (resume in-progress session) has no owning slice yet, flagged in the plan. Three stale "M3-2 write gate" references corrected to M3-3. Screenshot scaffolding: `-autoStartSession` launch arg. Verified: quality gate PASS — 6 packages 180 tests green; `xcodebuild build` SUCCEEDED; simulator zh/en screenshots of the live train screen (Upper day, DB bench press 30 kg, engine-derived plan), archived in evidence logs.
- **M3-1 per-set prescription + next-set engine + session intent models**: the training-flow engine layer (TDD red→green; RedeTrainingDecision now 55 tests, +22). `SessionSetPlanner.expand` turns the exercise-level prescription into a deterministic per-set sequence — MVP set shape is straight sets (same weight/reps/RIR per set) with per-exercise `restSeconds` (threaded through the slot rules from the legacy template vocabulary); §6.3's set-shape learning and warm-up generation stay explicitly deferred. `NextSetEngine.recommend` is the deterministic minimal subset of the §6.3 in-session contract: the user's last actually-performed weight is the baseline for the next set (complete 85 on set one → set two recommends 85; follow the plan exactly → the plan shape is kept), with a safety-first waterfall on top — pain reported → safety flag + ease 2.5 kg; last-set RIR ≤ 0.5 → ease; reps below the range floor → ease; floor 2.5 kg; no RIR data → no guessing; all planned sets done → nil. Typed `SetSkipReason` / `SessionEndReason` intent models (raw values are the audit codes; copy belongs to RedeL10n in M3-3) and `ExerciseReplacementEngine` (substitution-group candidates in catalog order, FR-TR6 groundwork). System logic gains §6.0.2. Verified: quality gate PASS — 6 packages 158 tests green; `xcodebuild build` SUCCEEDED.
- **M2-3 Today tab wired to real engines — M2 milestone complete**: the Today screen now renders live verdict + prescription data end-to-end (canonical JSON file → DataHealth clean view → branded input → verdict → prescription → SwiftUI), replacing the M0-2 static display data with zero visual changes (UI hard-acceptance rule). New thin composition layer `TodayModel` (app layer wires IO + injected clock only; all thresholds stay in packages); the app target now links RedeDomain/RedeDataHealth/RedePersistence/RedeTrainingDecision (pbxproj change justified: engine adoption). RedeL10n gains an engine-copy layer (`TodayEngineCopy`): typed verdict/prescription codes render to native bilingual signal-impact-decision sentences, 24 exercise names + training-day names, kg formatting — with a forbidden-words regression test (no \"AI\"/algorithm/系统认为/最佳 across every call×reason combination, copy baseline §4.1). Weights display in kg (decision recorded: no hardcoded lb until FR-SE1 unit switching lands; the prototype's lb was static-mock denomination). Prescription DTO gains `previousTopReps` + `nextProjectedWeightKg` for the Rail's last/next nodes. Real-device verification caught a genuine localization bug: the app bundle never declared zh-Hans (`CFBundleLocalizations`), so iOS language negotiation always resolved to English — Chinese-system users would have gotten an English app by default (masked in M0-3 by the in-app switcher); fixed in Info.plist. Independent review (2 MAJOR + 4 MINOR/NIT, all fixed in-slice): load is now a three-state outcome — unreadable user data renders an honest "data unavailable" state instead of being silently treated as a fresh install (single-write-gate spirit: never lead a user with corrupt data into a baseline-overwriting first session), and loading moved off the main thread (async .task) before M3 grows the file. Verified: quality gate PASS — 6 packages 136 tests green (RedeL10n 29 incl. 6 new copy tests); `xcodebuild build` SUCCEEDED; simulator screenshots of the calibration first-launch state in both locales (zh system-language path now correctly negotiates to Chinese) with all values traced to engine output (30 kg ×6 RIR 2 first-exposure, Rail 30×6 → 32.5×6).
- **M2-2 `RedeTrainingDecision` today-prescription engine + minimal exercise catalog**: the second decision engine of the clean rewrite (TDD red→green; package now 33 tests, +17 incl. 5 prescription goldens (all five training days locked)). `TodayPrescriptionEngine.plan(input:verdict:catalog:)` consumes the M2-1 verdict (rest → no prescription; never re-judges) and emits a fully-typed, copy-free, kg-denominated `TodayPrescription`: ordered exercises with sets / rep range / targetReps / targetWeightKg / targetRir / previousWeightKg / change(start-increase-hold-ease) / typed reason — the previous→target→change triple feeds the Receipt Change row, the train-screen why line, and the Rail in M2-3; lb conversion belongs to the rendering layer (FR-SE1). Day plans are not frozen templates (FR-ON3): a slot-rule generator (pattern + optional kind/equipment, first unused catalog match in declaration order) recomputes each day from `ExerciseCatalog.minimal` — 24 exercises with stable ids, movement patterns, primary/secondary muscles, equipment, substitution groups (6 multi-member groups for FR-TR6), and conservative kg start weights, factually reused from the legacy template inventory (tag `legacy-parity-final`, declared per the reuse rule; one start weight normalized 26→25 onto the 2.5 kg grid). PRD open question #1 resolved per its documented leaning and written back. Minimal progression is classic double progression locked by goldens, with RIR on a min-of-sets basis (any set at failure blocks an increase — safety over noise-resistance, explicitly decided in review): all sets at repMax with min RIR ≥ 1.0 (or no RIR data) → +2.5 kg and reps reset to repMin; min RIR ≤ 0.5 or below rep floor → −2.5 kg; else hold and push for repMax (no upper cap, intentional). Verdict modulation applies after progression: light ×0.9, deload ×0.8 with sets−1 (floor 2); weights round to the 2.5 kg grid, and a modulated weight that rounds back to its base is forced one increment down so light/deload always genuinely reduce. Dumbbell weights are per-dumbbell, barbell is total bar weight, cable/machine is stack reading (declared in the catalog header for the rendering layer). Catalog limitations honestly declared per system-logic §6.1 (no muscle-contribution weights / contraindication hints yet — no high-confidence muscle-level analysis may be derived from it); set-shape (top/backoff) belongs to M3-1, restSeconds to M3, bilingual exercise display names to RedeL10n in M2-3. System logic gains §6.0.1 documenting the full contract. Verified: quality gate PASS — 6 packages 130 tests green (RedeTrainingDecision 33, RedeDataHealth 26, RedeDomain 20, RedePersistence 19, RedeL10n 23, RedeWidgetShared 9); `xcodebuild build` SUCCEEDED.
- **M2-1 `RedeTrainingDecision` today-verdict engine + `CleanTrainingDecisionInput`**: rebuilt `ios/packages/RedeTrainingDecision` as the first decision engine of the clean rewrite (TDD red→green, 16 tests incl. 4 goldens). `CleanTrainingDecisionInput` is the engine's only entry type — its initializer is private and it can only be minted via `make(from: CleanAppDataView, todayISO:)`, so raw AppData cannot reach the engine at the type-system level; "today" is injected (no clock inside the engine, no IO, output never written back). Input surface follows PRD open question #2, now resolved per its documented leaning (recorded in the PRD table): logged training history (load/gaps/last performance) + plan structure only; subjective soreness/fatigue inputs stay FF. `TodayVerdictEngine.evaluate` is a pure safety-first waterfall producing the FR-T1 four-state call (train / light / rest / deload) with a typed primary `VerdictReason` and observable `VerdictSignal` facts — the engine emits no user-facing copy (reason codes become RedeL10n template keys in M2-3, which makes FR-T3's no-algorithm-names/no-"AI judgment" constraint structural). MVP thresholds (goldens-locked): trained today → rest; no history → train (calibration, no fake readiness); gap ≥ 14d → light (reentry); 21-day sustained load (≥ 3× planned days/week, max internal gap ≤ 2d) → deload — deliberately ranked above the ≥ 3-consecutive-days → rest rule because structural overreach outranks a short-window recovery signal; weekly plan count reached → light; last session mean RIR ≤ 0.5 with gap ≤ 1d → light; else train. Future-dated sessions are ignored; sessions without RIR data skip the near-failure rule (no guessing). Also extended `RedeDataHealth` with a `CleanProgram` projection (daysPerWeek guarded 1–14) and a strict session-date format guard with a new `invalidDateFormat` issue (→ 26 tests) so plan structure enters via the clean path. Day math is pure integer civil-calendar arithmetic (no DateFormatter — thread-safe, strict parsing, no TZ ambiguity). Independent review: 1 MAJOR (system-logic doc not updated — now added as §6.0) and 5 MINORs (DateFormatter thread-safety, silent invalid-date passthrough, weak boundary assertion, golden loop early-return, untested fallback hop) all fixed in-slice. Registered in CI `EXPECTED_PACKAGES`, the local quality gate, and the workspace. Verified: quality gate PASS — 6 packages 113 tests green (RedeTrainingDecision 16, RedeDataHealth 26, RedeDomain 20, RedePersistence 19, RedeL10n 23, RedeWidgetShared 9); `xcodebuild build` SUCCEEDED.
- **M1-3 `RedeDataHealth` clean view (minimal) — M1 data-foundation milestone complete**: rebuilt `ios/packages/RedeDataHealth` as the read-only purification layer between raw AppData and everything downstream (TDD red→green, 20 tests). `CleanAppDataViewBuilder.build(from:)` is a pure value transformation: raw AppData is carried verbatim and never rewritten; dirty/missing data is dropped (never silently patched) with every drop honestly recorded as a typed `DataHealthIssue` — these issues are also the future data-trust signal for the Progress page (MVP success criterion 5). Minimal guard rules: sessions need object shape + non-empty id + date + `completed == true`, duplicate ids keep first occurrence; exercises need a non-empty `exerciseId` (unattributable sets are dropped with them); sets need `weight ≥ 0` and `reps ≥ 1`, `rir` passes only within 0...15 (else projected nil with an issue); profile scalars pass within generous sanity ranges (age 10–120, heightCm 100–250, weightKg 20–400, weeklyTrainingDays 1–14), `trainingLevel` only as a known tier — out-of-range values project as nil with issues, and the layer never invents defaults (cold-start priors belong to the M2 engine). Engines from M2 on consume `CleanAppDataView` only, never raw AppData (system logic §1.2/§6). Registered in CI `EXPECTED_PACKAGES`, the local quality gate, and the workspace. Verified: quality gate PASS — 5 packages 91 tests green (RedeDataHealth 20, RedePersistence 19, RedeDomain 20, RedeL10n 23, RedeWidgetShared 9); `xcodebuild build` SUCCEEDED.
- **M1-2 `RedePersistence` JSON store + gated writer**: rebuilt `ios/packages/RedePersistence` as the single sanctioned canonical write path (TDD red→green, 19 tests). `JSONFileAppDataStore` owns the on-disk contract — `load()` is three-state (nil = no document yet / AppData / throws `.unreadable` for user data that exists on disk but cannot be decoded), `save()` is atomic via `Data.write(.atomic)`, `backupExisting()` copies the current version to a lexically-sortable timestamped backup before any overwrite. `CanonicalSessionWriter` owns the one gated orchestration (load → candidate → injected `AppDataWriteGate` → backup → atomic save → honest failure); M1-2 opens exactly one approved entry point, `appendCompletedSession` (requires `completed == true`, a session id, and rejects duplicate ids), with first-write bootstrap of a minimal schemaVersion-8 document. The DataHealth gate is an injected seam per Master Architecture §7 — the real implementation lands with M1-3; tests prove the writer honors gate rejection with the file left byte-identical. Acceptance evidence: unreadable existing user data is never overwritten (bytes asserted unchanged), backup-before-overwrite contains the previous version's exact bytes, gate rejection and save failures propagate honestly with no fake success, and unknown fields in the existing document survive the append verbatim (open-bag). Registered in CI `EXPECTED_PACKAGES`, the local quality gate, and the workspace. Verified: quality gate PASS — 4 packages 71 tests green (RedePersistence 19, RedeDomain 20, RedeL10n 23, RedeWidgetShared 9); `xcodebuild build` SUCCEEDED.
- **M1-1 `RedeDomain.AppData` MVP minimal model**: rebuilt `ios/packages/RedeDomain` from scratch as the first clean-rewrite data package (TDD red→green). Architecture: each entity holds its decoded JSON object verbatim (`storage: [String: JSONValue]`) and exposes the MVP subset (userProfile / programTemplate / history sessions / exercises / logged sets) as typed read-only views — open-bag preservation at every nesting level holds by construction, with no canonical-stringify/hash parity machinery (that retired with M1-0). `schemaVersion` stays 8 (legacy export vocabulary, open-door design; a bump requires explicit architecture approval) behind a strict guard (missing / non-integer / older → upgradeRequired / newer → futureIncompatible). JSON keys follow the legacy field vocabulary extracted from the `legacy-parity-final` tag. Mutation is deliberately absent — canonical writes belong to the M1-2 gated writer (single write gate). Tests: 20 green — schema guard incl. integral-float/fractional/negative version edges (9), open-bag round-trip incl. polymorphic string `rir` and non-object history elements preserved verbatim (4), typed views incl. mixed-type array read-as-nil-but-preserved semantics (6), and a real-export smoke test (the 805KB redacted schemaVersion=8 export decodes and round-trips losslessly; reference input, not a golden). Also fixed an M1-0 leftover: `Rede.xcworkspace` still carried references into the retired package set, causing Xcode/SwiftPM to recreate `.swiftpm` husks and trip the new CI allowlist assertion — which thereby proved its worth on its first real catch. Five stale references were dropped, the `RedeDomain` reference now resolves to the rebuilt package, and the previously missing `RedeWidgetShared` reference was added; the workspace now lists only the project + RedeDomain + RedeL10n + RedeWidgetShared. Registered RedeDomain in CI `EXPECTED_PACKAGES` and the local quality gate. Verified: quality gate PASS — RedeDomain 20, RedeL10n 23, RedeWidgetShared 9 tests green; `xcodebuild build` SUCCEEDED. Independent code review: 0 blockers/majors outstanding — all findings (schema-guard edge tests, test-helper simplification, doc wording, unused imports) fixed in-slice.
- **M1-0 legacy package retirement (clean-baseline cutover) + developer-log bootstrap**: after a five-lane read-only evidence audit (dependency graph, CI surface, truth-source docs, RedeDomain contents, logging baseline) and a scored decision matrix, retired all nine unused IronPath/PWA-era Swift packages — the `RedeDomain` chain (`RedeDomain`, `RedeDataHealth`, `RedePersistence`, `RedeHealthKit`, `RedeTrainingDecision`) plus the standalone placeholders (`RedeBackup`, `RedeUIKit`, `RedeLocalSnapshot`, `RedeNotifications`), ~330 tracked files — out of the working tree and compile surface; reference access stays via git history and the `legacy-parity-final` tag. `project.pbxproj` unlinked via scripted edit (-108 lines; the app target now links only `RedeL10n` + `RedeWidgetShared`, its real import surface — pbxproj change justified by the retirement itself). The CI package-test loop was replaced with an explicit `EXPECTED_PACKAGES` allowlist plus an unregistered-package assertion, fixing a silent-vanish hazard (a removed package previously dropped out of testing while CI stayed green); `.claude/quality-gate.cmd` added as the local mirror of CI. `ios/ParityFixtures` is kept: RedeL10n parity tests still consume it and it remains acceptance material for a possible future PWA-export migration (deferred product decision; the M1-1 model keeps the door open via open-bag + the legacy field vocabulary). Still-valid contract semantics (open-bag preservation, schema guards, edit semantics) are reborn as new tests in their rewrite slices (M1-1+), not silently dropped. Established `DEV_LOG.md` as the registered user-facing Chinese narrative log (manifest + README + AGENTS cross-links; backfilled for M0-1…M0-3). Verified: quality gate PASS — `swift test` green for RedeL10n (23 tests incl. parity fixtures) and RedeWidgetShared (9 tests), `xcodebuild build` SUCCEEDED.
- **M0-3 bilingual zh/en locale base**: added `RedeStrings` to the `RedeL10n` package — 90+ keys with native-written Chinese and English copy (shared intent, no sentence-by-sentence translation, per the copy baseline; anchors like 完成本组/Log set and 查看依据/Why this call asserted in tests) plus `RedeLocale.resolve` (zh* → zh, default en). The app layer gets an `@Observable` LocaleStore (follows system language; in-app switch via a temporary Settings sheet behind the Today gear; persistence lands with M5-2 into the AppData profile — no UserDefaults truth store). All four screens and the tab bar now render through bilingual keys; static display data stays until M2–M4. Legacy Terms/Formatters parity files coexist untouched. Verified: `swift test` green (RedeStringsTests + legacy parity), `xcodebuild` SUCCEEDED, simulator screenshots of all four tabs in both locales (8 screens) with layout unchanged from the prototype.
- **M0-2 design-language base + four-screen prototype reproduction**: per the user's directive that the front end must reproduce the existing HTML prototypes exactly, rebuilt the shell UI against `docs/rede-prototypes/rede-app.html` (the newest design truth, containing the D-A/D-B decisions). Added precise tokens/type scale (`RedeTheme.swift`) and the signature components (`RedeComponents.swift`: ForgedCard with grain + top-edge highlight + registration marks, ember-edge primary button, steel secondary button/toggle/segment control, overline labels, ring dots, custom 64pt tab bar on #100E0B), then statically reproduced all four screens (Today verdict hero + receipt + progress rail; Train instrument hero + set table; Progress verdict + monochrome charts + body map + development levels; Plan cycle dots + timeline + controls) with the prototype's display content as static data. Engine data lands in M2–M4 without visual changes; train-flow interactions and overlays belong to M3. Recorded the UI hard-acceptance rule (prototypes win over doc token values, e.g. t3=#9C9484, tab bar=#100E0B) in the MVP plan. Verified: `xcodebuild build` SUCCEEDED + simulator screenshots of all four tabs compared screen-by-screen against the prototype. Icons map Tabler→SF Symbols as platform equivalents.
- **M0-1 clean app shell (first clean-rewrite runtime slice)**: replaced the legacy app layer with a clean thin SwiftUI shell — removed 23 legacy `ios/Rede/*.swift` files (reference value preserved in git history), rewrote `RedeApp.swift`, and added `RootTabView` plus four tab placeholders (今日 / 训练 / 进展 / 计划; the Plan tab is an honest empty state with a "回到今日" action per the MVP plan §4.3). No business logic, no package imports, no storage or network in the app layer. `project.pbxproj` updated via scripted edit (-23/+5 file entries; package product dependencies kept for M1+). Verified: `xcodebuild build` SUCCEEDED + simulator install/launch screenshot showing the four-tab shell. MVP plan §9 progress table updated (M0-1 ✅).
- Added `docs/REDE_MVP_IMPLEMENTATION_PLAN.md` as the canonical MVP implementation-execution living doc: locked decision baseline (TestFlight free-first, core training-loop only, bilingual zh/en UI), IN/OUT scope, an M0–M6 slice queue with per-slice acceptance and validation commands, a TestFlight launch-readiness checklist, an acquisition/validation section that reconciles the roadmap's "validate-first" stance via App-as-validation, and a per-slice progress tracker (all slices start ⬜).
- Registered the new doc in `docs/DOCS_MANIFEST.md` (canonical living-doc set) and cross-linked it from `README.md` (documentation table), `AGENTS.md` (MVP execution entry), and `COMMERCIALIZATION_ROADMAP.md` (P1 now points to the slice-level execution doc). Reaffirmed it does not outrank the master architecture or system-logic docs.
- Registered the Rede prototype set (`docs/rede-prototypes/`: index + screens 01–09 + landing mocks + the integrated `rede-app.html`; files entered the repo with #509) under the manifest's reference-doc section. The prototypes are v0 HTML implementation references for the MVP core pages, push, widgets, share cards, and landing page; `docs/REDE_PRODUCT_DESIGN_LANGUAGE.md` remains the visual source of truth.
- Added `docs/REDE_PRD.md` as the canonical product-requirements doc: product definition and differentiation (explainable coaching vs loggers and black-box AI), target users and scenarios, eight product principles, four-tab IA, numbered functional requirements (FR-*) with user stories, plain-language acceptance criteria, priorities (MVP / FF / LATER with ⚠️GATE markers for architecture-gated work) and contract-source references into the system-logic doc, NFRs, success metrics, a release mapping (R0 TestFlight = MVP plan scope, R1 fast-follow, R2 public launch, R3 monetization, R4+ trust infra), and open questions. Registered in the manifest between the roadmap and the MVP plan; cross-linked from `README.md` (docs table + reading list), `AGENTS.md`, `COMMERCIALIZATION_ROADMAP.md`, and the MVP plan (successor wording updated: the PRD now exists; the development plan still follows at MVP completion).
- Gave the MVP implementation plan a bounded lifecycle: once the user explicitly confirms full MVP completion (all §9 slices ✅ + §1 TestFlight device acceptance), the doc is recorded into this changelog, absorbed into the canonical docs, de-registered from the manifest/README/AGENTS/roadmap, and deleted. Successor planning moves to a PRD plus a development plan based on the PRD and MVP completion, registered separately at that time. The lifecycle is mirrored in `docs/DOCS_MANIFEST.md`, `AGENTS.md`, and `COMMERCIALIZATION_ROADMAP.md`.
- Doc-only change: no engine, source, `project.pbxproj`, or package changes. Honest status — the clean runtime remains near-zero; existing `ios/` code stays legacy/reference inventory and is not MVP progress.

## 2026-06-07

- Retired the former Web/PWA implementation surface from the active repository: removed the Node/Vite runtime, TypeScript source/tests/scripts, browser assets, Supabase/Vercel implementation candidates, dev API, `RedeCloudSync` stub package, and package/lockfile tooling.
- Rebased the active project guidance to Swift/iOS: CI now runs Swift package tests and Xcode build, PWA validation routes are no longer current, and Swift test fixtures live under `ios/ParityFixtures`.
- Preserved the iOS-native account/cloud/sync/CRDT decision docs as canonical future architecture input; only the old implementation candidates were removed.
- Rewrote repository meta docs (`README.md`, `CONTRIBUTING.md`, `SECURITY.md`, `AGENTS.md`, `docs/agents/domain.md`) to stop instructing agents or contributors to use npm, Vite, browser tests, or old PWA routes.
- Added `docs/REDE_PRODUCT_COPY_BASELINE.md` as the canonical product-copy baseline for Rede, then tightened it from a string inventory into a brand-language baseline: core line, voice model, signature sentence patterns, commercial copy posture, v0 generation rules, and health/privacy/AI-risk guardrails.
- Registered the product-copy baseline in `docs/DOCS_MANIFEST.md` so future user-facing copy, locale, onboarding, paywall, App Store, and prototype-generation copy changes have a single living reference.
- Added `docs/REDE_PRODUCT_DESIGN_LANGUAGE.md` as the canonical product design-language baseline: replaced the AI-coach / command-center direction with a stronger decision-led `Forged Graphite + Emberline` brand system, four-tab IA guardrails, signature visual components, v0 generation prompt, and design QA checklist.
- Added the Share / Growth System direction to the iOS system logic and commercialization roadmap: local privacy-safe share cards first, muscle-level and PR growth assets, plan/routine sharing later, and account/cloud/feed/attribution behind explicit architecture gates.
- Expanded the Muscle Level Estimator into an engineering-ready contract: package boundary, clean input contract, output types, scoring pipeline, personal calibration, confidence, decision integration, share projection, implementation slices, and Swift test matrix.
- Folded the legacy beginner/intermediate/advanced training-level model into the Muscle Level Estimator target: `overallTier` replaces the parallel user-facing level, self-reported training level becomes only a cold-start prior, and recognized strength milestones such as bench 100kg / 225lb become level/tier breakthrough inputs.
- Re-audited the living docs with separate architecture, product, and doc-governance review lanes, then resolved P1/P2 conflicts in place: upstream exercise/template/equipment/set-shape/warm-up/support contracts now live in `docs/REDE_iOS_SYSTEM_LOGIC.md`; cloud/CRDT archives are rewritten as clean future-decision records; HealthKit, paid packaging, share metrics, and diet-market evidence are scoped to their approved boundaries.
- Removed the tracked `.codex-tools` pnpm bundle from the repository and ignored local tool/scratch directories so the repo no longer contradicts the Swift/iOS-only no-Node contract.
- Reframed the repository around a clean iOS rewrite baseline: living docs are now the target truth, existing `ios/` code is legacy/reference inventory, P0 paid-intent validation happens through an external website/mock/demo path, and `.claude` agent commands now guard against patching the polluted old runtime by default.

## 2026-06-06

- Added an isolated `/site` marketing surface for P0 commercialization validation: bilingual Rede + Larder positioning, product-path split, early-access/TestFlight intent form, and transparent founder-beta/price-intent smoke-test UI. Historical note: this `/site` surface was later removed with the Web/PWA cleanup on 2026-06-07.
- Reconciled the iOS system-logic living spec with the master architecture contract: `docs/REDE_iOS_SYSTEM_LOGIC.md` then separated implementation inventory, target product direction, and architecture-gated work.
- Clarified that Supabase, CRDT sync, watchOS, WatchConnectivity, full restore, and source-of-truth changes are target directions only until `docs/REDE_MASTER_TECHNICAL_ARCHITECTURE.md` is explicitly amended.
- Updated `AGENTS.md` product direction from the legacy five-tab shell to the commercial four-tab target (`今日 / 训练 / 进展 / 计划`) with Profile / Settings as a low-frequency entry.
- Extended the system-logic baseline with exercise catalog/equipment authority, gym equipment packs, load calibration, in-session set-shape learning, and warm-up execution boundaries.
- Tightened repo-facing guidance in `docs/DOCS_MANIFEST.md`, `README.md`, `docs/agents/domain.md`, and `docs/REDE_iOS_DECISION_CIRCUIT.html` so future agents do not treat the legacy five-tab shell or visual HTML as the target source of truth.
- Aligned `COMMERCIALIZATION_ROADMAP.md` with the local-first system contract: App Store subscription can ship before first-party account/cloud sync, while auth and cloud sync remain opt-in gated infrastructure.
- Rewrote `README.md` as a GitHub-facing native iOS project overview and removed the old Web/PWA operating guide.
- This changelog entry covers only the documentation files intentionally edited in this pass. Any existing `project.pbxproj`, source, or package/lockfile diffs in the working tree are outside this entry and require separate justification before inclusion.

## 2026-06-05

- Established the **living-doc system**: added `docs/DOCS_MANIFEST.md` as the single constraint list of canonical living docs, and a binding "Doc Discipline" section in `AGENTS.md` (no new top-level `.md`, code change → sync docs, archive-or-delete instead of new "v2" files, throwaway artifacts to `_scratch/`).
- Cleaned up 457 stale docs (legacy PWA design/audit docs + iOS-native-migration slices) via `git rm` (recoverable); kept only the manifest-registered canonical/reference docs. Migration directory `docs/ios-native-migration/` removed.
- Ignored local scratch trees `_slices/` and `_scratch/` in `.gitignore`.
- Fixed `ios/RedeWidget/Info.plist` to declare the required `CFBundleExecutable` and `CFBundlePackageType` keys.
- Docs-and-config only; no engine, source, `project.pbxproj`, or `package.json` changes.

## 2026-04-29

- Added `PLAN_PAGE_CLEANUP.md` to audit current Plan page sections, identify duplicate schedule/recommendation/draft/rollback surfaces, and define the target structure: current plan, weekly schedule, pending recommendations, and adjustment drafts.
- Added Plan Page Cleanup backlog tasks for view-model grouping, schedule-section consolidation, recommendation/draft de-duplication, and experimental-template rollback presentation.
- Confirmed this Plan audit pass is documentation-only and does not modify UI, training algorithms, engines, or data models.
- Added `COACH_ACTION_WORKFLOW.md` for Coach Action Workflow V1 planning, including the CoachAction contract, action types, status flow, confirmation/undo rules, active-session-only boundaries, Program Adjustment Preview handoff, follow-up tracking, implementation order, and testing strategy.
- Added Coach Action Workflow V1 backlog tasks for action contract/status flow, navigation-only actions, confirmation-gated active-session actions, Program Adjustment Preview handoff, local dismissal/undo, and outcome tracking.
- Confirmed this planning pass does not modify business logic, training algorithms, data models, or runtime UI.

## 2026-04-28

- Added `TRAINING_INTELLIGENCE_PLAN.md` for Training Intelligence V1 planning, including engine boundaries, display-only analysis, Program Adjustment Preview handoff rules, confirmation rules, protected core algorithms, implementation order, and testing strategy.
- Added Training Intelligence V1 backlog tasks for `sessionQualityEngine`, `recommendationConfidenceEngine`, `plateauDetectionEngine`, `volumeAdaptationEngine`, and `trainingIntelligenceSummaryEngine`.
- Added `COACH_AUTOMATION_PLAN.md` for Coach Automation V1 planning, including engine boundaries, confirmation rules, protected data, implementation order, and testing strategy.
- Added Coach Automation V1 backlog tasks for `dataHealthEngine`, `nextWorkoutScheduler`, `setAnomalyEngine`, `dailyTrainingAdjustmentEngine`, `smartReplacementEngine`, and `coachAutomationEngine`.

- Added frontend governance baseline: `FRONTEND_PLAN.md`, `UI_SPEC.md`, `COMPONENT_GUIDE.md`, `ROUTES.md`, `TASKS.md`.
- Added `API_CONTRACT.md` to document the current no-backend contract, localStorage persistence fields, backup import/export, health import boundaries, and program-adjustment mutation rules.
- Documented the current five-page IA: 今日, 训练, 记录, 计划, 我的.
- Documented that 计划 owns future plan/experimental template/rollback work, while 我的 owns settings/health import/backup and 记录 owns training-record management.
- No business implementation files or package files were intentionally changed.
