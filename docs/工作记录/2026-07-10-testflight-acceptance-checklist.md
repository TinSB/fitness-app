# Rede 1.9.2 (build 28) 发布验收清单（模拟器预验收已完成）

> **2026-07-21 版本重定向 1.9.2(28)**：28 = 27（已传 ASC）+ 训练日构成自由编排（#713）+ 动作库内容波 W1（#712）。1.8(25) 仍在苹果审核队列；本清单继续滚动——未勾项照旧有效，N 组补 N7/N8。上传/安装步骤按 1.9.2(28) 口径读。

---

## 第 0 步 · 上传与安装

- [x] 0.1 ✅ASC 直接核实：TestFlight build **1.8 (25)** 已处理完成，状态「准备提交」（2026-07-17）
- [x] 0.2 ✅ASC 直接核实：App Store 版本 **1.8** 已创建；商店公开版在提交前仍为 1.0
- [x] 0.3 ✅本机自动预检：app + widget 均为 **1.8/25**，模拟器可启动；权威 `.claude/quality-gate.cmd` exit 0，`BUILD SUCCEEDED`
- [ ] 0.4 ⏳本轮未取得新的 TestFlight 真机安装收据：手机更新到 **1.8 (25)** 后确认老数据仍在、无闪退/白屏

## R · 1.8 商店发布预检（2026-07-17）

- [x] R1 ✅en-US 与 zh-Hans 各 5 张（共 10 张）商店截图均来自真实 **1.8/25** 模拟器运行态，并逐张目检无个人信息
- [x] R2 ✅两套截图规格统一为 iPhone 6.9 英寸 **1320 × 2868 px**，顺序均为 Today / Development / Active Train / Exercise Library / Muscle Detail；旧 6.5 英寸槽改为继承对应语言的 6.9 英寸文件
- [x] R3 ✅What's New 已重定向到 **1.8 (25)**；隐私口径不变（Data Not Collected，HealthKit 只读且仅本机）
- [x] R4 ✅ASC 收据：build **25** 已绑定，双语元数据与 10 张新图已保存；2026-07-17 17:34 ET 提交成功，状态「等待审核」，提交 ID `73076a38-e6b8-43fa-a3df-e885f1a82f76`；审核通过后自动发布并立即面向所有用户，保留现有评分

---

## A · 等级校准（治「3 场 Lv.9 太快」）

- [x] A1 ✅模拟器 PASS（3 场种子：背/肱二 Lv.5、肩 Lv.4、胸 Lv.3、肱三 Lv.2——观感锚命中）进度页 →「发展」区块：你练过几场的肌群，等级观感应在 **Lv.4-6 附近**（不再是 3 场就 Lv.9）
- [x] A2 ✅模拟器 PASS（整体「校准中」如实 + 均衡度 69）整体级别与均衡度数字看着合理，没有夸张跳档

## B · 停练回归（你 7/8 抱怨的场景）

- [x] B1 ✅模拟器 PASS（正常态「按计划推进，照上次表现微调」）今日页顶部副句自然——**不再出现莫名其妙的「今天整体降一档」**
- [x] B2 ✅模拟器 PASS（6/15+6/16 种子停练 24 天：回推 A 头+「停练 24 天　循环从头开始，重量先回落」+轻练档——你 7/8 场景本尊治愈实拍）下次你停练三周以上回来：今日页应**从分化第一天重新开始**、文案是「先找回节奏」风格、建议重量比停练前明显轻

## C · 每周循环模式

- [x] C1 ✅模拟器 PASS（开关在、默认关、说明清楚）设置 → 训练周期 → 有「**每周重新开始循环**」开关，默认关，下方一句说明看得懂
- [x] C2 ✅模拟器 PASS（顺延模式弹窗「只换今天 · 推 A 顺延到下次」实拍；weekly 侧分支接线+L10n 双测试+#680 实拍兜底）今日页右上「今天换一天练」→ 弹窗正常、选项清楚；开关切换前后各弹一次，描述语境跟着变
- [x] C3 ✅模拟器 PASS（同一历史双模式对照：顺延=拉 A+透明副句 / weekly=推 A 从头——种子等价跨周验证）开着开关跨到下周一：今日页从分化第一天开始

## D · 肌群详情页（点进去看细分）

- [x] D1 ✅模拟器 PASS（真实点击：Lv.5 头部+背阔 Lv.4 每周 9 组/上背 Lv.3 每周 6 组/斜方+依据区）进度页 → 点「**背部**」行 → 弹出详情页：大等级 + 进度条 + **部位构成**（背阔 / 上背 / 斜方各自的 Lv 和每周组数）+ 依据区
- [x] D2 ✅模拟器 PASS（前束 Lv.2 每周 4.5 组/中束 Lv.1 0 组/后束 Lv.1 每周 3 组）点「**肩部**」→ 部位构成显示前束 / 中束 / 后束
- [x] D3 ✅模拟器 PASS（只有等级+依据，无空区块）点「胸部」（无细分的肌群）→ 只有等级和依据，**没有**空的「部位构成」区块
- [x] D4 ✅模拟器 PASS（斜方如实 Lv.1 + 每周 0 组）你没单独练过的部位（比如斜方 0 组）→ 如实显示 **Lv.1 + 每周 0 组**，不虚高

## E · 相对力量标准（按体重和性别）

- [x] E1 ✅模拟器 PASS（行在训练背景铭牌，未设显示 —）设置 → 训练背景 → 新增「**性别**」行（未设置显示 —）
- [x] E2 ✅模拟器 PASS（三选项+说明实拍；选男保存 → canonical 实证 sex="male" 落盘）点进去：三个选项（男 / 女 / 暂不设置）+ 说明「仅用于力量标准对比」；选「男」保存 → 铭牌行变「男」+ 出现一句修改收据
- [x] E3 ✅模拟器 PASS（选女+取消 → canonical 仍 "male" 不变）**出错场景**：再点进去换个选项但**不点保存直接取消** → 铭牌行保持原值不变
- [x] E4 ✅已验（批次 D 实拍：女 60kg 卧推 36 → 胸 Lv.10+「按体重的力量标准抬升了等级起点」依据行）填了性别且有体重（连过 Apple 健康或档案里有）后，进度页点你的强项肌群 → 依据区**可能**出现「按体重的力量标准抬升了等级起点」（是否出现取决于你的成绩过没过档——你的卧推水平大概率能看到）
- [x] E5 ✅模拟器 PASS（保存后 canonical sex 键移除——显式清除）改回「暂不设置」→ 下次进进度页等级回到原状（无残留）

## F · 自动均衡（弱肌群自动多安排）

> 前置：进度页「发展」里得先有带「**正在补足**」标签的肌群（数据少全在校准中就不会触发——正常，练几场再看）。

- [x] F1 ✅已验（批次 E 实拍：弯举 4/3 组=slot+1、预算 2 用满）有「正在补足」肌群时，正常训练日：该肌群为主的动作**组数比平时多 1 组**（比如弯举 2 组变 3 组）
- [x] F2 ✅已验（批次 E 实拍：抽屉底部「肱二头正在补足　今天多安排了组数」）点「查看依据」展开 → 最底部一行「**XX正在补足　今天多安排了组数**」
- [x] F3 ✅已验（批次 E 实拍全景零常驻小字）页面其他任何地方**没有**新增说明文字（无小字——你拍板的红线）
- [x] F4 ✅已验（批次 E 实拍：轻练日弯举回原组数零加量+引擎让位测试）轻练日 / 减载日：组数回到正常值（自动让位，不加）

## G · 遗留待验项（前几批一直没真机过）

- [ ] G1 ⏳真机项（模拟器授权弹窗交互受阻；开关/调度有 #639 批测试兜底——真机开「休息提醒」后到点看通知）**休息提醒**：训练中完成一组进入休息 → 锁屏/后台时到点收到通知（前提：设置里通知开着）
- [x] G2 ✅模拟器 PASS（AccessibilityM：今日页+进度页无截断无重叠，长行自然折行）**大字号**：系统设置 → 辅助功能 → 字体调大两档 → Rede 主要页面文字不截断、不重叠
- [ ] G3 ⏳真机项（模拟器桌面长按交互受阻；#550 写入侧已修——真机加 widget 看真实数据）**桌面小组件**：长按桌面添加 Rede widget → 显示你的真实训练数据（不是空占位）
- [x] G4 ✅模拟器 PASS（三 tab 卡渲染完整+「App Store 搜索 Rede」引流）**分享卡**：进度页「分享发展画像」→ 卡片生成正常，底部商店链接/二维码指向真实 App Store 页面

## F+ · 召回通知（1.4 新增，真机项）

- [ ] F+1 ⏳真机项：设置 → 通知 →「久未训练提醒」默认开；停练 5 天后应收到「该练XX了 / 距上次训练已有 5 天，重量沿用上次即可。」（banner 视觉与 G1 同场看——模拟器授权交互受阻，排程/文案已探针+测试双证）

## I · 1.5 新增目检项（热力图 + 进度页打磨，模拟器已验，真机看观感）

- [ ] I1 进度页「发展」区块：前/后**人形热力图**显示、练得多的肌群更亮、点任意色块直达该肌群详情；没数据的肌群只有细线轮廓（点不动是有意的）
- [ ] I2 进度页整体观感：周量图是细柱不是砖块、没有「橙色标出…」小字、里程碑同动作一行显示
- [ ] I3 周一早上打开进度页：肌群行**不再**集体出现下降箭头 ↘（趋势周一效应已修）

## J · 1.6 新增目检项（今日页图形 + 周口径，模拟器已验，真机看观感）

- [ ] J1 今日页抬头右侧：**七格周节奏条**（练过=橙实心、今天=描边）+「本周练 N 天」，和你实际这周练的天数对得上
- [ ] J2 练完一场后，总结卡吨位数字旁有一条近 5 场的小走势线
- [ ] J3 「查看依据」展开后没有那行「可采纳 · 可保持 · 可换动作」了
- [ ] J4 周一打开（或上周末刚练满时）：不再莫名被安排轻练——「本周」的账从周一重新算

## L · 1.8 沿用的重点目检项（去空虚化 K1-K8 + Live Activity 真机项）

- [ ] L1 训练 tab 没开训时：今天这场动作预览 + 底部「上次 · 日期」行 + 「开始训练」一键直启（不再折返今日页）
- [ ] L2 计划页「动作库 · 165 个动作」→ 分组清单 → 点开平板卧推：技术要点/调整难度/注意事项/循证依据齐全；训练中点当前动作名也能打开
- [ ] L3 休息日打开今日页：「上一场 · 日期」总结卡 + 「下一场」行；练完态多「本周练 N 天 · 合计 X」一行（数字和顶部小点条对得上）
- [ ] L4 **Live Activity（重点真机项）**：训练中完成一组进入休息 → 锁屏出现倒计时卡（动作名 + 下一组目标 + 橙色计时）；首次会弹「Allow Live Activities」→ Allow 后看纯净面
- [ ] L5 灵动岛：休息中回桌面看灵动岛胶囊倒计时；**长按**展开看动作 + 目标 + 大计时
- [ ] L6 休息中暂停/加时：Live Activity 跟着停（消失）/ 恢复重新出现且秒数正确
- [ ] L7 设置 → 数据 → 「导出训练数据」→ share sheet 出一个 rede-export-日期.json（存文件 App 打开看是完整数据）
- [ ] L8 周一早上打开今日页（真实周一）：状态行下一行「上周练 N 天 · 合计 X · 较前一周 ±Y%」

## M · 1.9 提交前手动验收项（2026-07-20 登记）

- [ ] M1 ⏳**评分请求 UserDefaults 门控检查（FR-SE11）**：debug build 连续完成训练，核对 `UserDefaults` 的 `reviewPrompt.lastRequestedVersion` 在**第 3 场完成落盘后**从空翻成当前版本号、且此后再完成训练保持不变（系统评分弹窗本身无法稳定截图，这个 key 即门控正确的证据；策略层 9 条单测已绿，缺的是端到端链路收据）

## N · 1.9 新功能真机项（2026-07-20 登记；模拟器已验，真机看手感）

- [ ] N1 **「本次训练」里的「现在练」（FR-TR14 S1；2026-07-30 S2 有意变更）**：任意正式组 `activeSet` 都能点稳定的「本次训练」统一开放行 → 同名 sheet → 选后面某动作「现在练」→ 当前动作变为所选、原当前顺延；记一组后仅每个「现在练」行级动作按原 S1 守卫降级，统一入口与移除/加动作/组数仍可用；当前最后一个动作也能打开编辑面。中途杀 App 重开顺序不乱
- [ ] N2 **计划编辑器撤销**：计划页进某训练日 → 移除一个动作 → 底部出「已移除「X」· 撤销」→ 点撤销回原位（连删可连撤）；**撤销条淡入淡出动效**（本批修过，真机看顺不顺）
- [ ] N3 **恢复默认暂存化**：没改过时「恢复默认」置灰；改动后点它列表变回教练方案、**仍留在面板**；按「采纳修改」才生效、按「取消」一切不变
- [ ] N4 **更新感知（FR-SE10）**：设置页「版本 / 检查更新 / 本次新增」三行在；点「本次新增」看 1.9.2 弹层（hero「训练日怎么排，你说了算」+ 三条具体亮点）；升级安装后 What's New 自动弹一次、再打开不重复弹
- [ ] N5 **今日页更新行位置**：有新版本时提示是**页底单行**（不在判断行上方压顶）；「稍后」7 天内不再出现
- [ ] N6 **编辑器操作区与触感（#710）**：两个编辑器（训练日动作/训练日先后）底部均为全宽「采纳修改」大按钮（无改动置灰）+ 右上 ✕；移除/撤销/恢复默认/采纳四动作有轻重分档震感；日序编辑器「恢复默认」点后留在面板、采纳才生效

- [ ] N7 **训练日自由编排（FR-PL7③）**：计划页「编排训练日」→ 把某天「换类型」（如第 4、5 天换成推 B、拉 B）→「采纳修改」→ 计划页 hero 变「自定义 · N 天循环」且训练日构成跟随；今日页/训练页处方按新构成走；杀进程重开仍在
- [ ] N8 **编排的加/删/撤销与上限**：能加一天、能删一天（只剩 1 天时删不掉）；删错出撤销条能逐步撤回；加到 14 天后「添加」与「撤销」都变灰（不会出现采纳成功但日序丢失）

- [ ] N9 **整动作跳过原因（FR-TR7）**：训练中点「更多」→「跳过这个动作」后，**同一 sheet** 原地切为四个既有原因（没器械/不适/疲劳/没时间）；选「不适/疼痛」后当前动作被跳过，完成并落盘后该动作的 `skippedExercises[].reason` 为 `painDiscomfort`。在原因面下滑关闭则取消，不跳过；「跳过这一组」的原四码入口保持不变。
- [ ] N10 **等级升级事实不会被吃掉**：练完触发肌群升级后，先去训练页再回今日页，今天这场仍有「背部 Lv.8 → Lv.9」；达到分享资格的行有分享图标与 chevron，低置信或旧事件只有纯文字、不可点，VoiceOver 不读成按钮
- [ ] N11 **升级 / 均衡改善卡**：点合格升级行进入预览，卡面只写等级变化与近 4 周训练天数；触发均衡改善后预览多一张「均衡改善」，只说均衡度变化与「正在补足」方向，无置信度、羞辱、鸡汤、弹窗或付费闸
- [ ] N12 **力量里程碑事后分享**：进展页里程碑整行可点；同动作同时有实测与估算时预览分别出现两页，估算页明确标「估算」，卡面单位与列表原样一致，不做 kg/lb 互转
- [ ] N13 **Widget 肌群等级**：非校准且已有等级时，small / medium widget 在今日判断下显示等级最高的两个肌群；校准中、新用户或 memory 缺失/坏文件时与旧版完全一致。等级允许滞后到最近一次进展页/练完态加载
- [ ] N14 **本次训练现场编辑（FR-TR14 S2）**：训练中打开「本次训练」→ 后续动作能「移除」，误删后本张 sheet 内可「撤销」；「移除」「撤销」完整文字区域至少 44×44pt 且空白处也能命中，在最窄支持机型、最大 Dynamic Type 与 VoiceOver 下仍能聚焦、读出动作与结果、顺序合理且不截断。「加一个动作」只列**开训时器械配置**可用且本场未排的动作，加入后紧跟当前动作，目标重量/次数/RIR 按**开训时单位**可正常执行；训练中改 Settings 不改变本场 picker/档位，下场才生效。当前动作剩余组数可 −/+，至少留 1 组且总组数不超过 8，并保留当前组已暂存快改。关闭 sheet 后撤销条消失；再做一次“加入动作 + 改组数 + 移除后续动作”并强杀 App，重开从「今日」选择继续训练后，队列、完整目标、组数、移除结果与会话配置都在，撤销条不恢复。练完后新增动作的真实组进入普通历史；remove event 不生成 skip，同 id 的另一 occurrence 可合法跳过；**训练中与训练小结 sheet 全程没有「存进计划」入口，只有完成并落盘后回到 Today，且构成确有差异时才进入 N16 的独立入口**。若移除的是 N15 的尚无事实 replacement terminal，立即精确撤销必须恢复原链；普通同 id 重加必须仍是新 ad-hoc，不继承链身份。**当前证据**：iPhone SE 375pt 与最大 Dynamic Type Simulator 已通过（无障碍字号动作区会纵向排列）；VoiceOver 实听、真手空白命中、触感与滚动仍留本项真机复验——Apple 不在 Simulator 提供 VoiceOver，本轮两台登记 iPhone 均离线
- [ ] N15 **中途换动作事实完整性（FR-TR6；五次停线已闭合，本地 GO，恢复进入 TestFlight 发布验收）**：
    1. 一场卧推原计划 3 组，先完成 2 组 →「更多」→ 换成哑铃卧推；换后仍是同一「动作 N/M」，动作卡显示哑铃卧推**第 1/1 组**，总进度不多不少。强杀 App 并重开继续，仍从哑铃卧推第 1 组恢复；完成保存后，历史详情同时看到卧推 2 组、哑铃卧推 1 组；下一场同 movement pattern 继续给哑铃卧推。
    2. 已在 A 完成 1 组后连续 A→B→C→D，B/C 都不记组：B/C/D 均不得中途重开热身；完成 D 后历史不能出现 B/C 空元素，A 与 D 的 link 两端唯一一致，下一场 sticky 给 D。完成 1 组都没有的独立全零事实替换仍只落最终动作，不出现空原动作或 split 字段。
    3. 最终替代动作只做一次「跳过这一组」后结束：该 actual occurrence 允许 `sets=[]`，但必须有真实顶层 skip 归属与 actual link；Today「上次」、练完动作数和进展柱不得把它当成完成实绩或遮住更早成绩。
    4. 把尚无事实的 replacement terminal 移除且不撤销：保存后 root 事实仍在、无悬空 link，下一场 sticky 按普通第一代表取 root。立即用原 removal 精确 Undo 则完整恢复链；移除后同 id「加一个动作」必须是无 role/link 的新 ad-hoc，即使该新生命周期后来又换走/离开 plan，旧 Undo 也不能复活已取消链。
    5. 疼痛边界走 A（带 `painFlag`）→B→A（无 pain）：同场 A 聚合后不得提前解除保守态；`sets=[]` occurrence 也不能算恢复。另确认同 pattern 临时加练不偷 sticky 槽位、A→B→A 下一场粘最终 A。
    6. 当前 A 先「跳过这一组」→ 换 B → B 零事实回换 A：新 A 的组表第 1 行必须是当前 active，不能继承旧 A occurrence 的 skipped 行；旧 A 的 skip 仍只在历史里归旧 occurrence。

    **当前本地证据**：专用 iPhone 17 Pro / iOS 26.5 Simulator 已走完 2/3 换、强杀恢复、完成保存、历史详情与 canonical 实读；occurrence 集成 12/12、`RedeTrainingDecision` 490/490，权威门禁 exit 0（10 个 Swift 包、通用 Simulator build、App 宿主 58/58，`QUALITY GATE: PASS`），独立复审无剩余 P0–P2。**本项仍未在 TestFlight 真机执行，保持 `[ ]`，须按以上步骤真实打勾。**

- [ ] N16 **练完存进计划（FR-TR14 收官）**：
    1. 在「本次训练」里加入哑铃弯举、移除面拉并完成训练；回 Today 练完态，先看到事实句「今天加了哑铃弯举，去掉了面拉」和「存进计划」，训练中与小结 sheet 均没有该入口。纯重排另测一次，应显示「今天调整了动作顺序」；什么都没改或最终构成与当前计划相同则整行不出现。
    2. 点「存进计划」后显示「已存进计划」并提供「撤销」约 5 秒；进入计划页对应训练日，动作与顺序和本场最终队列一致。目标重量、次数、RIR 和休息仍由处方给出，不应被今天的组数/重量写死。
    3. 在撤销条存在时点「撤销」，计划页恢复**实际写入瞬间**的完整 raw dayPlan 节点；不能只恢复 typed 已知字段。若写前没有该 dayCode 自定义键，撤销后该键应消失；若写前已有空 `daySequence:[]` 或空 `dayPlans:{}` sibling，撤销后必须逐字节保留。超过 5 秒后不再显示即时撤销，但仍可从计划编辑器手动修改或恢复默认。
    4. 用「今天换一天练」完成一场，确认保存目标取完成场真实 `templateId`，不会写回原本被换掉的日型。把最终构成改回该日默认模板再存一次：没有 sticky/substitution 把默认拉走时应清除默认等价自定义覆盖；存在这类 overlay 时应保存必要的 userPinned 默认动作，而不是假 clear/no-op。
    5. 加入并真实完成一个该训练日默认模板没有的 pattern 后存回；下一次该日仍能正常处方该动作，无历史时按既有 8/12/90 秒/RIR 2.0 兜底，有历史时正常读取表现。写入失败时必须显示「暂时无法存进计划　请再试一次」，原计划不变、不得假成功。
    6. 让入口出现后先从计划编辑器修改同一天，再回 Today 点击：若当前有效构成已等于目标或已经由别处写成同值，入口应刷新消失且没有成功收据；若仍不同，存回后点撤销应恢复刚才那次外部编辑后的最新构成，而不是 Today 最初加载时的旧构成。
    7. 永久 `A→B` 生效且本场零编辑时，Today 不应显示入口；last-actual sticky 已让下场同构时同样不显示。随后在永久 `A→B` 下把本场 B 改回默认 A：存回后 substitution 表仍保留，但该日写入 userPinned A，下一场真实处方必须仍是 A。

    **当前本地证据**：Persistence raw/atomic 完整包 110/110、`RedeTrainingDecision` 494/494、`RedeL10n` 144/144、App `SessionStoreDraftTests` 60/60；最终权威门禁覆盖全部 10 个 Swift 包、通用 Simulator build 与 App 宿主 81/81，末行 `QUALITY GATE: PASS`。测试覆盖未知 dayPlan/item 键、脏 item、空 `daySequence`/`dayPlans` 在写入与清除两种 raw 撤销路径的 bytes 恢复，入口出现后外部编辑的三条原子分支，sticky/substitution 的真实构成与条件收敛，以及真实换动作、先加后删再 S1 移动、文案素材与最终 target 对账、Today/Plan 错误面隔离、候选按需派生和快速双击 busy→首笔成功后的 5 秒撤销生命周期收敛。专用 iPhone 17 Pro / iOS 26.5 Simulator 已重捕入口、撤销条、计划编辑器与 no-op 四态；另用 `Rede-SaveToPlan-SE-QA`（iPhone SE 3rd generation / iOS 26.5，375×667pt）在最大 `accessibility-extra-extra-extra-large` 下实拍最长加+删分支。首次 RED 中事实句出现省略、动作词被挤成侧栏；按 #720 同款让 accessibility size 的动作词移到事实句下方后，最终 `.ai-tmp/save-to-plan/2026-07-30-savetoplan-se-ax5.png` 为 750×1334px、完整显示「今天加了髋内收机，去掉了杠铃耸肩」与下一行「存进计划」，MD5 `a4e4496723fc9d58207d4efe99d5b694`。本项仍未在 TestFlight 真机执行，保持 `[ ]`。

## H · 稳定性出错场景

- [x] H1 ✅模拟器 PASS（完成 1 组休息 2:58 强杀 → 重开弹「已完成的组都还在」→ 继续：第 1 组 ✓ 在、**休息计时连续恢复 1:28**）训练进行到一半，上滑强杀 App → 重新打开：训练进度还在，数据没丢
- [x] H2 ✅PASS-BY-DESIGN（App 纯本地零网络调用面；模拟器无飞行模式开关）飞行模式下用 App 记一组 → 一切照常（本地应用，无网不影响）

---

## 全过之后

全部打勾 → 关闭本清单；商店 1.8 已提交且等待审核，未勾选项继续作为发布后复验项（审核通过并自动发布前，公开版仍为 1.0）。
有任何一条不对 → 编号 + 截图发回来，进校准批次。
