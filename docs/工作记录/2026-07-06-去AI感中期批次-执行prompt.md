# 2026-07-06 去 AI 感中期批次 · 执行 Prompt（交接件）

> 来源：2026-07-05 去 AI 感诊断的中期候选（owner 2026-07-06 拍板 A 方向：主按钮 + 空态；App 图标继续压着不动）。
> 用法：把本文件整体作为任务输入交给执行会话。性质：一次性交接件，批次收口后可归档。
> 上一批（#647-#653）已收：first 徽标去重 / 分享卡换行 / 图表幽灵槽 / 教学提示场数上限 / 计划页折叠 / 练完态总结卡。

---

## 总控指令

沿用上一批全部纪律（独立分支/PR、失败测试先行-可测处、全量门禁、独立 code-reviewer、模拟器中英实拍、文案八问 + golden、DEV_LOG/规格写回批次收口统一落盘、auto-merge 限门禁绿+审查无 MAJOR+纯 UI）。本批特有：

- **M1 不 auto-merge**：主按钮是品牌关键视觉，PR 附全部消费场景的改前/改后对比截图，等 owner 过目拍板（同 Dynamic Type #644 惯例）。
- **Ember 公理硬边界**：主按钮为锻面 + ember 左缘，**禁止 ember 填充**（设计语言拍板；RedeTheme.swift 头注）。提权手段限：全宽、提亮锻面底、加高、字级、ember 缘宽、描边轮廓、按压反馈。
- 全部任务不碰数据、不碰引擎、不碰 L10n 语义串（M2 的文案腐烂修正除外）。

## Tasks

### M1: 主按钮（EmbButton）视觉权重提级
- **现象**：EmbButton 内容 hug 半宽 + redeBtn(0x211E18) 底与 redeSurface/redeBase 明度几乎无差 + 44pt——页面主 CTA 读作次级按钮，第一眼找不到「该按哪」（诊断 ⑤）。
- **scope**：组件级提权（方向：`maxWidth .infinity` 全宽、底提亮一档（候选 redeRaised 或新 token，保持锻面质感）、minHeight 44→50、fontSize 15→16、ember 左缘 2→3px 的组合；最终组合以实拍对比定）。**12 个消费处逐屏实拍回归**（今日页开训 / 训练页 Log set·热身完成·休息条 next·结束确认·保存 / 三处空态承接 / 引导 Open Today / 设置页反馈·编辑保存）；语境不适合全宽的（如设置页行内、休息条并排）允许加 hug 参数分档——但先试全局统一，以实拍定，不预设参数。
- **files**：ios/Rede/RedeComponents.swift（EmbButton）；若新 token 则 RedeTheme.swift。
- **acceptance**：主 CTA 一眼可辨；12 场景无破版；Ember 公理未破（无填充）；对比截图全套（中文为主、英文抽验）。
- **merge**：**只开 PR 附截图等 owner**。depends: 无。
- **规格写回**：设计语言按钮节更新取值（收口统一）。

### M2: 空态统一设计（§12.5 空态承接）
- **现象**：进度页空态 = 两行字 + 90% 纯黑（诊断 ①）；计划页无模板兜底态文案已腐烂——「周期结构、调整建议和回滚**将在后续版本加入**」全部已上线（FR-PL2/3/4），1.0 用户看到的是假话。
- **scope**：① 进度页空态：结构化预告（示意性灰柱/骨架 + 一句说明）+ EmbButton 承接「去今日页开训」（复用 M1 改后按钮；trainEmptyAction 串复用）；0 ForgedCard、不编数据（示意柱明确是装饰非数据，a11y 隐藏）。② 计划页 planEmptyNote 文案修正为如实现状（措辞过八问 + golden 更新）。③ 训练页空态（No session in progress）顺带核对是否同病，同标准处理。
- **files**：ios/Rede/ProgressTabView.swift（emptyState）；ios/packages/RedeL10n（planEmptyNote 修正 + 新说明串 + golden）；可能 ios/Rede/PlanTabView.swift。
- **acceptance**：进度页空态不再大片纯黑、有下一步承接；planEmptyNote 无腐烂承诺；中英实拍；防回潮测试全绿。
- **merge**：门禁+审查绿可 auto-merge（纯 UI/文案）。depends: M1（复用改后按钮）。
- **规格写回**：Copy Baseline（planEmptyNote 修正）；设计语言 §12.5 空态节补进度页案例（收口统一）。

## 收口清单
1. 两 PR 处置完毕（M1 等 owner 拍板不计入自动收口）。
2. DEV_LOG 批次条目 + 规格写回（设计语言按钮节/空态节、Copy Baseline）。
3. 实拍证据入 docs/工作记录/（2026-07-06-m<N>-*.png）；模拟器测试数据卸载。
