# IronPath Master Technical Architecture

> **This is the canonical, highest-level engineering contract for IronPath.**
> Every human and every AI agent (Claude, Codex, or otherwise) **must read this document before making changes** and **must obey it**. If a requested task conflicts with this document, **stop and require explicit architecture approval before writing any code.**

- **Status:** Authoritative / binding
- **Version:** 1.1
- **Last updated:** 2026-06-01
- **Baseline commit:** `0ecc39c` — *EDIT-4 计划配置编辑 (program template scalar config) V1 (#444)*
- **v1.1 amendment (iOS-17A):** activates the **first native canonical-AppData write path** (performed-set logging). Amends §2, §8, §9, §12, §27.
- **Subsequent amendments (baseline now current through #444):** the first capability ungatings + read/edit paths, all catalogued in §27 — HK-1 body-weight import (#428), N-1 rest-timer + N-2 weekly-training local notifications (#430/#432), W-1 readiness widget (#431), HK-2/HK-2b workout-history import (#433/#434), HK-3 workout export + HK-3b `HKWorkoutBuilder` migration (#435/#436), the Today/Profile/History/Plan canonical **read-for-display** paths (#437–#440), and the EDIT-1…EDIT-4 canonical **edit-write** paths (#441–#444). The binding rules in §1–§26 are unchanged by these.
- **Repository:** `TinSB/fitness-app` (working dir `ironpath`)
- **Supersedes (for day-to-day task scoping):** the scattered planning/strategy docs under `docs/` and the root `*.md` plans. Those remain historical context; **this document wins on any conflict about boundaries, ownership, or workflow.**

---

## 1. Document Authority and Purpose

### 1.1 Authority
This document is the **single source of architectural truth** for IronPath. It outranks:
- Any individual doc in `docs/` (~345 files; most are strategy/candidate explorations, not build-now mandates).
- Any root-level `*.md` plan (`FULL_STACK_REFACTOR_PLAN.md`, `TRAINING_INTELLIGENCE_PLAN.md`, `ARCHITECTURE.md`, etc.).
- Any prior instruction that contradicts the boundaries below.

It does **not** outrank explicit, in-the-moment human approval that *knowingly* amends this document. Such an amendment must be written back into this file **in the same change**.

### 1.2 Purpose
1. Stop scope creep — especially AI/Codex scope creep — by drawing **hard, testable boundaries**.
2. Protect the **source of truth** and user data safety above all else.
3. Make the React/Vite/TypeScript PWA → native iOS SwiftUI migration **safe, incremental, and verifiable**.
4. Give every future task a **decomposition and prompt template** that bakes the rules in.
5. Define the **validation and static-guard standard** that gates every merge.

### 1.3 How this document was scoped (internal prioritization)
Candidate topics were scored 1–5 against a weighted model (scope-creep prevention 20%, source-of-truth/data-safety 20%, iOS migration 15%, task decomposition 15%, validation clarity 10%, ownership clarity 10%, long-term value 5%, staleness risk 5%). Topics that create **hard boundaries, clear ownership, and validation standards** scored 4–5 and are stated as rules here. Topics that would **prematurely encourage CloudKit/HealthKit/auth/network sync/full AppData restore/CoreData/SwiftData/production-backend assumptions** were penalized and confined to §17 (Deferred Systems) as *gated, not designed*. Detailed per-file maps were treated as appendix-level and kept concise.

### 1.4 The one rule that overrides convenience
> **When in doubt, do less.** Prefer the smallest vertical slice that is fully validated and guarded over a larger change that is partially correct. A blocked task with a clear escalation is a success; a silently-expanded task is a failure.

---

## 2. Current Project State

| Aspect | State as of baseline `6df0149` (2026-05-30) |
| --- | --- |
| PWA | Mature product surface. React 19 + Vite + TypeScript. The live runtime users rely on. |
| Native iOS | Migrating. Thin SwiftUI shell + a WidgetKit extension over **12 local Swift packages**. Local-first only. |
| iOS migration progress | Completed **through iOS-16**, plus **iOS-17b** (in-RAM per-set capture, no persistence) and the **iOS-17S five-tab navigation shell** (今日/训练/记录/计划/我的; Focus relocated under 训练 unchanged, the other four tabs placeholder). Native local training history, on-device JSON snapshot store, saved-session detail, history search/filter + coarse **and custom from/to** date ranges, summary stats, **per-exercise recovery insight**, and **non-destructive draft recovery** are shipped. Set-logging (iOS-17) is an **approved epic, in progress** (capture slice 17b shipped, in-RAM only; canonical write path 17c not yet built). |
| Native data model | `IronPathDomain.AppData` — pure `Codable` value type, parity-pinned to the PWA export. |
| Native persistence | **Local on-device JSON files via Foundation `FileManager` only** (atomic write + backup-before-overwrite). Two sanctioned stores (§12), **both now active**: the Focus-history snapshot store and — as of iOS-17A — the canonical-AppData store (`JSONFileAppDataStore`, written via `CanonicalSessionWriter`, §8). **No** iCloud/CloudKit/HealthKit/Supabase/network/UserDefaults/SQLite/CoreData/SwiftData. |
| Restore (iOS-14 UI) | **In-memory local draft re-hydration only** (`LocalDraftRestorePlanner.reconcile`). Not a full AppData restore. |
| Full AppData restore | **Deferred**, gated behind DataHealth `buildCleanAppDataView` + the repair-apply pipeline. |
| `iOS-4B6` (userFacing / full arbitrationTrace) | **Deferred.** |
| Per-set logging (iOS-17 epic) | **Approved epic, in progress.** Capture (17b) in-RAM. **iOS-17A (this amendment) shipped 17c + 17d:** the first native canonical-AppData **write path** (performed sets → `AppData.history[].exercises[].sets`, via `CanonicalSessionWriter`, DataHealth-gated, backup-before-overwrite, no-fake-success — §8), plus the per-exercise "上次成绩" detail summary backed by a derived LocalSnapshot **v3** `setLogs` display copy (§12). **17e** engine consumption of performed sets remains deferred. |
| Cloud / auth / sync (native) | **Not built.** Stub packages / gated only. (PWA has gated cloud-production scaffolding — §4.) |
| HealthKit (native) | **Read body weight (HK-1) + workout history (HK-2) + workout EXPORT (HK-3) ACTIVE.** `IronPathHealthKit` is an approved, bounded, **read-only** Apple-Health adapter: the latest body mass → `AppData.healthMetricSamples` (kg); recent workout summaries → `AppData.importedWorkoutSamples` (DERIVED, display-only, `source: "healthkit_import"`) — both through the DataHealth-gated §8 write path. Imported workouts are NEVER canonical training and NEVER feed the engine. **HK-3** adds the first & only write-back: a user-triggered, idempotent, native-only EXPORT of native completed sessions (`AppData.history`) to Apple Health as `HKWorkout`s (read auth shares nothing; export shares ONLY the workout type; device-local; never re-exports imports). Other-type write-back stays deferred. See §6.2/§8.2/§16/§17/§18. |
| Local notifications (native) | **LOCAL reminders ACTIVE (N-1 rest-timer + N-2 training).** `IronPathNotifications` schedules on-device reminders — N-1 a one-shot `UNTimeIntervalNotificationTrigger` rest reminder after a completed set; N-2 a REPEATING weekly `UNCalendarNotificationTrigger` training reminder (user picks weekdays + time, iOS persists it) — confined to one `#if os(iOS)` adapter. LOCAL only — no network. **Remote / push notifications remain forbidden** (server → gated, §17). See §6.1/§16/§17/§18. |
| Home-screen widget (native) | **READINESS WIDGET ACTIVE (W-1).** A WidgetKit widget (a separate `IronPathWidgetExtension` target) renders today's readiness from a DERIVED read-only snapshot the app writes to a shared App Group. Read-only — never writes canonical AppData, never a source of truth (§12). On-device, no network. See §5/§6.1/§12/§17/§18. |

---

## 3. Product Architecture Overview

IronPath is a mobile-first personal training app with two front-ends over one shared training-intelligence contract:

```
                         ┌─────────────────────────────────────────────┐
                         │      Shared training-intelligence contract     │
                         │  (AppData shape + decision-engine semantics)   │
                         │   enforced cross-platform by PARITY GOLDENS    │
                         └───────────────┬───────────────┬───────────────┘
                                         │               │
              ┌──────────────────────────┘               └───────────────────────────┐
              ▼                                                                         ▼
   ┌────────────────────────┐                                          ┌────────────────────────────┐
   │  PWA (mature product)  │                                          │  Native iOS (migrating)     │
   │  React 19 + Vite + TS  │                                          │  SwiftUI shell + 12 packages│
   │  src/ + packages/*     │                                          │  ios/IronPath + ios/packages│
   │  Local-first; cloud    │                                          │  Local-first ONLY           │
   │  production GATED       │                                          │  No cloud/network/auth      │
   └────────────────────────┘                                          └────────────────────────────┘
```

- The **PWA is the product**. It is where features are mature and where users live today.
- **Native iOS is a migration target**, advanced one validated vertical slice at a time.
- The two are kept in lock-step by **parity goldens** (the TypeScript engine emits goldens; the Swift packages decode/assert against them, and Swift comments cite the exact TS source line they mirror — e.g. `buildCleanAppDataView` mirrors `src/dataHealth/cleanAppDataView.ts`).

---

## 4. Current PWA Architecture

- **`src/`** — the React 19 + Vite PWA. Notable module groups: `engines/` (the in-browser decision engine, incl. `trainingDecisionCleanInput.ts`), `dataHealth/` (`cleanAppDataView.ts`), `features/`, `presenters/`, `ui/` + `uiOs/`, `storage/`, `data/`, `models/`, `i18n/`, `observability/`, `diagnostics/`, `workers/`, and the **gated cloud-production scaffolding** `cloudProduction/`, `cloudSync/`, `sync/`, `auth/`, `productionApi/`, `productionCutover/`, `devApi/`.
- **`packages/core/`** — shared **pure** domain/engine logic; the **canonical TypeScript engine** from which parity goldens are generated.
- **`packages/contracts/`** — shared types and JSON-schema **contracts** (validated with `ajv`).
- **`apps/api/`** (`apps/api/src/node/devApiRunner.ts`) — a dev/SSR API runner (`npm run api:dev:build`). It is a **backend candidate for local development only — not a deployed production backend.**

**Runtime source of truth (PWA):** AppData is **client-side / local-first**. `@supabase/supabase-js` is a dependency and `src/cloudProduction/*` contains real Supabase auth/RLS/env-guard/cutover/kill-switch scaffolding (~22 files reference Supabase), but **cloud/account/auth is GATED** behind environment + cutover guards (§17). It is **not** the default runtime source of truth, and **must not** be promoted to default without an approved architecture task.

**Import direction (one-way):** `src/` → `packages/core` + `packages/contracts`. The packages **must not** depend on `src/`. `apps/api` may also consume the shared packages.

---

## 5. Native iOS Architecture

```
ios/
├── IronPath/                       ← THIN SwiftUI app layer (.swift + assets/plist)
│   ├── IronPathApp.swift           ← @main; Version probe links the original bootstrap packages (later packages link via real use)
│   ├── ContentView.swift           ← trivial root: five-tab TabView shell (今日/训练/记录/计划/我的, AppTab enum — iOS-17S)
│   ├── {Today,Training,History,Plan,Profile}RootView.swift  ← per-tab mount points (iOS-17S); 训练 hosts FocusModeShellView unchanged, the other four are placeholder empty states
│   ├── FocusModeShellView.swift     ← THE 专注训练 (Focus Mode) shell: .plan/.inSession/.completed;
│   │                                  .task does the real-clock opt-in + loadSavedSessions; restoredDraftBanner
│   ├── FocusModeMvpState.swift       ← @MainActor view-model: in-RAM UI state; deterministic default clock;
│   │                                  delegates ALL disk IO to LocalSessionSnapshotStore; restoreDraft (in-RAM)
│   ├── FocusModePreviewData.swift    ← deterministic AppData → buildCleanAppDataView →
│   │                                  createCleanTrainingDecisionInput → buildTrainingDecisionFromCleanInput
│   ├── FocusSavedSession{History,Detail,Preview}View.swift  ← history surface / detail sheet / completion preview
│   ├── FocusSession{Progress,Completion}View.swift, FocusSetChecklistView.swift, FocusModeExerciseCard.swift,
│   │   FocusModeStatusSurfaceView.swift, TrainingDecisionSummaryView.swift  ← pure presentation
│   └── Assets.xcassets, Info.plist
├── IronPathWidget/                 ← W-1 WidgetKit extension target (Bundle + ReadinessWidget + Info.plist + entitlements)
├── IronPath.xcodeproj/             ← app target + IronPathWidgetExtension widget target (W-1); 12 XCLocalSwiftPackageReference -> packages/*
├── IronPath.xcworkspace/           ← workspace wrapping project + package dirs
└── packages/                       ← ALL business logic lives here (12 packages, §6)
```

**Core principle:** the app layer is a **thin renderer**. The only app-layer logic lives in `FocusModeMvpState` (a `@MainActor ObservableObject` view-model) which holds in-RAM UI state and **delegates** persistence and reconciliation to packages. Views are pure presentation + label/date formatting.

**Canonical read-for-display (Today real-AppData read path V1):** the 今日 surface (`TodayRootView`) also READS the canonical store the write path uses (`JSONFileAppDataStore.applicationSupport()`) **read-only**, routes the document through DataHealth `buildCleanAppDataView` (§10), and renders the user's **real** readiness via the existing engine pipeline — the first native canonical **read-for-display** path. It adds **no write path** and does not move the source-of-truth boundary (§8). The thin `@MainActor TodayRealDataModel` (inlined in `TodayRootView.swift`) owns only the store wiring + the IO seam; all branch logic (missing / unreadable / empty / ready) lives in `IronPathTrainingDecision.resolveTodayReadinessState` (pure, unit-tested). The engine package never builds the clean view itself (guarded) — the app-layer loader does, and hands it the `CleanAppDataView`. **The 我的 surface (`ProfileRootView`) reuses the same load→clean→resolve shape (Profile real-AppData read path V1)** for the user's real profile / units / screening / settings + the derived latest body weight; because Profile is **not** engine-related, its pure branch resolver `resolveProfileDisplayState` lives in `IronPathDataHealth` (the owner of `CleanAppDataView`) rather than the engine package, keeping the import graph a DAG. **The 记录 surface (`HistoryRootView`) reuses the same load→clean→resolve shape (History real-AppData read path V1)** to render ONE unified, most-recent-first timeline of real completed training — native completed sessions from the DataHealth-cleaned `history` (tagged 原生) merged with the derived `importedWorkoutSamples` (tagged 来自 Apple 健康); its pure resolver `resolveHistoryDisplayState` + the `CompletedTrainingTimeline` merge live in `IronPathDataHealth`/`IronPathDomain`. A native session completed WITHOUT per-set detail is saved only to the local Focus snapshot store (never canonical `history`), so the thin model ALSO reads that store read-only and feeds it through a NEUTRAL `SupplementalNativeCompletion` value, merged + **deduped by id** (canonical wins) — losing no completion while keeping `IronPathDomain`/`IronPathDataHealth` free of any `IronPathLocalSnapshot` import (the two stay decoupled, §12). LocalSnapshot draft-recovery / restore / detail still live on the 训练 tab, untouched.

**Deterministic clock:** `FocusModeMvpState.clock` defaults to a **deterministic** reference date so package tests/previews are reproducible. `static let systemClock = { Date() }`; `useSystemClock()` is called exactly once, from `FocusModeShellView.task`, to opt into wall-clock time at launch. **Do not make the system clock the default.**

**Local-first only:** native iOS has **no** network, cloud, auth, HealthKit, or WebView. On-device durability is **local JSON files only** (§12), guarded statically (§22).

---

## 6. Swift Package Boundaries

There are **exactly 12 local Swift packages** under `ios/packages/`, wired into the app target (+ the W-1 `IronPathWidgetExtension` widget target) via `ios/IronPath.xcodeproj`. The app links all 12, but several are inert stubs.

### 6.1 Active packages
| Package | Purpose | Depends on |
| --- | --- | --- |
| `IronPathDomain` | Canonical data model: `AppData`, `SchemaVersion`, `JSONValue` (open bag), `TrainingSession`/`TrainingSetLog`/`ActualSetDraft`, `ExercisePrescription`, `MesocyclePlan`, `ProgramTemplate`, profiles, settings. Pure `Codable`, no IO. | *(Foundation only — dependency leaf)* |
| `IronPathDataHealth` | AppData validation + auto-repair: `buildCleanAppDataView` (clean read-only view), `AppDataIngressPipeline`, `AutoRepairOrchestrator`/`RepairEngine`/`RepairLedger`/`RepairRegistry`, `JSONFileAutoRepairBackupAdapter`, `DataHealthRuntimeGuard`. Mirrors `src/dataHealth/cleanAppDataView.ts`. | `IronPathDomain` |
| `IronPathTrainingDecision` | Pure decision engine: readiness, e1RM trend, exercise prescription, role floors, deload, modes, mesocycle/phase, arbitration trace. Consumes **only** `CleanTrainingDecisionInput`. Mirrors `src/engines/trainingDecisionCleanInput.ts`. | `IronPathDomain`, `IronPathDataHealth` |
| `IronPathPersistence` | Canonical-AppData persistence seam: `AppDataStore` protocol + `JSONFileAppDataStore` (atomic JSON-on-disk, backup). | `IronPathDomain` |
| `IronPathLocalSnapshot` | Focus session **history** (separate from AppData): `LocalSessionSnapshotStore` (the sanctioned on-disk JSON store), `LocalDraftRestorePlanner`, `LocalSnapshotHistory` (`.filtered`/`.grouped`), `LocalSnapshotStats`, `LocalSnapshotMigration`, `LocalCompletedSessionSnapshot`. | *(Foundation only — deliberately NOT coupled to Domain/AppData)* |
| `IronPathHealthKit` | **HK-1 + HK-2: approved, bounded, READ-ONLY Apple-Health adapter** (activated from the iOS-1 stub). HK-1: `BodyMassReading`/`BodyMassSampleSource` seam + pure `HealthKitBodyMassMapper` (→ `HealthMetricSample`, kg) + `HealthKitBodyMassImporter`. HK-2: `WorkoutReading`/`WorkoutSampleSource` seam + pure `HealthKitWorkoutMapper` (→ `ImportedWorkoutSample`, `source: "healthkit_import"`, DERIVED) + `HealthKitWorkoutImporter`. The real `HKHealthStore`/`HKWorkout` readers (`HealthKitBodyMassSource` + `HealthKitWorkoutSource`) are co-located in the ONLY file that imports HealthKit, compiled `#if os(iOS)` (host `swift test` excludes it). HK-3: the `WorkoutExport` seam (`WorkoutExportRequest`/`WorkoutExportSummary`/`WorkoutExportSink`) + pure `HealthKitWorkoutExporter` (native `TrainingSession` → export request, native-only) with `HealthKitWorkoutSource` also conforming to `WorkoutExportSink` in that SAME single adapter — exporting native completed sessions to Apple Health as `HKWorkout`s via `HKHealthStore.save` (the first & only write-back; reads still share nothing, export shares ONLY the workout type, no other type written). | `IronPathDomain` |
| `IronPathL10n` | Localization helper. | *(standalone)* |
| `IronPathNotifications` | **N-1 + N-2: approved, bounded, LOCAL-only notification adapter package.** Pure `RestReminderPolicy` (N-1: exercise-role rawValue → recommended rest seconds; injected clock → fire instant) + `TrainingReminderPolicy` (N-2: selected weekdays + time → repeating reminder requests; injected `now`/`Calendar` → next fire; reconstruct schedule from pending) + the `RestReminderScheduling` / `TrainingReminderScheduling` seams. The real `UNUserNotificationCenter` schedulers (`UserNotificationsRestReminderScheduler` + `UserNotificationsTrainingReminderScheduler`) live in the ONLY file that imports UserNotifications, compiled `#if os(iOS)`. LOCAL only (`UNTimeIntervalNotificationTrigger` rest + `UNCalendarNotificationTrigger` repeating training) — no remote/push, no network. | *(Foundation only — standalone; maps a role rawValue String, so no `Domain` edge)* |
| `IronPathWidgetShared` | **W-1: approved, bounded, read-only widget share** (new package). Pure `ReadinessWidgetSnapshot` (+ JSON codec) + `ReadinessWidgetPresentation` (snapshot → view state, honest placeholder) + `WidgetSnapshotStore`/`WidgetReloading` seams. The real App Group `FileManager` store (`AppGroupWidgetSnapshotStore`) + the WidgetKit reloader (`WidgetTimelineReloader`, the ONLY WidgetKit importer here) are `#if os(iOS)`. The derived snapshot is read-only for the widget and never a source of truth (§12). | *(Foundation only — standalone; the snapshot carries plain Strings, so no `Domain` edge)* |

### 6.2 Stub packages (inert, `0.0.1` bootstrap) — **must stay inert**
| Package | Reserved for (DEFERRED) | Today |
| --- | --- | --- |
| `IronPathCloudSync` | Cloud sync | Stub. No network/CloudKit. `Version` only. |
| `IronPathBackup` | Backup/export | Stub. `Version` only. |
| `IronPathUIKit` | Shared UI components | Stub. `Version` only. |

> The stubs are linked solely so `IronPathLinkedPackages.versions` can drive the iOS bootstrap parity test. Adding real cloud/sync/UI behavior to one of **these** stubs is a **forbidden change** (§18). `IronPathHealthKit` was **activated** by HK-1 (now in §6.1) — a read-only Apple-Health adapter approved in §17/§18, its read scope refined to workout history by HK-2/HK-2b (§8.2), and given a bounded, user-triggered workout **EXPORT** (the first write-back) by HK-3 (§8.2/§16/§17/§18); it retains its `Version` constant for the bootstrap probe. No other stub may be filled without its own approved, contract-amending task.

### 6.3 The package rules (enforceable)
1. **`IronPathDomain` is the dependency leaf** (Foundation only).
2. **`IronPathLocalSnapshot` stays decoupled from `IronPathDomain`/AppData.** It is a presentation-layer history record, not the canonical model. Do not make it import `IronPathDomain`.
3. **The import graph is a DAG** — no cycles. Current edges: `DataHealth → Domain`; `TrainingDecision → Domain, DataHealth`; `Persistence → Domain`; `HealthKit → Domain` (HK-1 — the pure mapper emits `HealthMetricSample`; `Domain` stays the leaf); `LocalSnapshot`, `L10n`, `Notifications` (N-1 — Foundation-only; the policy maps an exercise-role rawValue `String` to rest seconds, so it needs no `Domain` edge), `WidgetShared` (W-1 — Foundation-only; the readiness snapshot carries plain Strings, so it needs no `Domain` edge), and the 3 remaining stubs are standalone; the app target links all 12, and the W-1 `IronPathWidgetExtension` widget target additionally links `WidgetShared`.
4. **Packages never depend on the app target.**
5. **Stub packages stay inert** until an approved architecture task (this document amended) fills them.

---

## 7. TypeScript Module Boundaries

| Module | Responsibility | May import |
| --- | --- | --- |
| `src/` | PWA React app: pages, hooks, view state, in-browser engine, gated cloud-production scaffolding | `packages/core`, `packages/contracts` |
| `packages/core/` | Shared **pure** domain/engine logic; canonical source for parity goldens | `packages/contracts` (types) |
| `packages/contracts/` | Shared types + JSON-schema contracts (`ajv`) | *(nothing app-specific)* |
| `apps/api/` | Dev/SSR API runner (`devApiRunner`); backend **candidate**, local-dev only | `packages/core`, `packages/contracts` |

**Rules:**
1. Dependencies flow **one way**: `src` and `apps/api` depend on `packages/*`; `packages/*` never depend on `src` or `apps`.
2. `packages/core` is the **canonical engine**. Changing its output **requires regenerating parity goldens** (§21).
3. `apps/api` must not be treated as a production backend, and the PWA must not hard-require it to function.

---

## 8. Source-of-Truth Contract

> The **source of truth** is the canonical AppData document. Everything else is a *view*, a *draft*, or a *history snapshot* derived from it.

| Surface | Canonical source of truth today | Derived records |
| --- | --- | --- |
| PWA | Client-side AppData (local-first). Cloud GATED (§17). | engine views, presenters |
| Native iOS | `IronPathDomain.AppData`, persisted as JSON via `IronPathPersistence.JSONFileAppDataStore` | `IronPathLocalSnapshot` Focus-session history (separate store, **not** AppData); in-RAM drafts |
| Cross-platform | AppData **shape** + engine **semantics**, enforced by parity goldens | — |

**Hard rules:**
1. There is exactly **one** canonical AppData per surface. Drafts and Focus-history snapshots are **derived** and must never silently become the source of truth.
2. **The Focus-history store (`IronPathLocalSnapshot`) must never read or write canonical AppData.** It is a presentation-layer record by design.
3. **No task may change the source-of-truth boundary** (where canonical AppData lives, or who may overwrite it) without amending this document first.
4. Canonical AppData is mutated only through the established path (`IronPathPersistence` save, gated by DataHealth where untrusted input is involved). Do not add a second, parallel write path.

### 8.1 First native canonical-AppData write path (iOS-17A)

Until iOS-17A the native canonical store was **read/seam-only**. iOS-17A activates the **first** native write path, and it is the **only** sanctioned one (rule 4 above): the user's freshly-performed sets are appended to canonical `AppData.history`.

- **Owner:** `IronPathPersistence.CanonicalSessionWriter` — pure orchestration over the injected `AppDataStore` + a DataHealth gate closure. It does **not** import DataHealth (the gate is injected by the app layer), so the package import graph (§6.3: `Persistence → Domain`) is unchanged.
- **Shape:** performed sets are written to `history[].exercises[].sets` (`TrainingSetLog`, kg-stored), built by the pure `IronPathDomain.NativeCompletedSessionBuilder`. They are **never** written to `focusActualSetDrafts` — that is the in-progress lifecycle buffer the DataHealth session-lifecycle guard clears on a `completed` session, so storing performed sets there would be silently lost on reload.
- **Append-only:** the writer only **appends** a newly-created completed session (`AppData.appendingHistorySession`, open-bag preserving, no `schemaVersion` change). It never replaces or merges an external document — that is the **deferred** full-AppData restore (§14), which this path does **not** implement.
- **DataHealth-gated (§10):** before any write, the candidate is routed through `processIncomingAppData` (read-only: `allowMutation:false`, `allowAutoRepair:false`) and accepted **only** when the appended session survives `buildCleanAppDataView` with all its performed sets intact. A rejected candidate is **not** written.
- **Backup-before-overwrite + atomic + no fake success:** when a prior canonical file exists, `store.backup()` runs **before** `store.save()`; the save is atomic; every failure **throws** and is surfaced honestly (`FocusCanonicalSaveStatus.failed`). Nothing is reported as saved unless it was.
- **Honest no-op:** if no per-set detail was captured, nothing canonical is written (`.skipped`) — never a fabricated empty record.
- **Rollback story:** the timestamped `…backup-<ISO>` copy taken before each overwrite is the rollback artifact — a bad new write can be reverted by restoring that backup. A first-ever write (no prior file) seeds from `AppData.emptyCurrent()` and needs no backup. A present-but-**unreadable** document is **never** overwritten (`existingDocumentUnreadable` is thrown — unparseable user data is preserved, not destroyed). Each backup **filename is uniquified** (millisecond ISO-8601 stamp + a disambiguating `-N` suffix on a same-instant clash, FIX-1), so rapid successive sanctioned writes never false-fail on a same-second name collision — backup-before-overwrite, atomicity, and honest failure (a genuine backup failure still throws) are unchanged.

**Source-of-truth impact:** this **activates** (does not move) the canonical write boundary already named in the table above. Canonical AppData still lives in `JSONFileAppDataStore`; iOS-17A makes the app actually write to it through the single sanctioned, DataHealth-gated path. The Focus-history store and its new v3 `setLogs` (§12) remain **derived** and are never read back as the source of truth.

### 8.2 HealthKit imports (HK-1 body weight, HK-2 workout history)

HK-1 adds a **second typed entry point on the SAME write path — not a second write path** (rule 4). Apple Health body weight is imported into canonical AppData through the §8.1 owner `IronPathPersistence.CanonicalSessionWriter`, which now exposes `appendHealthMetricSample` alongside `appendCompletedSession`; both delegate to one private gated orchestration (`performGatedMutation`: load → build candidate → DataHealth gate → backup → atomic save → honest throw). **HK-2 adds a THIRD entry point on that SAME path** — `appendImportedWorkoutSample` (+ a batch `appendImportedWorkoutSamples` that folds a whole history list into one gated write) — still through `performGatedMutation`, still not a new write path.

- **Landing spot:** `AppData.healthMetricSamples` — a `HealthMetricSample { metricType: "body_weight", unit: "kg", source: "apple_health_export" }` (kg, built by the pure `IronPathHealthKit.HealthKitBodyMassMapper`). This is the modeled home for Apple-Health-origin samples, mirroring the PWA (`src/engines/healthImportEngine.ts` / `appleHealthTypeMap.ts`). The import does **NOT** write `userProfile.weightKg` — that is the user's self-entered field; the "current body weight" is **derived** as the latest body-weight sample (`src/engines/healthSummaryEngine.ts` → `latestBodyWeightKg`).
- **Append-only + idempotent:** `AppData.appendingHealthMetricSample` is open-bag preserving (only `healthMetricSamples` is rewritten; `schemaVersion` unchanged — no schema bump) and dedups by the sample's content-addressed `id`, so re-importing the same latest reading is a clean no-op.
- **DataHealth-gated (§10), backup-before-overwrite, atomic, no fake success:** identical guarantees to §8.1. The candidate is routed read-only through `processIncomingAppData` (`allowMutation:false, allowAutoRepair:false`) and accepted only when the imported sample survives the clean view; a prior file is backed up before the atomic save; every failure throws and is surfaced honestly; a present-but-unreadable document is never overwritten.
- **Read-only ingress, on-device:** HealthKit is read ONLY (authorization shares nothing — `toShare: []`); imported data never leaves the device. The real `HKHealthStore`/`HKWorkout` readers (`HealthKitBodyMassSource` + `HealthKitWorkoutSource`) are co-located in ONE `#if os(iOS)` file; the mapping is pure and unit-tested via the injectable `BodyMassSampleSource` / `WorkoutSampleSource` seams.

**HK-2 — derived, NON-canonical landing (the hard red line):** imported Apple-Health **workouts** land in `AppData.importedWorkoutSamples` (`source: "healthkit_import"`), a bag **SEPARATE from `history`**. They are **derived / display-only**: they are NEVER native canonical `TrainingSession`s and NEVER feed the `IronPathTrainingDecision` engine (readiness / e1RM). The native engine does not port `buildHealthSummary`, so — like HK-1's `healthMetricSamples` — `importedWorkoutSamples` is unread by the engine; this is guarded (`iosHealthKitWorkoutImportStaticGuards`: no `IronPathTrainingDecision` source references `importedWorkoutSamples`). Making an imported workout canonical, or letting it influence the engine, is a **source-of-truth/engine-input change requiring its own approved task** — not HK-2.

**Source-of-truth impact:** uses the already-activated §8.1 write boundary; introduces no new write path and does not move where canonical AppData lives. Imported workouts are explicitly **non-canonical** derived records.

**HK-3 — workout export (write-back), OUTSIDE the §8.1 canonical write path.** The user-triggered HK-3 export READS native completed sessions from `AppData.history` and WRITES them to Apple Health as `HKWorkout`s (`HealthKitWorkoutSource` conforming to the pure `WorkoutExportSink` seam; the pure `HealthKitWorkoutExporter` does the native-only mapping). It does **NOT** go through `CanonicalSessionWriter` and does **NOT** write `AppData` — **AppData stays the source of truth; Apple Health is a DERIVED export target.** Idempotency lives in Apple Health (each `HKWorkout` is tagged with the IronPath session id in metadata and a pre-export query skips ids already present), so there is **no app-side dedup storage and no schema bump**. Native-only / no-loop-back: the mapper accepts only `[TrainingSession]`, so an imported (`healthkit_import`) workout is never written back. Export shares ONLY the workout type (`toShare: [workoutType]`); no other Apple-Health type is written. Device-local. Guarded by `iosHealthKitWorkoutExportStaticGuards` (+ `iosBootstrapForbiddenImports`).

### 8.3 First native canonical-AppData EDIT write path (EDIT-1)

EDIT-1 activates the **first native in-place EDIT** of canonical AppData: the user edits their own `UserProfile` scalar fields (姓名/性别/年龄/身高/体重/训练水平/目标/每周天数/单次时长) in the 我的 tab. It is a **sanctioned MUTATION alongside append — NOT a second write path** (rule 4).

- **One path, generalized in name:** the previously append-only private orchestration is now **`performGatedMutation`** (it already took an arbitrary `(AppData) -> AppData` candidate transform). A new entry point `CanonicalSessionWriter.updateProfile` funnels through it, so append and edit share the identical `load → DataHealth gate → backup-before-overwrite → atomic save → honest throw` ceremony on the same `JSONFileAppDataStore`. (`iosHealthKitWorkoutImportStaticGuards` updated in sync to the new name; net write-path protection unchanged.)
- **Open-bag candidate builder:** the pure `AppData.withUpdatedProfile` rewrites **only** the `userProfile` key; every other top-level key + all unknown fields survive verbatim, `schemaVersion` is unchanged (**no bump**), ISO timestamps are carried through (§9). `UserProfile.withScalarFields` preserves the profile's own `id`/`injuryFlags`/`painNotes`/open-bag while replacing the nine scalars.
- **Defensive DataHealth gate (§10):** the app supplies a `validate` gate that re-encodes → re-decodes (re-running the `SchemaVersion` guard) → routes the candidate through `buildCleanAppDataView` and accepts it **only** if the edited profile survives byte-identical. An invariant-breaking candidate is rejected and nothing is written (no fake success).
- **Two-step, user-explicit:** the surface stays the read-only #438 view until the user taps 「编辑」, edits, and taps 「保存」 — nothing is written without the explicit save. Honest 已保存/未保存·失败 status; a present-but-unreadable document is never overwritten.
- **Self-entered weight:** 体重 edits `userProfile.weightKg` (kg-stored, entered in the display unit), distinct from the HK-derived `healthMetricSamples` (never touched).
- **Not a restore (§13/§14):** an in-place single-key edit of the already-canonical document; it never replaces/merges an external/backup document.

**Source-of-truth impact:** **activates** (does not move) the canonical write boundary — canonical AppData still lives in `JSONFileAppDataStore`; EDIT-1 makes the app write a user edit to it through the single sanctioned, DataHealth-gated path.

**EDIT-2 note (display-unit preference, kg/lb).** EDIT-2 **reuses** this exact edit-write boundary for the 我的 tab's display-unit toggle: a sibling entry point `CanonicalSessionWriter.updateUnitSettings(displayUnit:)` funnels through the SAME `performGatedMutation` (NOT a second write path — still exactly one `store.save`), and the pure open-bag `AppData.withUpdatedUnitSettings` + `UnitSettings.withDisplayUnit` rewrite **only** `unitSettings.displayUnit` (reading the freshly-loaded on-disk unit settings, so the typed `weightUnit` + any unknown unit key survive verbatim), `schemaVersion` unchanged (**no bump**). Same defensive `buildCleanAppDataView` re-validation; same backup→atomic→honest-fail; a present-but-unreadable document is never overwritten. **Storage stays kilograms** — only the DISPLAY preference is persisted, no stored weight is coerced. User-explicit toggle (the one-time seed never writes); not a restore (§13/§14). **Source-of-truth impact: reuses (does not move) the EDIT-1 boundary.** Guarded by `UnitSettingsEditTests` + the EDIT-2 cases in `CanonicalSessionWriterTests`.

**EDIT-3 note (screening list edit).** EDIT-3 **reuses** this exact edit-write boundary for the 我的 tab's screening profile: a sibling entry point `CanonicalSessionWriter.updateScreening(_:)` funnels through the SAME `performGatedMutation` (NOT a second write path — still exactly one `store.save`), and the pure open-bag `AppData.withUpdatedScreening` + `ScreeningProfile.withEditedLists` rewrite **only** the `screeningProfile` key's three self-reported lists (疼痛触发 `painTriggers` / 受限动作 `restrictedExercises` / 纠正优先 `correctionPriority`), `schemaVersion` unchanged (**no bump**). The engine/DataHealth-managed `adaptiveState` (issue scores / performance drops), `postureFlags`, `movementFlags`, `userId`, and any unknown screening key survive verbatim — the edit touches ONLY the three user lists. Same defensive `buildCleanAppDataView` re-validation (comparing the clean view's **raw** screening, since `buildCleanScreening` legitimately re-caps/filters the engine-managed `adaptiveState`); same backup→atomic→honest-fail; a present-but-unreadable document is never overwritten. User-explicit two-step 「编辑→保存」; not a restore (§13/§14). **Source-of-truth impact: reuses (does not move) the EDIT-1 boundary.** Guarded by `ScreeningProfileEditTests` + the EDIT-3 cases in `CanonicalSessionWriterTests`.

**EDIT-4 note (program config edit).** EDIT-4 **reuses** this exact edit-write boundary for the 计划 (Plan) tab's program template: a sibling entry point `CanonicalSessionWriter.updateProgramConfig(primaryGoal:splitType:daysPerWeek:)` funnels through the SAME `performGatedMutation` (NOT a second write path — still exactly one `store.save`), and the pure open-bag `AppData.withUpdatedProgramConfig` + `ProgramTemplate.withConfigScalars` rewrite **only** the `programTemplate` key's three user scalar config fields (主要目标 `primaryGoal` / 分项 `splitType` / 每周天数 `daysPerWeek`), reading the freshly-loaded on-disk program template, `schemaVersion` unchanged (**no bump**). The program's `id` / `userId` / the engine-managed `correctionStrategy` / `functionalStrategy` strategy blobs / any unknown program key — and the engine-managed STRUCTURED plan that lives elsewhere (`mesocyclePlan` weeks array / prescriptions / adaptive state) — survive verbatim; the edit touches ONLY the three user scalars. Same defensive `buildCleanAppDataView` re-validation (comparing the clean view's **raw** program, which `buildCleanAppDataView` leaves untouched — no program cleaning); same backup→atomic→honest-fail; a present-but-unreadable document is never overwritten. User-explicit two-step 「编辑→保存」; not a restore (§13/§14). Thin UI inlined in `PlanRootView.swift` (no new app file, `project.pbxproj` untouched). **Source-of-truth impact: reuses (does not move) the EDIT-1 boundary.** Guarded by `ProgramConfigEditTests` + the EDIT-4 cases in `CanonicalSessionWriterTests`.

**DEEP-EDIT-1 note (logged-set correction).** DEEP-EDIT-1 **reuses** this exact edit-write boundary for the 记录 (History) saved-session detail — the FIRST edit to reach a NESTED structure (`history[]→exercises[]→sets[]`) rather than a top-level scalar: a sibling entry point `CanonicalSessionWriter.updateHistorySet(sessionId:exerciseId:setIndex:weightKg:reps:rir:)` funnels through the SAME `performGatedMutation` (NOT a second write path — still exactly one `store.save`), and the pure nested open-bag `AppData.withUpdatedHistorySet` (+ `TrainingSetLog.withCorrectedMetrics` / `ExercisePrescription.withUpdatedSets` / `TrainingSession.withUpdatedExercises`) rewrites **only** the matched set's three metric fields (重量 `weight` kg / 次数 `reps` / `rir`) inside **only** the `history` key, `schemaVersion` unchanged (**no bump**). It preserves the open bag at EVERY level: every other `history` session, every other exercise, every other set, each level's unknown keys, and the edited set's identity (`setIndex`/`exerciseId`/`id`) / `completedAt` ISO timestamp / `done` survive verbatim. A logged set is an ENGINE INPUT (the user's own recorded performance — `TrainingSetLog`), so the engine recomputing e1RM/readiness FROM the corrected value is the EXPECTED behaviour (DEEP-EDIT review §1/§4); it NEVER touches an engine OUTPUT (the `mesocyclePlan` weeks blob, prescription/advice fields, computed phase/readiness/e1RM), which are left verbatim and recomputed by the engine, not this edit (**§11 unchanged**). Same defensive `buildCleanAppDataView` re-validation (the corrected set must SURVIVE the clean view AND its metrics must land exactly as intended); same backup→atomic→honest-fail; a present-but-unreadable document is never overwritten. Weight entered in the display unit, stored as kg (storage stays kilograms). User-explicit two-step 「编辑→保存」; not a restore (§13/§14). Thin UI inlined in `FocusSavedSessionDetailView.swift` (no new app file, `project.pbxproj` untouched). **Source-of-truth impact: reuses (does not move) the EDIT-1 boundary.** Guarded by `HistorySetEditTests` + the DEEP-EDIT-1 cases in `CanonicalSessionWriterTests`.

**SR-4 note (exercise replacement — 换动作 / 复原).** SR-4 **reuses** this exact edit-write boundary for the 记录 (History) saved-session detail's "换动作" (smart exercise replacement) — the SECOND edit into the NESTED `history[]→exercises[]` structure (after DEEP-EDIT-1's `…→sets[]`): a sibling entry point `CanonicalSessionWriter.updateExerciseReplacement(sessionId:exerciseId:replacementExerciseId:)` funnels through the SAME `performGatedMutation` (NOT a second write path — still exactly one `store.save`), and the pure nested open-bag `AppData.withUpdatedExerciseReplacement` (+ `ExercisePrescription.withReplacedIdentity` / `TrainingSession.withUpdatedExercises`) rewrites **only** the matched exercise's three USER-OVERRIDE identity fields (`actualExerciseId` / `displayExerciseId` / `recordExerciseId`) inside **only** the `history` key — APPLY sets all three to the chosen replacement, RESTORE (a nil `replacementExerciseId`) clears all three (so the record falls back to the untouched original `id`/`exerciseId`) — `schemaVersion` unchanged (**no bump**). It preserves the open bag at EVERY level (every other session / exercise / set, each level's unknown keys) and, on the target exercise, the engine-opened `originalExerciseId`, the prescription body (`id`/`exerciseId`/`name`/`prescription`/advice), and the performed `sets` survive verbatim. The user's chosen ACTUAL exercise is recorded performance attribution (an engine INPUT); it NEVER touches an engine OUTPUT — the engine-opened `originalExerciseId`, the `mesocyclePlan` weeks blob, prescriptions, and any computed phase/readiness/e1RM are left verbatim and recomputed by the engine FROM the clean record, not this edit (**§11 unchanged in contract; the smart-replacement engine now also produces recommendations — see §11 SR-4 note**). Same defensive read-only clean-view re-validation (the override must land EXACTLY — apply → all three == the replacement; restore → all three cleared — AND the exercise must survive the clean view); same backup→atomic→honest-fail; a present-but-unreadable document is never overwritten. User-explicit two-step 「换动作 → 保存」 (+ 「恢复原动作」); not a restore (§13/§14). Thin UI inlined in `FocusSavedSessionDetailView.swift` (no new app file, `project.pbxproj` untouched). **Source-of-truth impact: reuses (does not move) the EDIT-1 boundary.** Guarded by `ExerciseReplacementEditTests` + the SR-4 cases in `CanonicalSessionWriterTests`.

---

## 9. AppData Boundary

`IronPathDomain.AppData` is the canonical model. Its invariants are **data-safety invariants**, static-guarded by tests.

**Invariants (do not break):**
1. **Pure value type, no IO.** `AppData` and its members perform no network/disk/persistence operations.
2. **Lossless round-trip, including unknown fields,** via the `JSONValue` "open bag." Guarded by `AppDataOpenBagPreservationTests`, `AppDataUnitFieldPreservationTests`, `AppDataTypedFieldActivationTests`, `AppDataCodableRoundTripTests`.
3. **Schema version is explicit and change-guarded.** Bumping `SchemaVersion` requires a deliberate migration **and** updating `AppDataSchemaVersionGuardTests`.
4. **ISO-8601 timestamps only.** Guarded by `AppDataIsoTimestampStaticGuardTests`.
5. **Parity with the real PWA export.** Guarded by `AppDataRealExportParityTests`. A canonical JSON form (`AppData.canonicalJSONData()`) backs the persistence hash/round-trip contract.

**iOS-17A note (history append):** `AppData.appendingHistorySession` rewrites **only** the `history` key (in place), appending one completed session built from typed models. It preserves every other top-level key and all unknown/open-bag fields verbatim, leaves `schemaVersion` unchanged (an append is not a schema change), and stays a pure value transform (no IO in `IronPathDomain`). Guarded by `NativeCompletedSessionTests` (open-bag + existing-history preservation + canonical round-trip).

**EDIT-1 note (profile edit):** `AppData.withUpdatedProfile` rewrites **only** the `userProfile` key (in place) and `UserProfile.withScalarFields` replaces only the nine editable scalars — both preserve every other key, all unknown/open-bag fields (top-level **and** within the profile), `schemaVersion` (no bump), and ISO-8601 timestamps; both are pure value transforms (no IO in `IronPathDomain`). An edit is a sanctioned MUTATION (§8.3), not an append and not a restore (§13/§14). Guarded by `ProfileScalarEditTests` (open-bag + non-scalar preservation + canonical round-trip) and the EDIT-1 cases in `CanonicalSessionWriterTests`.

**EDIT-2 note (unit-settings edit):** `AppData.withUpdatedUnitSettings` rewrites **only** the `unitSettings` key (in place) and `UnitSettings.withDisplayUnit` replaces only the `displayUnit` preference — both preserve every other key, all unknown/open-bag fields (top-level **and** within the unit settings, including the typed `weightUnit`), `schemaVersion` (no bump), and ISO-8601 timestamps; both are pure value transforms (no IO in `IronPathDomain`). **Storage stays kilograms** (invariant: `weight` fields are kg; the unit fields are display-only) — this edit coerces no stored value. A sanctioned MUTATION (§8.3), not a restore (§13/§14). Guarded by `UnitSettingsEditTests` (open-bag + weightUnit preservation + canonical round-trip) and the EDIT-2 cases in `CanonicalSessionWriterTests`.

**EDIT-3 note (screening edit):** `AppData.withUpdatedScreening` rewrites **only** the `screeningProfile` key (in place) and `ScreeningProfile.withEditedLists` replaces only the three self-reported lists (`painTriggers`/`restrictedExercises`/`correctionPriority`) — both preserve every other key, all unknown/open-bag fields (top-level **and** within the screening, including the engine-managed `adaptiveState`/`postureFlags`/`movementFlags`/`userId`), `schemaVersion` (no bump), and ISO-8601 timestamps; both are pure value transforms (no IO in `IronPathDomain`). The engine-managed `adaptiveState` (issue scores / performance drops) is carried through untouched — DataHealth's `buildCleanScreening`, not this edit, owns it. A sanctioned MUTATION (§8.3), not a restore (§13/§14). Guarded by `ScreeningProfileEditTests` (open-bag + engine-state preservation + canonical round-trip) and the EDIT-3 cases in `CanonicalSessionWriterTests`.

**EDIT-4 note (program-config edit):** `AppData.withUpdatedProgramConfig` rewrites **only** the `programTemplate` key (in place) and `ProgramTemplate.withConfigScalars` replaces only the three user scalar config fields (`primaryGoal`/`splitType`/`daysPerWeek`) — both preserve every other key, all unknown/open-bag fields (top-level **and** within the program, including `id`/`userId` and the engine-managed `correctionStrategy`/`functionalStrategy` strategy blobs), `schemaVersion` (no bump), and ISO-8601 timestamps; both are pure value transforms (no IO in `IronPathDomain`). The engine-managed STRUCTURED plan (`mesocyclePlan` weeks / prescriptions / adaptive state) is never touched — only the three user scalars on `programTemplate`. A sanctioned MUTATION (§8.3), not a restore (§13/§14). Guarded by `ProgramConfigEditTests` (open-bag + strategy/structure preservation + canonical round-trip) and the EDIT-4 cases in `CanonicalSessionWriterTests`.

**DEEP-EDIT-1 note (logged-set edit):** `AppData.withUpdatedHistorySet` rewrites **only** the `history` key (in place), re-emitting ONLY the matched session; `TrainingSetLog.withCorrectedMetrics` / `ExercisePrescription.withUpdatedSets` / `TrainingSession.withUpdatedExercises` replace only the target set's 重量/次数/rir (resp. only the `sets`/`exercises` array) — all preserve every other key, all unknown/open-bag fields at every nesting level (session / exercise / set), `schemaVersion` (no bump), and ISO-8601 timestamps (including the edited set's `completedAt`); all are pure value transforms (no IO in `IronPathDomain`). This is the FIRST edit into a NESTED structure, but the invariant is identical — every sibling set/exercise/session + every level's open bag survive verbatim. A sanctioned MUTATION (§8.3), not a restore (§13/§14). The engine-managed OUTPUTS (mesocycle weeks / prescriptions / computed e1RM) are never touched (§11) — a logged set is an engine INPUT the engine recomputes from. Guarded by `HistorySetEditTests` (per-level open-bag + sibling preservation + engine-output preservation + canonical round-trip) and the DEEP-EDIT-1 cases in `CanonicalSessionWriterTests`.

**FIX-1 note (backup filename ≠ AppData timestamp):** the canonical store's backup *filename* gained millisecond precision + same-instant `-N` disambiguation (§8.1) to remove a same-second `.backupFailed` false failure. This is a **persistence-layer artifact only** — `AppData`'s own ISO-8601 timestamp **encoding (invariant 4) is unchanged**, no field is added/dropped, `schemaVersion` is untouched, and `IronPathDomain` is not modified.

**Forbidden:** dropping/normalizing-away unknown fields; silent schema bumps; adding IO to `IronPathDomain`; changing timestamp encoding; making `IronPathDomain` depend on another package.

---

## 10. DataHealth Boundary

`IronPathDataHealth` is the **validation chokepoint** between untrusted AppData and the engine / any restore.

- `buildCleanAppDataView(appData, clock:)` (`CleanAppDataViewBuilder.swift`) turns possibly-untrusted AppData into a **clean, validated, read-only view**.
- `AppDataIngressPipeline` is the ingress that calls it; auto-repair (`AutoRepairOrchestrator`/`RepairEngine`/`RepairLedger`/`RepairRegistry` + `JSONFileAutoRepairBackupAdapter`) repairs **after taking a file backup first**.

**Rules:**
1. Downstream consumers (notably `IronPathTrainingDecision`) **must receive the clean view, never raw AppData.**
2. `buildCleanAppDataView` is the **gate** any future **full AppData restore** must pass through (§14).
3. Auto-repair must **back up before rewriting** and must **not fake success** (errors are surfaced honestly).
4. Do not bypass DataHealth to feed raw AppData into the engine or a restore path.
5. **External imports are gated here too (HK-1 + HK-2).** Untrusted external data — Apple Health body weight (§8.2) **and** workout summaries (§8.2, HK-2) — is routed through `processIncomingAppData` / `buildCleanAppDataView` **before** the canonical write and accepted only if it survives the clean view. HealthKit-origin samples never reach canonical AppData except through this chokepoint. Imported workouts land in the **derived, display-only** `importedWorkoutSamples` bag (never `history`, never engine input).

---

## 11. TrainingDecision Boundary

`IronPathTrainingDecision` is a **pure, deterministic** engine depending only on `IronPathDomain` + `IronPathDataHealth`.

**Rules:**
1. **Input must be `CleanTrainingDecisionInput`, built by `createCleanTrainingDecisionInput(cleanView:metadata:)` from a DataHealth clean view — never raw AppData.** `buildTrainingDecisionFromCleanInput` accepts that branded value type **only**. (Mirrors `src/engines/trainingDecisionCleanInput.ts`; the `cleanInput`/`CleanInputEvidence` field is the proof the raw AppData passed through the clean view.)
2. **Determinism.** No IO, no networking, no ambient clock/randomness — time is injected (e.g. `FixedRuntimeGuardClock`). Output is reproducible.
3. **Output shape is parity-pinned** to the PWA engine via `TrainingDecisionGoldenFixtures`, `*ParityTests`, golden-decode tests, and `TrainingDecisionShapeStabilityTests`. Changing output shape requires regenerating and reviewing goldens.
4. **`userFacing` surface / full `arbitrationTrace` is DEFERRED (`iOS-4B6`).** Only the internal/golden-decodable arbitration trace exists today. Do not ship the user-facing trace ahead of that approved task.

**iOS-17e-5 note — closed-loop adaptive output (engine now consumes recorded sets).** The progression cluster ported across iOS-17e-1~4 (per-exercise e1RM → performance query → progression suggestion → fine-tune / load feedback) is now **wired into the core slice's top-level decision**: `buildTrainingDecisionFromCleanInput` derives the e1RM trend from the cleaned performed-set history (`TrainingDecisionE1RMTrend.isE1rmTrendUp`) and folds it — via the same branch order as `trainingDecisionEngine.ts` — into the **history-driven `progressionMode` and the new `weeklyAdjustment`** projection (`TrainingDecisionModes.buildWeeklyAdjustment`: direction / magnitudePct / blockedBy / appliesFromIsoDate). This **closes the loop** the iOS-17e-0 history goldens pinned: a rising performed-set trend over ≥4 sessions yields `progressionMode='progress'` + `weeklyAdjustment.direction='increase' (+5%)`, while a flat or too-short history holds. The 3 history goldens (`progressive-overload` / `plateau-stall` / `insufficient-history`) flipped from **decode-only to compute-assert** — Swift recomputes the full decision from synthetic inputs and asserts field-by-field equality. Invariants held: **cold-start output is unchanged** (no history → no e1RM trend → no weekly increase; the 10 cold-start goldens stay byte-identical, zero drift), the output stays **parity-pinned and reproduces the TS engine** (rule 3), this remains a **pure read of recorded sets** (rule 2; no write path touched), and the e1RM trend is still the **GLOBAL** top-set pool, not per-exercise (the SR-4 caveat below is unchanged). UI surfaces (`TrainingDecisionSummaryView`) render `progressionMode` + `weeklyAdjustment` **read-only** — a mirror of engine output, never a source-of-truth write (§15).

**iOS-17e-6a note — fineTune projection is now LIVE + the `asOfDate` injected-clock contract.** The 17e-3 `ProgressionRulesEngine.makeSuggestion` shipped a golden-neutral STUB for the precision fine-tune (it hard-coded the `insufficient_history` fallback). 17e-6a REMOVES the stub: `makeSuggestion` now calls the already-ported `SetWeightFineTuneEngine.buildSetWeightFineTune` (17e-4) **for real**, threading an **injected `asOfDate`** (the §11.2 clock — rule 2). ⚠️ **`asOfDate` CONTRACT:** a `nil` asOfDate SILENTLY degrades to the `insufficient_history` fallback → the legacy decision-tree baseline. This is exactly the old stub behaviour, so the 6 existing `progression-suggestion/*` goldens (history anchored outside any plausible live window) stay **byte-identical** (the `makeSuggestion(_:_:asOfDate:)` default is `nil`); the contract is pinned in the `ProgressionRulesEngine` / `SetWeightFineTuneEngine` source headers — any LIVE decision wiring MUST pass the injected `nowIso`, because `nil` is a silent regression, not a crash. **2 NEW** `progression-suggestion/fine-tune-*` goldens pass `asOfDate = deterministicClockIso` over recent in-window history so the live `applyFineTuneIfDataRich` body (**±10% clamp / 2.5-round / legacy-respect**) is actually exercised: `fine-tune-uptrend-applied` returns the clamped projection AND proves the injected clock changes the output vs the nil/legacy baseline; `fine-tune-legacy-respect` fires the legacy-respect guard while a data-rich `fineTuneNeutrality` proves the projection ran. The TS `makeSuggestion` gains only an **additive optional `asOfDate`** (production callers omit it → keep the wall clock, behaviour unchanged). **FIX-3 correction — the original "DEFERRED to iOS-17e-6b" claim here was architecturally wrong.** `makeSuggestion` is **NOT** part of TrainingDecision (**0 callers** in `trainingDecisionEngine.ts`); its consumers are the **un-ported** `sessionBuilder`/`recommendationDiff` chain (`recommendationDiffEngine.ts:74` / `sessionBuilder.ts:76`). The iOS `TrainingDecision` per-exercise prescription is supplied by `TrainingDecisionExercisePrescription.buildWorkingSetTargets` (the `exercisePrescriptions → workingSetTargets` chain, `trainingDecisionEngine.ts:1966-1983`, iOS-4B5), **never** by `makeSuggestion`. So "wiring `makeSuggestion` into the TrainingDecision per-exercise FRAME" is a **pseudo-requirement, CLOSED**; `makeSuggestion`/`fineTune`/`loadFeedback` (17e-3/4/6a) are **parity-pinned libraries with no iOS production consumer — exactly as in TS, not a defect** — and **no `CleanTrainingDecisionInput.templateExercises` expansion is required**. A `sessionBuilder`-style "suggest next weight from history" surface, if the product ever wants it, is a separate track that ports the `sessionBuilder`/`recommendationDiff` chain. **Invariants held:** the 6 function-level goldens AND every cold-start/history decision golden are byte-identical (zero drift), output stays parity-pinned + reproduces TS (rule 3), this is a **pure read** (rule 2; no write path / `CanonicalSessionWriter` touch, zero `: Date`), and 17e-1~4 are reused without semantic change. **FIX-3 status notes (no behaviour change):** (a) **`asOfDate` contract — current state:** 17e-6a documented the contract, the `makeSuggestion(_:_:asOfDate:)` default of `nil` safely reproduces the legacy behaviour, and the 2 `fine-tune-*` goldens pin that the LIVE fineTune path is reachable; there is **no runtime `precondition` assert yet** — `nil` still *silently* degrades (above). A runtime assert (reject `nil` at a LIVE decision call site) should land **together with** the first such live call site, of which there is currently **NONE** (the 17e-3/4/6a fineTune path has no iOS production consumer). (b) **`isE1rmTrendUp` is GLOBAL:** `TrainingDecisionE1RMTrend.isE1rmTrendUp` is a **GLOBAL cross-exercise top-set pool** (faithfully reproducing TS), **not** a per-exercise signal — do not read the 6b correction as implying a per-exercise e1RM trend exists (consistent with the SR-4 caveat above).

**SR-4 note (smart-replacement recommendations + honest "respects replacement" scope).** The ported `SmartReplacementEngine.buildSmartReplacementRecommendations` (SR-3, parity-pinned to the committed goldens — **unchanged by SR-4, which only CONSUMES it; no engine logic edit, no golden change**) now **produces** the "换动作" recommendations the app's saved-session detail renders. Its input — a `SmartReplacementParams` (the current exercise + a training-history signal) — is **DERIVED FROM the DataHealth clean view** (the cleaned `history`), **never raw AppData**: the app reads canonical AppData read-only through `processIncomingAppData` → its `cleanView` (§10) and assembles the params from that, so the "engine eats clean, never raw" boundary holds. (This engine takes its own loose `SmartReplacementParams`, NOT the branded `CleanTrainingDecisionInput` of rule 1 — that brand is `buildTrainingDecisionFromCleanInput`'s contract; SR-4 still feeds clean-derived input only.)

**Honest V1 "respects replacement" scope (no overstatement).** A 换动作 records the user's chosen actual exercise via the three user-override identity fields (`actualExerciseId`/`displayExerciseId`/`recordExerciseId`, §8.3). What ACTUALLY honors that today: (1) the smart-replacement engine's history pain-pattern attribution keys off `actualExerciseId` (`collectHistoryPainPatterns`), so a recorded replacement IS reflected when it later recommends; (2) the SR-4 detail UI shows the ACTUAL exercise name (library-resolved) and the performed sets read as attributed to it. What is **NOT** honored in V1 (deferred, stated plainly): the native `TrainingDecision` e1RM trend is a GLOBAL top-set pool (`TrainingDecisionE1RMTrend`), **not** per-exercise, so a replacement does **not** change the trend; and the unified 记录 timeline (`CompletedTrainingTimeline`) still renders the stored exercise `name`, not the override. Per-exercise e1RM attribution and timeline-name honoring are **future work**, not claimed by SR-4. The engine is never handed raw AppData and its parity is unchanged.

---

## 12. Local Snapshot Persistence Boundary

`IronPathLocalSnapshot` owns the native Focus-session **history**. It is the home of the **only sanctioned app-side disk store** for Focus sessions.

**`LocalSessionSnapshotStore`** writes JSON under `<Application Support>/IronPathLocalSnapshots/` with a hard, statically-guarded local-only contract:
- **Atomic write** (`Data.write(.atomic)` → temp sibling + rename); a mid-write crash leaves the prior file intact.
- **Backup-before-overwrite** for the rolling `…-latest.json` pointer (`…-latest.json.bak`); history entries are append-only.
- **No fake success** — every mutating call throws on failure; callers must treat a throw as "not saved."
- **Scoped delete** — `clear()` only removes this store's own prefixed files in the sanctioned directory.

**Rules:**
1. **Allowed on-device storage = local JSON files via Foundation `FileManager` only.** **Forbidden** here: iCloud/CloudKit/ubiquity, HealthKit, Supabase, URLSession/network, WebKit, **UserDefaults, SQLite, CoreData, SwiftData** — enforced by `iosLocalJsonPersistenceStaticGuards`.
2. **This store must never read or write canonical AppData** (§8). It is a presentation-layer record.
3. `LocalSnapshotHistory.filtered/.grouped` and `LocalSnapshotStats.derive` are **pure** (no internal clock — `now` is injected). `LocalDraftRestorePlanner` and `LocalSnapshotMigration` are pure (no disk).
4. The canonical-AppData store (`IronPathPersistence.JSONFileAppDataStore`) is the **other** sanctioned JSON store; it is the source of truth (§8) and is distinct from this history store. As of iOS-17A it lives in its own `IronPathAppData/` subdirectory under Application Support, so the two stores never collide.
5. **iOS-17A schema v3 — derived per-set display copy.** `LocalCompletedExerciseSnapshot.setLogs` (`[LocalCompletedSetEntrySnapshot]?`: `setIndex` + kg `weightKg?`/`reps?`/`rir?`) is a **derived display copy** of the canonical performed sets, written **alongside** the canonical AppData write (§8.1) and read only to render the "上次成绩" detail. It is **never** read back as a source of truth, and this store still **never** reads/writes canonical AppData (rule 2). The bump is a guarded migration (v1/v2 files decode with `setLogs == nil` and migrate forward, showing no per-set detail honestly): `LocalCompletedSessionSnapshot.currentSchemaVersion = 3`, `LocalSnapshotValidator.acceptedSchemaVersions = {1,2,3}`, guarded by `LocalSetLogsV3Tests` + the existing migration/validation suite.
6. **W-1 App Group derived read-only share.** The home-screen widget reads a small **derived** readiness snapshot the app writes to the shared App Group container (`group.com.ironpath.app.ios`, `IronPathWidgetShared.AppGroupWidgetSnapshotStore`, atomic JSON). Like the LocalSnapshot history (rule 2), this share is a **presentation record**: the app WRITES it, the widget READS it; it is **never** read back as a source of truth and **never** touches canonical AppData. On-device only — no iCloud/network/remote. Guarded by `iosReadinessWidgetStaticGuards`.

---

## 13. Draft Recovery Boundary

Draft recovery restores the **in-memory editing buffer** of an interrupted Focus session.

**Rules (verbatim invariant):**
> **Restore re-hydrates an in-memory local draft only. It is non-destructive and is NOT a full AppData restore. It never overwrites the canonical AppData document.**

1. `FocusModeMvpState.restoreDraft(from:)` regenerates the slice from the scenario and reinstates **only** per-exercise completed counts + a clamped resume cursor, via `LocalDraftRestorePlanner.reconcile` (matched exercise ids only; impossible progress rejected).
2. **A failed restore leaves the current session untouched** (no fake restore).
3. Wiring full AppData restore into this flow is a **forbidden change** until §14 is satisfied.

---

## 14. Full AppData Restore Gate

Full AppData restore (replacing/merging canonical AppData from an external/backup document — the repair-apply pipeline `load → snapshot → backup → apply → save`) is **deferred and gated.**

**Gate conditions — ALL required before full restore may be designed or built:**
1. Input passes **`IronPathDataHealth.buildCleanAppDataView`** (no raw restore).
2. **Backup-before-apply** is guaranteed (the persistence/auto-repair backup seam), with a documented **rollback** story.
3. This document is amended with the approved restore design, including its **source-of-truth impact**.
4. A regression-lock/static-guard suite for the restore path exists and is green.

Until then: **the iOS UI restore stays an in-memory draft (§13).** The `IronPathPersistence` load/save/backup seam exists (iOS-3A foundation) but the user-facing full-restore/repair-apply product flow is **not** wired. Any PR attempting full AppData restore without satisfying this gate must be stopped and escalated.

---

## 15. UI Architecture Rules

1. **The SwiftUI app layer stays thin** — rendering + wiring only; the only logic is the `FocusModeMvpState` view-model, which delegates to packages.
2. **The PWA UI rules elsewhere in `AGENTS.md` still apply** (mobile-first; restrained; no admin dashboards; no card-stacking; collapse exercise metadata; "专注训练" label for Focus Mode in user-facing UI).
3. Views bind to **package value types** (`TrainingDecisionCoreSlice`, `LocalCompletedSessionSnapshot`, `LocalSnapshotHistory`/`Stats`), not bespoke app-layer logic.
4. Dangerous actions require confirmation; empty states need title + explanation + one action; **no fake success** (honest 已保存/未保存).
5. No network/cloud/auth/WebView code in any UI layer.

---

## 16. Local-first Rules

1. **Native iOS must remain local-first** until a future approved architecture task (this document amended) explicitly changes it.
2. On-device durability = **local JSON files via Foundation only** (the two sanctioned stores, §12). Everything else is in-RAM value types.
3. The PWA is also local-first; its cloud-production scaffolding is gated (§4, §17).
4. **No native data leaves the device.** No telemetry, no sync, no remote calls.
5. **Local notifications are allowed** (N-1 rest-timer + N-2 training): on-device `UNUserNotificationCenter` scheduling only — one-shot (`UNTimeIntervalNotificationTrigger`) and repeating weekly (`UNCalendarNotificationTrigger`, the user picks weekdays + time and iOS persists it), confined to the single `#if os(iOS)` adapter (§6.1/§18), user-authorized, nothing leaves the device. **Remote / push notifications stay forbidden** (they require a server → §17).

---

## 17. Deferred Systems

These systems are **named but not designed here.** Each is *gated*: it requires a dedicated, approved architecture task that amends this document **before any implementation.**

| System | Current state | Gate before building |
| --- | --- | --- |
| Full AppData restore / repair-apply | Deferred (seam exists) | §14 gate |
| Cloud sync / CloudKit | Stub (`IronPathCloudSync`); `IronPathPersistence` comments name a CloudKit-backed store for **iOS-7** | Approved task; source-of-truth + offline-merge design |
| Supabase / backend (production) | PWA `src/cloudProduction/*` scaffolding present, **gated**; `apps/api` is dev-only | Auth + account + hosting architecture gate |
| Auth / accounts | PWA scaffolding gated; native none | `AUTH_USER_ACCOUNT_LIFECYCLE_ARCHITECTURE_GATE` resolved + amended here |
| HealthKit — read-only body weight (HK-1) + workout history (HK-2; distance + avg/max heart rate HK-2b) | **UNGATED (HK-1 + HK-2 + HK-2b) — active.** `IronPathHealthKit` is an approved read-only Apple-Health adapter; latest body mass → `AppData.healthMetricSamples` (kg); recent workouts → `AppData.importedWorkoutSamples` (DERIVED, display-only, never engine input), each carrying type / start–end / duration / energy + (HK-2b) per-workout distance + avg/max heart rate read read-only over the workout window — all via the §8.2 gated path. | *Met by HK-1 + HK-2 + HK-2b: privacy + permission design shipped (§6.1/§8.2/§18); read-only, on-device.* |
| HealthKit — workout EXPORT / write-back (HK-3) | **UNGATED (HK-3) — active.** User-triggered, idempotent, native-only EXPORT of native completed sessions (`AppData.history`) to Apple Health as `HKWorkout`s (session-id metadata idempotency; export shares ONLY the workout type; device-local; never re-exports imports; never writes AppData). | *Met by HK-3: write-authorization + privacy design shipped (§6.2/§8.2/§16/§18); guarded by `iosHealthKitWorkoutExportStaticGuards`.* |
| HealthKit — standalone / other-type write-back | Deferred. Standalone metric sampling (a first-class `healthMetricSamples` type) + writing any OTHER Apple-Health type back (body weight / heart rate / …). | Approved task per slice; needs its own privacy review |
| Local notifications — rest-timer + training reminders | **UNGATED (N-1 + N-2) — active, LOCAL only.** `IronPathNotifications` schedules on-device reminders (user-authorized), confined to one `#if os(iOS)` adapter: N-1 a one-shot `UNTimeIntervalNotificationTrigger` rest reminder; N-2 a repeating weekly `UNCalendarNotificationTrigger` training reminder (iOS persists it). | *Met by N-1 + N-2: local-only + permission design shipped (§6.1/§16/§18); on-device, no server.* |
| Remote / push notifications (APNs) | Deferred — push needs a server. | Approved task; server + push infrastructure design |
| Home-screen widget (readiness) | **UNGATED (W-1) — active, read-only.** A WidgetKit `IronPathWidgetExtension` renders a DERIVED readiness snapshot the app writes to a shared App Group; the widget never writes canonical AppData and is never a source of truth. | *Met by W-1: derived-only App Group share + read-only widget design shipped (§5/§6.1/§12/§18); on-device, no network.* |
| `iOS-4B6` userFacing / full arbitrationTrace | Deferred | Approved task |
| Backup/export, shared UIKit | Stubs (`IronPathBackup`, `IronPathUIKit`) | Approved task |

> Existing `docs/*` and `src/cloudProduction/*` about auth/account/backend/cloud are **strategy/candidate/gated** work, not approvals. **Do not read them as "build it now."**

---

## 18. Forbidden Changes

A change is **forbidden** (stop and escalate) if it does any of the following without an approved architecture task that amends this document:

**Platform / persistence / network (native iOS especially):**
- Introduces **CloudKit, iCloud, Supabase, URLSession/networking, WebView, auth, UserDefaults, SQLite, CoreData, or SwiftData** into native iOS. (On-device durability stays local JSON via Foundation.) **HealthKit** is permitted ONLY as the HK-1 body-weight + HK-2 workout-history READ adapters **plus the HK-3 workout-EXPORT adapter** (§6.2/§8.2), confined to the single `#if os(iOS)` `HealthKitBodyMassSource` file (which hosts both read sources and the HK-3 export) and guarded by `iosBootstrapForbiddenImports` (+ `iosHealthKitWorkoutExportStaticGuards`); imported workouts are **derived/display-only** (`importedWorkoutSamples`, never canonical `history`, never engine input — guarded by `iosHealthKitWorkoutImportStaticGuards`), and may carry **workout-attached sub-fields read read-only as part of that same import — distance + avg/max heart rate (HK-2b)** — which stay derived/display-only `ImportedWorkoutSample` fields (never a standalone `HealthMetricSample`, never canonical, never engine input; heart rate adds only the `heartRate` quantity to the read set and reuses the already-exempted `HKQuantityType` token — no new banned symbol). **Write-back to Apple Health (HK-3), standalone metric sampling (heart rate or others as first-class `healthMetricSamples`), and any other HealthKit metric, remain forbidden** without a new approved, contract-amending slice.
- Introduces **remote / push notifications** (APNs, the `aps-environment` entitlement, `registerForRemoteNotifications`, a `UNNotificationServiceExtension`, or PushKit) — these need a server and stay gated (§17). **`UserNotifications` is permitted ONLY as the N-1 / N-2 LOCAL reminder adapters** (`IronPathNotifications`, §6.1/§16), confined to the single `#if os(iOS)` `UserNotificationsRestReminderScheduler` file — a local one-shot `UNTimeIntervalNotificationTrigger` (N-1 rest) plus a repeating weekly `UNCalendarNotificationTrigger` (N-2 training; iOS persists it, the app stores no schedule) — and guarded by `iosBootstrapForbiddenImports`.
- Lets the **home-screen widget write canonical AppData / become a source of truth**, or moves the **App Group** share off its derived/read-only role. The W-1 widget (`IronPathWidgetExtension`) is READ-ONLY: the app writes a derived readiness snapshot to the App Group (`group.com.ironpath.app.ios`, §12) and the widget only reads it (`IronPathWidgetShared`, §6.1). WidgetKit is confined to the widget target + the single `#if os(iOS)` reloader; the widget has **no network/cloud/remote push**. A native target beyond this one widget extension, or any non-local widget data source, needs a new approved, contract-amending slice.
- Adds real implementation to a **stub package** (`IronPathCloudSync`/`IronPathBackup`/`IronPathUIKit`). (`IronPathHealthKit` was **activated** by HK-1 into a read-only adapter — §6.1; its read-only boundary is itself guarded. `IronPathNotifications` is a **new** N-1 package — §6.1 — not one of these stubs. The other three stay inert.)
- Couples `IronPathLocalSnapshot` to `IronPathDomain`/AppData, or makes it the source of truth.
- Moves native iOS off **local-first**, or promotes PWA cloud-production from gated to default.

**Source of truth / data safety:**
- Changes the **source-of-truth boundary** (where canonical AppData lives / who can overwrite it).
- Implements **full AppData restore** without satisfying §14.
- Makes **draft restore** overwrite canonical AppData (it stays an in-memory draft, §13).
- Feeds **raw AppData** into `TrainingDecision` (must be a `CleanTrainingDecisionInput`, §10/§11).
- Drops unknown fields / breaks open-bag preservation; bumps `SchemaVersion` without migration + guard; changes timestamp encoding (§9).
- Removes "backup-before-overwrite" / "no fake success" from a store; ships the `userFacing`/full `arbitrationTrace` ahead of `iOS-4B6`.

**Engineering hygiene:**
- Puts business logic into the **thin app layer**.
- Creates a **dependency cycle** or makes `IronPathDomain` depend on another package; inverts the TS import direction.
- Hand-edits **generated parity goldens**.
- Modifies **`package.json` / lockfiles** without explicit justification.
- Modifies **`project.pbxproj`** unless absolutely necessary and justified.
- Uses **`git worktree`**, works **directly on `main`**, uses **`--admin`**, or **bypasses branch protection**.
- Commits raw (non-redacted) user-export fixtures, or leaks server tokens into `dist` (CI's dist scan).

---

## 19. Allowed Change Patterns

Green-light patterns (still subject to validation §21 and guards §22):

1. **Add a validated vertical slice** to native iOS by moving one PWA capability into packages + thin UI, with parity goldens + tests + guards.
2. **Extend an active package** (Domain/DataHealth/TrainingDecision/Persistence/LocalSnapshot) with new pure logic, fully tested, parity-pinned where it affects engine output.
3. **Improve the thin SwiftUI app layer** (rendering, wiring, accessibility) without adding logic.
4. **Add/strengthen static guards, regression locks, and tests.**
5. **Add documentation** (including amending *this* document when an architecture decision is approved).
6. **Refactor within a boundary** without changing public contracts or the import graph.
7. **PWA feature work** within the existing local-first, gated-cloud boundaries.

---

## 20. Migration Strategy

**Migrate by validated vertical slice — never by blindly copying PWA screens one-by-one.**

1. **Pick a thin slice** with a clear AppData input and a clear output (e.g. "readiness for today," "history search").
2. **Land the logic in a package** (`IronPathDomain` for data; a logic package for behavior). Keep it pure; cite the mirrored TS source line.
3. **Pin parity**: if it affects engine output, generate goldens from `packages/core` and assert in Swift.
4. **Gate the input** through DataHealth → `CleanTrainingDecisionInput` where the slice consumes AppData.
5. **Render in the thin app layer** — no logic in the view.
6. **Add guards**: static guard(s) + tests covering the new boundary. **Every migrated feature must have validation and guard coverage.**
7. **Keep it local-first.** If a slice *seems* to need cloud/auth/HealthKit/network, **stop** — that is a gated system (§17), not a slice. (Local JSON files via Foundation are allowed.)

Migration respects the existing iOS-N sequencing (§27). Do not skip ahead into deferred milestones.

---

## 21. Validation Standard

> No change merges unless the required checks pass **normally** (no `--admin`, no protection bypass).

### 21.1 Required (TypeScript — enforced by CI `.github/workflows/ironpath-ci.yml`, Node 22, `TZ=America/New_York`)
CI runs, in order:
```bash
npm ci
npm run api:dev:build   # build the dev API (SSR bundle)
npm run typecheck       # tsc --noEmit
npm test                # vitest run
npm run build           # vite build
# Dist token scan: fails if dist leaks node:http/node:sqlite/devApiRunner/serverAdapter/... tokens
```
All steps must be green. Also expected locally before PR:
```bash
git diff --check                 # no whitespace/conflict markers
# package.json + package-lock.json UNCHANGED unless a dependency change is justified
```

### 21.2 Required (Swift / iOS — local; **CI does NOT build/test Swift**)
There is **no `npm run validate:ios` script.** Validate Swift manually:
```bash
# Per package (all 12 under ios/packages/*):
cd ios/packages/<Package> && swift test

# App build (both destinations):
xcodebuild -workspace ios/IronPath.xcworkspace -scheme IronPath \
  -destination 'generic/platform=iOS' build
xcodebuild -workspace ios/IronPath.xcworkspace -scheme IronPath \
  -destination 'platform=iOS Simulator,name=iPhone 17 Pro' build
```

### 21.3 Scope-aware validation
- **Docs-only change:** run `npm run typecheck && npm test && npm run build` for confidence + `git diff --check`; Swift `swift test`/`xcodebuild` are not required (no Swift/TS source changed) — **state this explicitly in the PR.**
- **TypeScript change:** §21.1 mandatory; regenerate parity goldens if engine output changed.
- **Swift change:** §21.1 (CI) **and** §21.2 (local Swift) mandatory.
- **If a check is unavailable or too expensive, say exactly what was and was not run, and why.**

---

## 22. Static Guard Standard

Static guards / regression locks are **first-class** — they encode invariants as failing-on-violation tests.

**Guards that must stay green:**
- **iOS local-only boundary:** `iosLocalJsonPersistenceStaticGuards` (no iCloud/CloudKit/HealthKit/Supabase/URLSession/WebKit/UserDefaults/SQLite/CoreData/SwiftData in the app/persistence stores).
- **HealthKit boundary (HK-1/HK-2 read + HK-3 export):** `iosBootstrapForbiddenImports` confines `import HealthKit` / `HKHealthStore` / `HKQuantityType` / `HKWorkout` to the single `HealthKitBodyMassSource.swift` adapter and asserts the bounded write boundary (reads share nothing; the HK-3 export shares ONLY the workout type and writes ONLY `HKWorkout`s — no `HKQuantitySample`, no other type); the HK-3 export surface (native-only, idempotent, user-triggered) is locked by `iosHealthKitWorkoutExportStaticGuards`; every other ios Swift file stays HealthKit-free. `iosBootstrapPackageGraph` sanctions the `IronPathHealthKit → IronPathDomain` local-path dep.
- **AppData data safety:** `AppDataSchemaVersionGuardTests`, `AppDataIsoTimestampStaticGuardTests`, `AppDataOpenBagPreservationTests`, `AppDataUnitFieldPreservationTests`, `AppDataTypedFieldActivationTests`, `AppDataCodableRoundTripTests`, `AppDataRealExportParityTests`.
- **Engine contract:** `TrainingDecisionShapeStabilityTests`, golden/parity/clean-input tests; the iOS bootstrap parity test (`IronPathLinkedPackages.versions`).
- **TypeScript:** the `tests/` vitest suite (parity, boundary, regression-lock specs).
- **Build/dist:** CI's dist token scan; `scripts/scan-production-dist-safety.mjs`, `scripts/check-dist-size.mjs`, `scripts/predeploy-check.mjs`.
- **Parity goldens:** generated by `scripts/generate-parity-goldens.mjs` (+ `scripts/parityGoldensEntry.ts`). **Never hand-edit goldens — regenerate them.**

**Rule:** every new boundary introduced by a task **ships with a guard**. Removing or weakening a guard requires explicit justification in the PR and is reviewed as an architecture change.

---

## 23. Branch / PR Workflow

> The repository workflow is **non-negotiable**.

1. **Confirm** the repo path and a clean working tree before starting.
2. **Fetch** and base work on **latest `origin/main`**. Local `main` may diverge from `origin/main`; create the task branch directly from `origin/main`.
3. **Create a normal task branch** (e.g. `docs/...`, `ios-N-...`). **Never work directly on `main`.**
4. **Do NOT use `git worktree`.** Work in the main checkout.
5. Make the change; run the **validation standard** (§21).
6. **Commit only the intended files** (never `git add -A`/`.` blindly — untracked tooling files like `.claude/*` must not be committed).
7. **Push** the branch and **open a PR.**
8. **Wait for required checks.** Enable **auto-merge (squash)** only if required checks pass and **branch protection allows it normally.**
9. **Do NOT use `--admin`. Do NOT bypass branch protection.** If CI fails, **stop** and report.
10. After a successful squash merge, **delete the task branch** and **leave the repository clean.**

---

## 24. AI Development Rules

**Every AI agent (Claude / Codex / other) must:**
1. **Read this document before implementing any task.** (`AGENTS.md` points here.)
2. **Stop on conflict.** If a requested task conflicts with this document, **do not code** — require explicit architecture approval first, and amend this document as part of that approval.
3. **Never silently introduce** new persistence, network, cloud, auth, or platform dependencies, or any **source-of-truth change.** These are gated (§17) and forbidden by default (§18).
4. **Stay in scope.** Implement the smallest validated slice. Do not opportunistically expand.
5. **Keep the app layer thin** and **logic in packages**.
6. **Route AppData through DataHealth** before TrainingDecision; keep restore an in-memory draft.
7. **Validate and guard** every change (§21, §22). Report exactly what was and was not run.
8. **Follow the branch/PR workflow** (§23) verbatim — no worktree, no `--admin`, no protection bypass.
9. **Be honest about state.** If a check failed or was skipped, say so. Do not claim "done" without verification ("no fake success").
10. **Prefer escalation over silent assumption.** A clarifying question or a documented "blocked" beats an unapproved change.

---

## 25. Future Task Prompt Template

Every future Claude/Codex task must be framed with this template. Copy it, fill every field, and refuse to proceed if a field would violate this document.

```markdown
## Task: <short title>

**Baseline commit:** <git short SHA of latest origin/main, e.g. 64f37e7>
**Goal:** <one sentence — the user-visible or engineering outcome>
**Scope:** <the single vertical slice; what is explicitly in>

**Allowed files / systems:**
- <e.g. ios/packages/IronPathLocalSnapshot/**, ios/IronPath/*.swift (render only)>
- <e.g. tests/** , docs/**>

**Forbidden files / systems:**
- project.pbxproj (unless absolutely necessary + justified here)
- package.json / package-lock.json (unless a justified dependency change)
- Any stub package (CloudSync/Backup/UIKit) — and any HealthKit use beyond the HK-1/HK-2 read + HK-3 workout-export adapter (other-type write-back / other metrics)
- CloudKit/iCloud/Supabase/HealthKit/URLSession/WebView/auth/UserDefaults/SQLite/CoreData/SwiftData

**Source-of-truth impact:** <MUST be "none" unless this is an approved architecture task amending §8/§14>
**Data safety impact:** <open-bag preservation? schema version? restore? draft vs canonical? backup-before-overwrite? — state explicitly>

**Validation commands:**
- npm run typecheck && npm test && npm run build
- git diff --check
- (if Swift) swift test for affected ios/packages/*; xcodebuild generic + iPhone 17 Pro
- (if engine output changed) regenerate parity goldens

**PR & merge rules:**
- Branch from latest origin/main (no worktree); never commit to main
- Open PR; wait for required checks; auto-merge (squash) only if checks pass & protection allows normally
- No --admin; no branch-protection bypass

**Cleanup rules:**
- Commit only intended files; delete branch after squash merge; leave repo clean
```

---

## 26. Risk Register

| # | Risk | Likelihood | Impact | Mitigation (enforced here) |
| --- | --- | --- | --- | --- |
| R1 | AI scope creep introduces cloud/auth/persistence | High | High | §17 gates, §18 forbidden list, §24 rule 3, §25 template forces "forbidden systems" |
| R2 | Draft restore silently becomes full AppData restore | Medium | High (data loss) | §13 invariant, §14 gate, §18 |
| R3 | Raw AppData reaches TrainingDecision | Medium | High (bad decisions) | §10/§11; `CleanTrainingDecisionInput` is the only accepted type |
| R4 | Open-bag/unknown fields dropped on round-trip | Medium | High (data loss) | §9, `AppDataOpenBagPreservationTests` |
| R5 | Silent schema version bump | Low | High | §9, `AppDataSchemaVersionGuardTests` |
| R6 | PWA↔Swift parity drift | Medium | Medium | §21 goldens; never hand-edit (§22) |
| R7 | Business logic leaks into the thin app layer | Medium | Medium | §5/§15/§19, code review |
| R8 | Stub package quietly implemented | Medium | High | §6.2/§6.3, §18 |
| R9 | `LocalSnapshot` coupled to AppData / becomes source of truth | Medium | High | §6.3, §8, §12 |
| R10 | A store loses atomic-write / backup / no-fake-success | Low | High | §12, `iosLocalJsonPersistenceStaticGuards` |
| R11 | Process violation (worktree/`--admin`/main) | Low | High | §23, §24 rule 8 |
| R12 | CI green but iOS untested (CI excludes Swift) | Medium | Medium | §21.2 mandatory local Swift validation |
| R13 | This doc goes stale vs. code | Medium | Medium | Amend-in-same-change rule (§1.1); baseline commit recorded |

---

## 27. Appendix: Current iOS Migration Milestones

Native iOS has advanced as a sequence of validated slices. Completed **through iOS-17b**, the **iOS-17S** five-tab navigation shell, **iOS-17C** (#424, Plan+Today read-only surface), **iOS-17A** (the first native canonical-AppData write path for performed sets), **HK-1** (the first native capability ungating: a read-only Apple-Health body-weight import), **N-1** (a local-only rest-timer notification), **W-1** (a read-only home-screen readiness widget via a WidgetKit extension + App Group derived share), **N-2** (local-only weekly training reminders, within the N-1 local-notification boundary), **HK-2** (a read-only Apple-Health **workout-history** import, within the HK-1 read-only HealthKit boundary; derived/display-only), **HK-2b** (a read-only **field refinement** of that workout import: per-workout distance + avg/max heart rate, still derived/display-only, still within the HK-2 boundary), **HK-3** (the first Apple-Health **write-back**: a user-triggered, idempotent, native-only workout EXPORT of native completed sessions to Apple Health as `HKWorkout`s, confined to the single adapter), **HK-3b** (a behavior-preserving migration of that export to the non-deprecated `HKWorkoutBuilder`), the **Today / Profile / History / Plan** canonical **read-for-display** paths (#437–#440 — each reads the real on-device canonical AppData **read-only** through DataHealth and renders it; the Plan read superseded the old `PlanSurfaceSummary` presenter), and the **EDIT-1…EDIT-4** canonical **edit-write** paths (#441–#444 — profile scalars / display unit / screening lists / program config, all funnelled through the one sanctioned `performGatedMutation`). Baseline: latest `origin/main` — `0ecc39c` (EDIT-4 #444).

| Milestone | Summary |
| --- | --- |
| iOS-3A | Data Health runtime foundation: `AppDataStore` protocol + `JSONFileAppDataStore` (atomic JSON, backup). |
| iOS-4B1…4B5 | TrainingDecision Swift type skeleton → core rule skeleton → readiness + e1RM slice → deload/clamp/modes → exercise prescription + volume floor. |
| **iOS-4B6** | userFacing / full `arbitrationTrace`. **DEFERRED.** |
| iOS-9 | Local JSON persistence + saved-session history (`LocalSessionSnapshotStore`). |
| iOS-12 (#416) | Native local restore + history + testability bundle; extracted `IronPathLocalSnapshot`. |
| iOS-13 (#417) | Local history product surface + restore reconciliation (exercise-id matching) + grouping. |
| iOS-14 (#418) | Native history + draft recovery bundle: `LocalSnapshotHistory.filtered` search/filter, `LocalSnapshotStats.mostCommonScenarioLabel`, history search field + summary card, **non-destructive in-memory draft recovery**, real-clock opt-in (`useSystemClock`, default deterministic). |
| iOS-15 (#420) | Local history detail + per-exercise recovery insight: pure `LocalSnapshotRecovery.insight` (read-only projection over `LocalDraftRestorePlanner.reconcile` → per-exercise restorable/changed + new-exercise list + remapped resume), coarse history date-range filter (`LocalHistoryDateRange`), honest resume affordance. Restore stays an in-memory draft. |
| **iOS-16 (#421)** | Custom history date range: additive `LocalHistoryCustomDateRange` + `LocalSnapshotHistory.filtered(customRange:)` (inclusive UTC-day interval, reversed-normalized, composes with coarse/search/scenario/completed) + thin from/to `DatePicker`s. |
| iOS-17 epic (approved) | Per-exercise set logging. **17.0** review (Option C: AppData = source of truth via `IronPathPersistence`, optional denormalized snapshot copy). **17a** Domain typing — already satisfied by iOS-2C. **17b (#422)** in-RAM per-set capture (weight/reps/RIR) into the existing `ActualSetDraft`, kg-stored, **no persistence** → source-of-truth impact: none. **17c + 17d shipped in iOS-17A** (below). **17e** engine consumption — deferred. |
| **iOS-17S** | Tab shell scaffold: `ContentView` → five-tab `TabView` (今日/训练/记录/计划/我的 via an `AppTab` enum), Focus relocated under 训练 **unchanged**, four placeholder empty-state RootViews, all five `*RootView` files pre-registered into `project.pbxproj`. **The only slice authorized to edit `project.pbxproj`** — it unlocks parallel per-tab fills (each later slice edits only its own RootView). Navigation only → source-of-truth & data-safety impact: none. |
| **iOS-17C (#424)** | Plan + Today read-only surface: `PlanRootView`/`TodayRootView` render a pure `TrainingDecisionSurfacePresentation` projection of the engine slice. Read-only presentation; no persistence/source-of-truth impact. |
| **iOS-17A (#426)** | **Native per-set logging mega (17c + 17d).** Capture extended in `FocusSetChecklistView` (weight/reps/RIR, display-unit entry, kg-stored). **First native canonical-AppData write path:** `IronPathPersistence.CanonicalSessionWriter` appends a completed `TrainingSession` (performed sets in `history[].exercises[].sets`, built by `IronPathDomain.NativeCompletedSessionBuilder`) — DataHealth-gated, backup-before-overwrite, atomic, no-fake-success (§8.1). Detail summary "上次成绩" backed by a derived LocalSnapshot **v3** `setLogs` display copy (§12 rule 5). Amends §2/§8/§9/§12. Source-of-truth impact: **activates** (does not move) the canonical write boundary. Forbidden systems untouched; `project.pbxproj` not edited (no new app-target files). |
| **HK-1 (#428)** | **HealthKit read-only body-weight import — first capability ungating.** `IronPathHealthKit` activated into a read-only Apple-Health adapter: pure `BodyMassReading`/`BodyMassSampleSource` seam + `HealthKitBodyMassMapper` (→ `HealthMetricSample`, kg) + `HealthKitBodyMassImporter`, with the real `HKHealthStore` reader (`HealthKitBodyMassSource`) compiled `#if os(iOS)` only. User-gated import in the 我的 tab appends the latest body weight to `AppData.healthMetricSamples` (kg) via `CanonicalSessionWriter.appendHealthMetricSample` — the §8.2 DataHealth-gated, backup-before-overwrite, atomic, no-fake-success path (same path as §8.1; idempotent by content id). Adds `NSHealthShareUsageDescription` + a HealthKit entitlement + `CODE_SIGN_ENTITLEMENTS` (justified `project.pbxproj` edit — first capability ungating + 2 new app files). Static guards updated in sync (`iosBootstrapForbiddenImports` exempts the one adapter file + asserts read-only; `iosBootstrapPackageGraph` sanctions `HealthKit → Domain`). Amends §2/§6.1/§6.2/§6.3/§8.2/§10/§17/§18/§27. Source-of-truth impact: uses the §8.1 boundary, no new write path. Forbidden systems otherwise untouched; HealthKit write-back (HK-3) still forbidden. |
| **N-1 (#430)** | **Local rest-timer notification — local-only capability ungating.** New Foundation-only standalone package `IronPathNotifications`: pure `RestReminderPolicy` (exercise-role rawValue → recommended rest seconds; injected clock → fire instant) + `RestReminderScheduling` seam + the real `UNUserNotificationCenter` scheduler (`UserNotificationsRestReminderScheduler`, the ONLY UserNotifications importer, `#if os(iOS)`, a LOCAL `UNTimeIntervalNotificationTrigger`). Thin app `RestReminderModel` (honest status, never imports UserNotifications, in-RAM) + an in-session section; the Focus shell schedules a reminder when a set completes and cancels on exercise-switch / end / complete / reset. Registers an 11th package + 2 new app files in `project.pbxproj` (justified — new capability; local notifications need NO entitlement). Static guards in sync (`iosBootstrapForbiddenImports` confines the local UserNotifications tokens to the one adapter + bans remote-push everywhere; `iosBootstrapPackageGraph`/`iosBootstrapTargetSettings` add the package; new `iosLocalRestTimerNotificationStaticGuards`). Amends §2/§5/§6.1/§6.3/§16/§17/§18/§27. **LOCAL only — no network, no remote/push (still gated §17), no AppData / source-of-truth change.** |
| **W-1 (#431)** | **Readiness home-screen widget — read-only capability ungating (first extension target).** New Foundation-only standalone package `IronPathWidgetShared`: pure `ReadinessWidgetSnapshot` (+ JSON codec) + `ReadinessWidgetPresentation` (snapshot → view state, honest placeholder) + `WidgetSnapshotStore`/`WidgetReloading` seams; the real App Group `FileManager` store (`AppGroupWidgetSnapshotStore`) + the WidgetKit reloader (`WidgetTimelineReloader`, the ONLY package WidgetKit importer) are `#if os(iOS)`. The 今日 surface publishes a DERIVED readiness snapshot to a shared App Group (`group.com.ironpath.app.ios`) via the thin `WidgetSnapshotWriterModel` (never imports WidgetKit/FileManager, never writes canonical AppData). A new `IronPathWidgetExtension` WidgetKit target (`ios/IronPathWidget/`) READS that snapshot and renders it — read-only, never a source of truth (§12). Registers the 12th package + a 2nd native target (app-extension) + App Group entitlements on both + the embed/target-dependency in `project.pbxproj` (justified — a widget needs its own extension target). Static guards in sync (`iosBootstrapTargetSettings` now expects 2 native targets + the widget's extra package link; `iosBootstrapPackageGraph` adds the package; new `iosReadinessWidgetStaticGuards`). Amends §2/§5/§6.1/§6.3/§12/§17/§18/§27. **Read-only derived share — no network, no cloud, no remote, no canonical AppData / source-of-truth change.** |
| **N-2 (#432)** | **Local weekly training reminders — within the N-1 local-notification boundary (capability refinement, not expansion).** Adds to the existing `IronPathNotifications` package the pure `TrainingReminderPolicy` (+ `TrainingReminderRequest` / `TrainingReminderSchedule` / `PendingTrainingReminder`; selected weekdays + time → per-weekday repeating requests; injected `now`/`Calendar` → next-fire instant; reconstruct schedule from pending) + the `TrainingReminderScheduling` seam (reuses N-1's `RestReminderAuthorization`). The real `UserNotificationsTrainingReminderScheduler` (a REPEATING weekly `UNCalendarNotificationTrigger(repeats: true)`) is co-located in the SAME single `#if os(iOS)` `UserNotificationsRestReminderScheduler` file, so every real `UNUserNotificationCenter` call stays in one file. iOS persists the repeating notifications; the app stores no schedule (the live state is read back from the notification center). Thin user-gated card + view-model co-located in `ProfileRootView` (honest status, never imports UserNotifications, no FileManager/UserDefaults) — **no new app file, `project.pbxproj` untouched** (the package source is SPM-auto-included). Static guards in sync (`iosBootstrapForbiddenImports` confines the new `UNCalendarNotificationTrigger` to the one adapter; new `iosTrainingReminderNotificationStaticGuards`). Refines §6.1/§16/§17/§18/§27 (broadens the local-notification clauses from rest-timer-only to rest-timer + repeating training). **LOCAL only — no network, no remote/push (still gated §17), no AppData / source-of-truth change. No boundary expansion** — the local-notification capability was already ungated by N-1; pre-sanctioned by the ungating roadmap §4.2 + the prior §27 Next list. |
| **HK-2 (#433)** | **HealthKit read-only workout-history import — read scope refinement within the HK-1 boundary.** Adds to `IronPathHealthKit` the pure `WorkoutReading`/`WorkoutSampleSource` seam + `HealthKitWorkoutMapper` (→ `ImportedWorkoutSample`, `source: "healthkit_import"`, content-addressed id) + `HealthKitWorkoutImporter`, with the real `HKWorkout` reader (`HealthKitWorkoutSource`) co-located in the SAME single `#if os(iOS)` adapter file as HK-1. User-gated import in the 我的 tab appends recent workouts to `AppData.importedWorkoutSamples` via `CanonicalSessionWriter.appendImportedWorkoutSamples` — the §8.2 DataHealth-gated, backup-before-overwrite, atomic, no-fake-success path (idempotent by content id). **DERIVED / display-only: imported workouts land in `importedWorkoutSamples` (SEPARATE from `history`), are NEVER canonical `TrainingSession`s, and NEVER feed the engine** (guarded — no `IronPathTrainingDecision` source reads them). Reuses HK-1's HealthKit entitlement + usage string; **`project.pbxproj` untouched, no new app file** (model + section inlined in `ProfileRootView`, N-2 precedent). Static guards in sync (`iosBootstrapForbiddenImports` adds `HKWorkout`/`HKWorkoutActivityType` to the sole-adapter exemption + read-only assertions; new `iosHealthKitWorkoutImportStaticGuards`). Amends §2/§6.1/§6.2/§8.2/§10/§17/§18/§27. Source-of-truth impact: uses the §8.1 boundary, no new write path; imported workouts are explicitly non-canonical. HealthKit write-back (HK-3) still forbidden. |
| **HK-2b (#434)** | **HealthKit workout distance + avg/max heart rate — a read-only FIELD refinement within the HK-2 workout-history boundary (not a boundary expansion).** Extends the pure `WorkoutReading` seam (adds `avgHeartRateBpm`/`maxHeartRateBpm`; `distanceMeters` was already reserved) and `HealthKitWorkoutMapper` (carries distance + avg/max heart rate into the derived `ImportedWorkoutSample` — fields already present in the HK-2 shape + its `training-model.ts` mirror, so **no schema bump**, old records stay `nil`). The SAME single `#if os(iOS)` adapter (`HealthKitWorkoutSource`) reads distance from the workout's own `statistics(for:)` (activity-mapped distance type) and avg/max heart rate from a read-only discrete `HKStatisticsQuery` over the workout's start–end window; the `heartRate` quantity is added to the READ set (`toShare: []` unchanged). **DERIVED / display-only: distance + heart rate are `ImportedWorkoutSample` sub-fields — NEVER a standalone `HealthMetricSample`, NEVER canonical `history`, NEVER engine input** (the engine still references `importedWorkoutSamples` nowhere — guarded). Thin UI shows distance / heart rate when present (still “来自 Apple 健康”). Reuses HK-1's HealthKit entitlement + `NSHealthShareUsageDescription` (one usage string covers all reads); **`project.pbxproj` untouched, no new app file.** No new banned HealthKit symbol token (heart rate reuses the already-exempted `HKQuantityType`); static guard `iosHealthKitWorkoutImportStaticGuards` extended in sync (§22). Refines §17/§18/§27 (the §18 HealthKit clause now distinguishes workout-attached sub-fields from forbidden standalone metrics). Source-of-truth impact: none (derived, source-tagged, non-canonical, not engine input). |
| **HK-3 (#435)** | **HealthKit workout WRITE-BACK (export) — the first & only Apple-Health write.** Adds to `IronPathHealthKit` the pure `WorkoutExport` seam (`WorkoutExportRequest`/`WorkoutExportSummary`/`WorkoutExportSink`) + `HealthKitWorkoutExporter` (native `TrainingSession` → export request, NATIVE-ONLY); `HealthKitWorkoutSource` also conforms to `WorkoutExportSink` in the SAME single `#if os(iOS)` adapter, exporting native completed sessions (`AppData.history`) to Apple Health as `HKWorkout`s via `HKHealthStore.save`. User-triggered (我的 → 写回 Apple 健康), idempotent (session-id `HKWorkout` metadata, queried back — no app-side dedup, no schema bump), device-local. **Native-only / no-loop-back: only `[TrainingSession]` is mapped — imported (`healthkit_import`) workouts are never re-exported; the exporter references `ImportedWorkoutSample` nowhere.** Export shares ONLY the workout type (`toShare: [workoutType]`) and writes ONLY `HKWorkout`s — no other Apple-Health type, no `HKQuantitySample`. AppData stays source of truth (export READS `history`, never writes AppData). Adds `NSHealthUpdateUsageDescription`; reuses HK-1's entitlement; **no new app file, `project.pbxproj` code untouched** (model + section inlined in `ProfileRootView`). Static guards in sync (§22): the `iosBootstrapForbiddenImports` read-only boundary becomes write-bounded (still sole-holder; reads share nothing; export shares only the workout type; still no `HKQuantitySample`/`HKWorkoutBuilder`); the HK-2/HK-2b legacy no-write negatives relaxed (kept: no `HKQuantitySample`); new `iosHealthKitWorkoutExportStaticGuards` — net protection unchanged. Amends §2/§6.1/§6.2/§8.2/§16/§17/§18/§22/§27/§28. Source-of-truth impact: none (export is OUTSIDE the §8.1 path; AppData unchanged). HealthKit other-type write-back + standalone metrics still forbidden. |
| **HK-3b (#436)** | **HKWorkout export migrated to `HKWorkoutBuilder` — technical-debt cleanup within the HK-3 export boundary; behavior-preserving.** The single `#if os(iOS)` adapter now builds each exported workout via `HKWorkoutBuilder` (`beginCollection → addMetadata → endCollection → finishWorkout`) instead of the deprecated `HKWorkout(activityType:…)` initializer + `HKHealthStore.save` (silences the iOS 17 deprecation warning). **Equivalent replacement:** same activity type / start–end window / session-id metadata / idempotency query / `toShare: [workoutType]` scope / honest-failure semantics — the only observable change is the gone warning. Static guards updated in sync (§22): `iosBootstrapForbiddenImports` + `iosHealthKitWorkoutExportStaticGuards` now sanction `HKWorkoutBuilder` as the export mechanism in the one adapter (still sole-holder, still only `HKWorkout` written, still no `HKQuantitySample`). Pure mapper / `WorkoutExportSink` seam / Domain / app / `Info.plist` / entitlements / `project.pbxproj` untouched. **No new capability, no contract change, no schema bump; source-of-truth impact: none.** |
| **Today real-AppData read (#437)** | **First native canonical-AppData READ-for-display path.** `TodayRootView` reads the user's real on-device canonical AppData (the SAME `JSONFileAppDataStore.applicationSupport()` store the §8.1 write path uses) **read-only**, routes it through DataHealth `buildCleanAppDataView` (§10), and computes today's readiness via the existing `createCleanTrainingDecisionInput → buildTrainingDecisionFromCleanInput → TodayReadinessSummary` pipeline — replacing iOS-17C's fixed sample. Honest states (§15.4): no file / first launch / no cleaned history → empty; a present-but-unreadable document → degrade (left untouched, never overwritten). Pure branch resolver `resolveTodayReadinessState` added to `IronPathTrainingDecision` (additive §19.2 — no engine/golden change; the engine still never constructs the clean view, guarded); the thin `@MainActor TodayRealDataModel` is **inlined in `TodayRootView.swift`** — `project.pbxproj` untouched, no new app file (N-2/HK-2/HK-3 precedent). The 今日 widget snapshot now carries the real readiness when available. Amends §5/§27. **Source-of-truth impact: none (read-only, no new write path). Data-safety impact: none.** |
| **Profile real-AppData read (#438)** | **Native canonical-AppData READ-for-display for the 我的 surface** (the second read-for-display path, reusing the *Today real-AppData read* #437 shape). `ProfileRootView` reads the user's real on-device canonical AppData (the SAME `JSONFileAppDataStore.applicationSupport()` store) **read-only**, routes it through DataHealth `buildCleanAppDataView` (§10), and renders the real profile / units / screening / settings + the DERIVED latest Apple-Health body weight (latest `body_weight` `HealthMetricSample`, kg — selected directly, NOT via the unported `healthSummaryEngine`; §8.2) — replacing iOS-17B's fixed sample. Honest states (§15.4): no file / first launch / no profile content → empty; a present-but-unreadable document → degrade (left untouched, never overwritten). Pure read-model `ProfileDisplayData` added to `IronPathDomain` (derived latest body weight + `hasAnyContent`); pure branch resolver `resolveProfileDisplayState` added to `IronPathDataHealth` (Profile is not engine-related, so it lives with `CleanAppDataView`, not the engine package — import graph still a DAG; additive §19.2, no engine/golden change). Thin `@MainActor ProfileRealDataModel` **inlined in `ProfileRootView.swift`** — `project.pbxproj` untouched, no new app file (N-2/HK-2/HK-3/Today-read precedent); the HealthKit import/export + reminder sections are unchanged. Amends §5/§27. **Source-of-truth impact: none (read-only, no new write path; latest body weight is derived, never written into `userProfile.weightKg`). Data-safety impact: none.** |
| **History real-AppData read (#439)** | **Native canonical-AppData READ-for-display for the 记录 surface** (the third read-for-display path, reusing the *Today* #437 / *Profile* #438 shape). `HistoryRootView` reads the user's real on-device canonical AppData (the SAME `JSONFileAppDataStore.applicationSupport()` store) **read-only**, routes it through DataHealth `buildCleanAppDataView` (§10), and renders ONE unified, most-recent-first timeline of real completed training: native completed sessions from the CLEANED `cleanedHistory` (tagged 原生) + the DERIVED `importedWorkoutSamples` (tagged 来自 Apple 健康, display-only — never canonical, never engine input, §8.2) — replacing iOS-17's LocalSnapshot-only viewer. **No-loss merge:** a native session completed WITHOUT per-set detail is saved only to the local Focus snapshot store (never canonical `history` — the writer skips it), so the thin model ALSO reads that store read-only and feeds those as a NEUTRAL `SupplementalNativeCompletion`, **merged + deduped by id (canonical wins)** — every completion shown once, no duplicate rows; the dedup keeps `IronPathDomain`/`IronPathDataHealth` free of any `IronPathLocalSnapshot` import (decoupled, §12). Honest states (§15.4): no file / first launch / no completed training → empty; a present-but-unreadable document → degrade (left untouched, never overwritten). Pure read-model `CompletedTrainingTimeline` added to `IronPathDomain`; pure branch resolver `resolveHistoryDisplayState` added to `IronPathDataHealth` (additive §19.2, no engine/golden change). Thin `@MainActor HistoryRealDataModel` **inlined in `HistoryRootView.swift`** — `project.pbxproj` untouched, no new app file (Today/Profile-read precedent); the 训练 tab's LocalSnapshot draft-recovery / restore / per-session detail are unchanged. Amends §5/§27. **Source-of-truth impact: none (read-only, no new write path). Data-safety impact: none.** |
| **Plan real-AppData read (#440)** | **Native canonical-AppData READ-for-display for the 计划 surface** (the fourth read-for-display path, reusing the *Today* #437 / *Profile* #438 / *History* #439 shape). `PlanRootView` reads the user's real on-device canonical AppData (the SAME `JSONFileAppDataStore.applicationSupport()` store) **read-only**, routes it through DataHealth `buildCleanAppDataView` (§10), and renders the real mesocycle + program template via the pure read-model `IronPathDomain.PlanDisplay` + branch resolver `IronPathDataHealth.resolvePlanDisplayState` — replacing iOS-17C's fixed sample. Plan is **not** engine-related, so the projection lives in DataHealth/Domain (NOT the engine package), keeping the import graph a DAG with no `DataHealth → IronPathTrainingDecision` edge; the pre-existing `IronPathTrainingDecision.PlanSurfaceSummary` presenter was thereby **superseded** (now removed as dead code — see the §27 cleanup pass). Honest states (§15.4): no file / first launch / no plan content → empty; a present-but-unreadable document → degrade (left untouched, never overwritten). Thin `@MainActor PlanRealDataModel` **inlined in `PlanRootView.swift`** — `project.pbxproj` untouched, no new app file (Today/Profile/History-read precedent). Amends §5/§27. **Source-of-truth impact: none (read-only, no new write path). Data-safety impact: none.** |
| **EDIT-1 (#441)** | **First native canonical-AppData EDIT write path — profile scalar fields.** The user edits their `UserProfile` scalars (姓名/性别/年龄/身高/体重/训练水平/目标/每周天数/单次时长) in the 我的 tab, two-step 「编辑→保存」, written **in place** through the SAME sanctioned, DataHealth-gated write path as every append (§8.3): the append-only private orchestration is generalized in name to `performGatedMutation`, and a new `CanonicalSessionWriter.updateProfile` funnels through it (NOT a second write path). Pure open-bag candidate builders `AppData.withUpdatedProfile` + `UserProfile.withScalarFields` (rewrite only `userProfile`/the nine scalars; preserve everything else + unknown/open-bag + `schemaVersion` (no bump) + ISO timestamps). Defensive `buildCleanAppDataView` re-validation before commit; backup-before-overwrite + atomic + honest fail; never overwrite an unreadable document; user-explicit save; device-local. 体重 edits the self-entered `userProfile.weightKg` (distinct from HK `healthMetricSamples`). Thin UI inlined in `ProfileRootView.swift` — **no new app file, `project.pbxproj` untouched** (N-2/HK-2/read-path precedent). Guarded by `ProfileScalarEditTests` + EDIT-1 `CanonicalSessionWriterTests`; `iosHealthKitWorkoutImportStaticGuards` updated to the `performGatedMutation` name in sync (net protection unchanged). Amends §8 (§8.3)/§9/§27. **Source-of-truth impact: activates (does not move) the canonical write boundary; not a restore (§13/§14). Data-safety: open-bag/schema/timestamp preserved, defensive clean-view re-validation, backup+atomic+honest-fail.** |
| **EDIT-2 (#442)** | **Display-unit preference (kg/lb) persistence — within the EDIT-1 edit-write boundary (§8.3), not a boundary expansion.** The 我的 tab's 「显示单位」 toggle was in-memory-only; now a USER toggle persists the display preference to canonical `AppData.unitSettings.displayUnit` through the SAME sanctioned, DataHealth-gated write path as every append/edit: a sibling entry point `CanonicalSessionWriter.updateUnitSettings(displayUnit:)` funnels through the SAME `performGatedMutation` (NOT a second write path — still exactly one `store.save`). Pure open-bag candidate builders `AppData.withUpdatedUnitSettings` + `UnitSettings.withDisplayUnit` (rewrite only `unitSettings.displayUnit`, reading the freshly-loaded on-disk unit settings so the typed `weightUnit` + unknown unit keys survive; preserve everything else + `schemaVersion` (no bump) + ISO timestamps). **Storage stays kilograms** — only the DISPLAY preference is persisted; no stored weight is coerced. Defensive `buildCleanAppDataView` re-validation before commit; backup-before-overwrite + atomic + honest fail; never overwrite an unreadable document; user-explicit toggle (the one-time seed never writes); device-local. Thin UI inlined in `ProfileRootView.swift` — **no new app file, `project.pbxproj` untouched** (EDIT-1/N-2/HK-2 precedent); no static guard changed (the single-`store.save` invariant is unchanged). Guarded by `UnitSettingsEditTests` + EDIT-2 `CanonicalSessionWriterTests`. Amends §8.3/§9/§27. **Source-of-truth impact: reuses (does not move) the EDIT-1 boundary; not a restore (§13/§14). Data-safety: open-bag/schema/timestamp preserved, defensive clean-view re-validation, backup+atomic+honest-fail.** |
| **EDIT-3 (#443)** | **Screening profile (self-reported lists) editing — within the EDIT-1 edit-write boundary (§8.3), not a boundary expansion.** The 我的 tab's 筛查 section was read-only; now the user edits the three self-reported lists (疼痛触发/受限动作/纠正优先) two-step 「编辑→保存」, written **in place** through the SAME sanctioned, DataHealth-gated write path as every append/edit: a sibling entry point `CanonicalSessionWriter.updateScreening(_:)` funnels through the SAME `performGatedMutation` (NOT a second write path — still exactly one `store.save`). Pure open-bag candidate builders `AppData.withUpdatedScreening` + `ScreeningProfile.withEditedLists` (rewrite only the `screeningProfile` key's three lists; preserve everything else + the engine-managed `adaptiveState`/`postureFlags`/`movementFlags`/`userId` + unknown/open-bag + `schemaVersion` (no bump) + ISO timestamps). Defensive `buildCleanAppDataView` re-validation before commit (comparing the clean view's **raw** screening — `buildCleanScreening` legitimately re-caps/filters the engine-managed adaptive state); backup-before-overwrite + atomic + honest fail; never overwrite an unreadable document; user-explicit save; device-local. Thin UI inlined in `ProfileRootView.swift` — **no new app file, `project.pbxproj` untouched** (EDIT-1/EDIT-2/N-2/HK-2 precedent); no static guard changed (the single-`store.save` invariant is unchanged). Guarded by `ScreeningProfileEditTests` + EDIT-3 `CanonicalSessionWriterTests`. Amends §8.3/§9/§27. **Source-of-truth impact: reuses (does not move) the EDIT-1 boundary; not a restore (§13/§14). Data-safety: open-bag/schema/timestamp preserved, defensive clean-view re-validation, backup+atomic+honest-fail.** |
| **EDIT-4 (#444)** | **Program template scalar config editing — within the EDIT-1 edit-write boundary (§8.3), not a boundary expansion.** The 计划 (Plan) tab's 模板 Program card was read-only (#440); now the user edits the three user scalar config fields (主要目标/分项/每周天数) two-step 「编辑→保存」, written **in place** through the SAME sanctioned, DataHealth-gated write path as every append/edit: a sibling entry point `CanonicalSessionWriter.updateProgramConfig(primaryGoal:splitType:daysPerWeek:)` funnels through the SAME `performGatedMutation` (NOT a second write path — still exactly one `store.save`). Pure open-bag candidate builders `AppData.withUpdatedProgramConfig` + `ProgramTemplate.withConfigScalars` (rewrite only the `programTemplate` key's three scalars, reading the freshly-loaded on-disk program template; preserve everything else + the program `id`/`userId`/the engine-managed `correctionStrategy`/`functionalStrategy` blobs + the engine-managed structured `mesocyclePlan` (weeks/prescriptions) + unknown/open-bag + `schemaVersion` (no bump) + ISO timestamps). Defensive `buildCleanAppDataView` re-validation before commit (comparing the clean view's **raw** program — `buildCleanAppDataView` leaves `programTemplate` untouched); backup-before-overwrite + atomic + honest fail; never overwrite an unreadable document; user-explicit save; device-local. **Only the three user scalars are editable — the engine-managed structured plan (周期 weeks / prescriptions / 策略 blobs) is never edited (read-only).** Thin UI inlined in `PlanRootView.swift` — **no new app file, `project.pbxproj` untouched** (EDIT-1/EDIT-2/EDIT-3/N-2/HK-2 precedent); no static guard changed (the single-`store.save` invariant is unchanged). Guarded by `ProgramConfigEditTests` + EDIT-4 `CanonicalSessionWriterTests`. Amends §8.3/§9/§27. **Source-of-truth impact: reuses (does not move) the EDIT-1 boundary; not a restore (§13/§14). Data-safety: open-bag/schema/timestamp preserved, defensive clean-view re-validation, backup+atomic+honest-fail.** |
| **History search + source filter (#446)** | **Read-only search + source/date filter for the 记录 unified timeline.** The History real-AppData read (#439) dropped the iOS-14/15 LocalSnapshot search/filter when it replaced the LocalSnapshot-only viewer with the unified `CompletedTrainingTimeline`; this restores it FOR that timeline. New pure, order-PRESERVING `CompletedTrainingTimeline.filtered(query:source:dateRange:now:)` in `IronPathDomain`: case-insensitive text search over a row's `searchableText` (来源标签 + native 动作名 / imported workout type, newline-joined), a source segment (`CompletedTrainingSourceFilter` 全部/原生/来自 Apple 健康), and a coarse `CompletedTrainingDateRange` (全部/最近7天/最近30天) measured in UTC calendar days against an INJECTED `now` — a nil/unparseable timestamp is excluded from a BOUNDED range but never dropped from 全部, never crashes; all filters compose (AND), each defaults to a no-op. `NativeCompletedTraining`/`SupplementalNativeCompletion` gain a defaulted display-only `exerciseNames` (the 动作名 target; canonical names from `make`, snapshot-only names supplied read-only by the thin app). A FRESH Domain leaf — NOT a reuse of the LocalSnapshot enums (no `IronPathLocalSnapshot` import; packages stay decoupled, §12). `HistoryRootView` adds a `.searchable` field + source segment + date menu feeding the pure filter, with an honest "没有匹配的记录" state distinct from the no-data empty state; thin app wiring inlined — **no new app file, `project.pbxproj` untouched** (read-path precedent). Guarded by `CompletedTrainingTimelineFilterTests` (search/source/date/compose/empty/order). Allowed-change §19.2/§19.3/§19.5; amends §27. **Source-of-truth impact: none (read-only pure projection; no new write path, no engine/golden change). Data-safety impact: none.** |
| **DEEP-EDIT-1** | **Logged-set correction (重量/次数/RIR) — within the EDIT-1 edit-write boundary (§8.3), not a boundary expansion; the FIRST edit into a NESTED structure (`history[]→exercises[]→sets[]`).** The 记录 (History) saved-session detail was read-only; now the user corrects one logged set's 重量/次数/RIR two-step 「编辑→保存」, written **in place** into canonical `AppData.history[].exercises[].sets[]` through the SAME sanctioned, DataHealth-gated write path as every append/edit: a sibling entry point `CanonicalSessionWriter.updateHistorySet(sessionId:exerciseId:setIndex:weightKg:reps:rir:)` funnels through the SAME `performGatedMutation` (NOT a second write path — still exactly one `store.save`). Pure nested open-bag candidate builders `AppData.withUpdatedHistorySet` + `TrainingSetLog.withCorrectedMetrics` / `ExercisePrescription.withUpdatedSets` / `TrainingSession.withUpdatedExercises` (rewrite only the matched set's three metrics inside only `history`, re-emitting only the matched session; preserve every other session/exercise/set + each level's unknown/open-bag + the edited set's identity/`completedAt`/`done` + `schemaVersion` (no bump) + ISO timestamps). A logged set is an ENGINE INPUT — the engine recomputes e1RM/readiness FROM it (DEEP-EDIT review §1/§4); **no engine OUTPUT is touched (the `mesocyclePlan` weeks blob / prescriptions / computed phase/readiness/e1RM — §11 unchanged)**. Weight entered in display unit, stored as kg. Defensive `buildCleanAppDataView` re-validation before commit (the corrected set must survive the clean view AND land exactly as intended); backup-before-overwrite + atomic + honest fail; never overwrite an unreadable document; user-explicit save; device-local. Thin UI inlined in `FocusSavedSessionDetailView.swift` — **no new app file, `project.pbxproj` untouched** (EDIT-1…EDIT-4 precedent); no static guard changed (the single-`store.save` invariant is unchanged). Guarded by `HistorySetEditTests` + DEEP-EDIT-1 `CanonicalSessionWriterTests`. Amends §8.3/§9/§27 (§11 engine boundary unchanged). **Source-of-truth impact: reuses (does not move) the EDIT-1 boundary; not a restore (§13/§14). Data-safety: open-bag/schema/timestamp preserved at every nesting level, defensive clean-view re-validation, backup+atomic+honest-fail.** |
| **DEEP-EDIT-1 display (read canonical)** | **Read-only follow-up to DEEP-EDIT-1 — the 记录 saved-session detail's per-set 重量/次数/RIR now read CANONICAL-FIRST.** A corrected logged set lives in canonical `AppData.history[].exercises[].sets[]` (DEEP-EDIT-1), but the detail rendered the DERIVED LocalSnapshot `setLogs` copy — which a correction does NOT rewrite — so after a cold start the row showed the STALE pre-correction value. Now the detail reads the authoritative canonical metric for any set that reached `history` (matched by session id == snapshotId / exercise id / setIndex — the exact DEEP-EDIT-1 write identity), so a correction shows PERSISTENTLY (cold start too); a set with no canonical counterpart (snapshot-only / legacy, inherently uneditable) honestly falls back to the LocalSnapshot copy. Reuses the read-for-display shape (#437–#440): the thin `FocusModeMvpState.canonicalSetDisplay(for:)` reads canonical through the SAME read-only DataHealth ingress the edit gate uses (clean view via `buildCleanAppDataView`, §10 — read-only, no second cleaning entry point) and feeds the pure `IronPathDataHealth.resolveSavedSessionSetDisplay` (canonical-first match, NEUTRAL `SavedSessionSetFallback` translated by the app layer — NO `IronPathLocalSnapshot` import, packages stay decoupled §12, zero `: Date`). The detail's in-RAM value override is removed (canonical is the source); the two-step 「编辑→保存」 wiring is unchanged. Thin app wiring inlined — **no new app file, `project.pbxproj` untouched**; the no-bare-`AppData` / no-`buildCleanAppDataView(` presentation guards still hold. Guarded by `SavedSessionSetDisplayProjectionTests`. Allowed-change §19.2/§19.5; amends §27. **Source-of-truth impact: none (read-only; no new write path; LocalSnapshot stays append-only + decoupled; engine/golden untouched). Data-safety impact: none.** |
| **SR-0** | **Smart-replacement parity scaffold — parity PIPELINE only, NO engine logic (the engine port is SR-1+).** The TS `buildSmartReplacementRecommendations` (`src/engines/smartReplacementEngine.ts`) gains a parity slice: the generator (`scripts/parityGoldensEntry.ts` → `generateSmartReplacement`, run via `scripts/generate-parity-goldens.mjs`) produces **4 NEW** `smart-replacement/*` goldens under `tests/fixtures/parity/golden/` from synthetic `inputs/` fixtures (explicit-priority spread anchor + bare bench-press natural + low-readiness fatigue + pain-history), collectively covering all four `SmartReplacementPriority` values (primary/secondary/angle_variation/avoid). The `IronPathTrainingDecision` package gains a minimal **decode-only** Codable mirror (`SmartReplacementGolden`/`SmartReplacementRecommendation` + priority/fatigue enums) and a `SmartReplacementGoldenDecodeTests` that decodes the committed goldens and asserts count + priority-count integrity + 4-priority coverage + enum resolvability + unknown-key tolerance — **SPM-auto-included, `project.pbxproj` untouched, zero `: Date`.** Goldens are GENERATED, never hand-edited (§22); the 14 pre-existing parity goldens regenerate byte-identically (`generated 18 fixture(s); 4 changed`) — no existing golden / engine logic / write path / source-of-truth touched. The two parity contract guards (`parityFixturesContract`, `parityFixturesGenerationConsistency`) updated in sync for the new ids + count. Adds `docs/ios-native-migration/IOS_SR0_SMART_REPLACEMENT_PARITY_SCAFFOLD_V1.md`; amends §27. **Source-of-truth impact: none (no engine logic, no write path). Data-safety impact: none.** |
| **SR-1** | **Exercise-library data port — DATA + pure parse functions only, NO replacement-engine logic (SR-2/SR-3 deferred).** Faithfully ports the four frozen library tables from `src/data/exerciseLibrary.ts` — `EXERCISE_DISPLAY_NAMES` (94) / `EXERCISE_EQUIPMENT_TAGS` (61) / `EXERCISE_ENGLISH_NAMES` (63) / `EXERCISE_ALIASES` (59) — plus the `ExerciseEquipmentTag` enum, the `ExerciseName` struct, and the `EXERCISE_KNOWLEDGE_OVERRIDES` **key set** (63 ids, keys ONLY — the override VALUES are SR-2/SR-3 engine knowledge, NOT ported) into a new pure `IronPathTrainingDecision.ExerciseLibrary` namespace (`ExerciseLibrary.swift`), together with the pure parse functions `getExerciseNameEntry` / `formatExerciseDisplayName` / `normalizeExerciseReference` / `resolveExerciseReferenceToId` / `mapLegacyAlternativeLabelsToIds` (mirrored line-by-line; ordered `KeyValuePairs` preserve TS insertion order so resolve()'s first-match-wins stays faithful through the genuine `face-pull`/`face_pull`, `landmine-press`/`landmine_press` collisions). A new GENERATED `exercise-library/library-snapshot-v1` golden dumps the whole library id→{displayName,englishName,equipmentTags,aliases} + the override-id key set; `ExerciseLibraryParityTests` reconciles the Swift tables against it **item-by-item** (id universe + every field + array order + override set + counts) and unit-tests the parse functions against TS behaviour. **SPM-auto-included, `project.pbxproj` untouched, zero `: Date`, no IO.** Goldens GENERATED never hand-edited (§22); the 18 pre-existing parity goldens regenerate byte-identically (`generated 19 fixture(s); 1 changed`) — no existing golden / engine logic / write path / source-of-truth touched. The parity count guards (`parityFixturesContract`, `parityFixturesGenerationConsistency`, `iosBootstrapParityStillGreen`, + the `iosLocal*`/`iosNative*` `--check` guards) bumped 18→19 in sync. Adds `docs/ios-native-migration/IOS_SR1_EXERCISE_LIBRARY_PORT_V1.md`; amends §27. **Source-of-truth impact: none (no engine logic, no write path; §8/§11 unchanged). Data-safety impact: none.** |
| **SR-2** | **Replacement-engine port — the underlying replacement LOGIC, NOT the top-level smartReplacement (SR-3 deferred) and NOT the write path.** Faithfully ports the three replacement-engine functions from `src/engines/replacementEngine.ts` — `buildReplacementOptions` / `validateReplacementExerciseId` / `isSyntheticReplacementExerciseId` — and every private helper they need (baseExerciseId / buildCurrentIdentitySet / normalizeName / isSelfOrAlias / rankFromPriority / optionFromId / reasonForReplacement / appendReasonNotes / equipment* / `formatFatigueCost`·`formatReplacementCategory` from `src/i18n/formatters.ts`) into a new pure `IronPathTrainingDecision.ReplacementEngine` (`ReplacementEngine.swift`), consuming the SR-1 `ExerciseLibrary`. The "engine knowledge" it reads — `EXERCISE_EQUIVALENCE_CHAINS` (id + members, 60 keys) and the engine-used SUBSET of `EXERCISE_KNOWLEDGE_OVERRIDES` (fatigueCost / equivalenceChainId / alternativeIds / alternativePriorities / regressionIds / progressionIds, 63 ids) — is transcribed verbatim into `ReplacementEngineKnowledge.swift` (the override VALUES SR-1 deferred; the descriptive chain/override fields the engine never reads are intentionally not ported). Mirrored line-by-line incl. the literal-backslash `normalizeName` `/[（）()\\s-]/g` quirk and the stable rank-sort / equipment re-sort tie-breaks. **The session-mutating `applyExerciseReplacement` / `restoreOriginalExercise` (WRITE PATH) are NOT ported.** **5 NEW** `replacement-engine/*` goldens: 1 `knowledge-snapshot-v1` (chains + override subset, reconciled item-by-item) + 4 OUTPUT fixtures (bench-press-explicit / lat-pulldown-equipment / hack-squat-chain / validation-synthetic) covering the explicit-alternatives, chain-derivation, equipment-context, and validation/synthetic branches; `ReplacementEngineParityTests` re-runs the PORTED engine on each golden's echoed `engineInput` and asserts the `ReplacementOption[]` + validate/synthetic verdicts match exactly, and reconciles the knowledge tables item-by-item. **SPM-auto-included, `project.pbxproj` untouched, deps/lockfile byte-unchanged, zero `: Date`, no IO.** Goldens GENERATED never hand-edited (§22); the 19 pre-existing parity goldens regenerate byte-identically (`generated 24 fixture(s); 5 changed`) — no existing golden / engine logic / write path / source-of-truth touched (§8/§11 smartReplacement output unchanged). The parity count guards (`parityFixturesContract`, `parityFixturesGenerationConsistency`, `iosBootstrapParityStillGreen`, + the `iosLocal*`/`iosNative*` `--check` guards) bumped 19→24 in sync. Adds `docs/ios-native-migration/IOS_SR2_REPLACEMENT_ENGINE_PORT_V1.md`; extends §19.2 (engine-package pure logic + parity goldens); amends §27. **Source-of-truth impact: none (no write path; §8/§11 unchanged). Data-safety impact: none.** |
| **SR-3** | **Top-level smart-replacement engine port + parity FULFILLMENT — the recommendation LOGIC, consuming SR-1/SR-2, NOT the write path (SR-4 deferred).** Faithfully ports the top-level `buildSmartReplacementRecommendations` + every private helper from `src/engines/smartReplacementEngine.ts` () into a new pure `IronPathTrainingDecision.SmartReplacementEngine` (`SmartReplacementEngine.swift`), **consuming the already-ported lower layers and re-porting none of them**: SR-1 `ExerciseLibrary` (display names + `formatExerciseDisplayName`, which IS `formatExerciseName`), SR-2 `ReplacementEngine` (`buildReplacementOptions`/`validateReplacementExerciseId`/`isSyntheticReplacementExerciseId`), and SR-2 `ReplacementEngineKnowledge` (the `EXERCISE_EQUIVALENCE_CHAINS` id+members + the fatigueCost/equivalenceChainId/alternativeIds/alternativePriorities override subset). A new `SmartReplacementKnowledge.swift` ports **only the ADDITIONAL** `EXERCISE_KNOWLEDGE_OVERRIDES` fields the smart engine reads that SR-2 deliberately left out (`movementPattern` / `primaryMuscles` / `skillDemand` / `kind` / `contraindications`, 63 ids, exact TS source order). Mirrored line-by-line incl. the final-sort `localeCompare(other, 'zh-Hans-CN')` PINYIN tiebreak (reproduced via Foundation ICU `Locale("zh-Hans-CN")`), the JS-stable sorts (pre-sort-index tiebreak), and `Map` insertion-order semantics. **The session-mutating `applyExerciseReplacement` / `restoreOriginalExercise` (WRITE PATH) are NOT ported (SR-4 integration).** **PARITY FULFILLMENT:** the 4 SR-0 `smart-replacement/*` goldens are UPGRADED from decode-only to **COMPUTE-AND-ASSERT** — new `SmartReplacementEngineParityTests` decodes each fixture's committed `inputs/smart-replacement/*` `params` (the SAME input `generateSmartReplacement` feeds the TS engine), runs the PORTED Swift engine, and asserts the produced `SmartReplacementRecommendation[]` equals the golden `recommendations` **item-by-item + in order** (+ recount of currentExerciseId/priorityCounts); a same-module extension adds a producing initializer to the SR-0 `SmartReplacementRecommendation` (the SR-0 `SmartReplacementGolden.swift` stays byte-identical; its decode tests remain). **NO new fixtures, NO golden change** — the 4 smart-replacement goldens + all 24 parity goldens are byte-unchanged (zero `--diff-filter=M`), so **NO count-guard bump** (still 24); the existing parity guards stay green untouched. **SPM-auto-included, `project.pbxproj` untouched, deps/lockfile byte-unchanged, zero `: Date`, no IO.** **§11 smartReplacement engine output is now REPRODUCED by the Swift port (parity-pinned to the committed goldens), semantics unchanged.** Adds `docs/ios-native-migration/IOS_SR3_SMART_REPLACEMENT_ENGINE_PORT_V1.md`; Allowed-change §19.2 (extend an active package: engine-package pure logic + parity); amends §27. **Source-of-truth impact: none (no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **SR-4** | **Smart-replacement INTEGRATION — engine recommendations + 换动作 UI + gated write of the user's chosen actual exercise (the SR track's write slice, closing SR-0…SR-4).** Wires the ported (SR-3) `SmartReplacementEngine` into the app: (a) the 记录 saved-session detail produces read-only recommendations via `SmartReplacementEngine.buildSmartReplacementRecommendations`, fed a `SmartReplacementParams` DERIVED FROM the DataHealth clean view (current exercise + clean training-history pain signal; never raw AppData, §10/§11) — **the engine logic + all goldens are UNCHANGED (SR-4 only CONSUMES; zero golden drift)**; (b)+(c) a two-step 「换动作 → 保存」 (+ 「恢复原动作」) writes the user's chosen actual exercise through the SAME sanctioned, DataHealth-gated write path as every append/edit: a sibling entry point `CanonicalSessionWriter.updateExerciseReplacement(sessionId:exerciseId:replacementExerciseId:)` funnels through the SAME `performGatedMutation` (NOT a second write path — still exactly one `store.save`). Pure nested open-bag candidate builders `AppData.withUpdatedExerciseReplacement` + `ExercisePrescription.withReplacedIdentity` rewrite **only** the matched history exercise's three USER-OVERRIDE identity fields (`actualExerciseId`/`displayExerciseId`/`recordExerciseId`) inside **only** `history` — APPLY → the replacement, RESTORE (nil) → cleared — preserving every other session/exercise/set + each level's open bag + the engine-opened `originalExerciseId` + the prescription body + the performed `sets` + `schemaVersion` (no bump). It records an engine INPUT (the user's actual exercise) and **never touches an engine OUTPUT** (`originalExerciseId` / `mesocyclePlan` weeks / prescriptions / computed phase·readiness·e1RM — §11). (d) The detail UI shows the ACTUAL exercise name (library-resolved) with a 已换 marker + the original, so the performed sets read as attributed to it. Defensive read-only clean-view re-validation (override must land exactly + survive the clean view); backup→atomic→honest-fail; never overwrite an unreadable document; user-explicit two-step; device-local; not a restore (§13/§14). **Honest V1 "respects replacement" scope:** the smart-replacement engine's history pain-pattern attribution keys off `actualExerciseId` (honored), and the detail UI attributes sets to the actual exercise; the native e1RM trend is a GLOBAL pool (not per-exercise) so a replacement does NOT change it, and the unified 记录 timeline still renders the stored `name` — fuller downstream honor is deferred (§11 SR-4 note). New Domain `ExerciseReplacementEdit.swift`; thin UI inlined in `FocusSavedSessionDetailView.swift` (recommendations panel + swap action + actual-exercise display) — **no new app file, `project.pbxproj`/deps/lockfile byte-unchanged, zero `: Date`**; no static guard relaxed (the single-`store.save` invariant holds). Guarded by `ExerciseReplacementEditTests` (Domain open-bag/identity/restore/no-op) + the SR-4 cases in `CanonicalSessionWriterTests` (gated write / single save / preservation / restore). Adds `docs/ios-native-migration/IOS_SR4_SMART_REPLACEMENT_INTEGRATION_V1.md`; amends §8.3/§11/§27. **Source-of-truth impact: reuses (does not move) the EDIT-1 boundary; not a restore (§13/§14). Data-safety: open-bag/schema/timestamp preserved at every nesting level, defensive clean-view re-validation, backup+atomic+honest-fail.** |
| **FIX-1** | **Backup-filename same-second collision fix — a bounded robustness fix to the canonical store's `JSONFileAppDataStore.backup()`, NOT a write-boundary change.** SR-4 integration surfaced that the backup filename used a SECOND-level ISO-8601 stamp, so two sanctioned writes in the same second resolved to the same `…backup-<stamp>` name → `copyItem` failed → a false `.backupFailed` that failed the whole gated write (honest, no data loss, but affecting every write path). The stamp now carries **millisecond** precision (`.withFractionalSeconds`, ISO-8601 still valid) AND the destination is **uniquified** by an existence probe (a `-2/-3…` suffix on a same-instant clash), so rapid successive writes each get a DISTINCT backup. **`performGatedMutation` orchestration / the single `store.save` / backup-before-overwrite / atomic write / honest failure are all unchanged** — only the same-second FALSE failure is removed; a GENUINE backup failure (unwritable directory) still throws `.backupFailed`. No AppData / `schemaVersion` (no bump), no engine, no goldens, no `IronPathDomain`; `project.pbxproj` / deps / lockfile byte-unchanged; device-local; not a restore (§13/§14). Guarded by the new `JSONFileAppDataStoreTests` cases (same-second distinct-name success + genuine-failure honest throw). Allowed-change §19.4/§19.6; amends §8.1/§9/§27. **Source-of-truth impact: none (no write-path / boundary change). Data-safety impact: none (a false failure is removed; backup-before-overwrite + atomic + honest-failure all preserved).** |
| **iOS-17e-0** | **Engine-consumption progression parity scaffold — parity BASELINE only, NO engine logic (the progression-cluster port is 17e-1+).** Adds **3 NEW** history-bearing `training-decision/*` parity fixtures (`progressive-overload-v1` / `plateau-stall-v1` / `insufficient-history-v1`) whose `synthetic.sessions[].sets` are non-empty, so the REAL TS `trainingDecisionEngine` reads the performed-set history and emits its history-driven ADAPTIVE output: `progressive-overload` → `progressionMode='progress'` + `weeklyAdjustment.direction='increase'` (the CLOSED LOOP — rising logged sets recommend adding load — that the static native template, with its DEFERRED-inert `TrainingDecisionExercisePrescription` + GLOBAL e1RM boolean, cannot reproduce yet); `plateau-stall` (flat weight) / `insufficient-history` (< 4-session trend threshold) → `hold`. GENERATED via `scripts/generate-parity-goldens.mjs` → `generateTrainingDecisionExpanded` (reusing the iOS-4B0 expanded projection — **no new golden shape**), never hand-edited (§22); the 24 pre-existing parity goldens regenerate byte-identically (`generated 27 fixture(s); 3 changed`) — zero `--diff-filter=M`, the 3 new goldens additive (`--diff-filter=A`). The `IronPathTrainingDecision` package gains a **DECODE-ONLY** `TrainingDecisionProgressionGoldenDecodeTests` that decodes the 3 goldens into the parity `TrainingDecision` shape and asserts the adaptive fields + per-fixture progression values + the adaptive contrast + shape stability + forward-compat — it does **NOT** compute-assert (the static engine cannot match an adaptive golden), and the new ids are kept OUT of `TrainingDecisionGoldens.allIds`/`.expandedIds` so every existing compute/decode parity test stays pinned to the 10 cold-start goldens (all green). **COMPUTE-ASSERT flips slice-by-slice as 17e-1…17e-5 port the cluster (per-exercise e1RM → performance query → progression suggestion → fine-tune/load → wire to surface).** The parity count guards (`parityFixturesContract`, `parityFixturesGenerationConsistency`, `iosBootstrapParityStillGreen`, the `iosLocal*`/`iosNative*` `--check` guards + the per-slice `training-decision` dir-count guards) bumped 24→27 / 10→13 in sync. **SPM-auto-included, `project.pbxproj`/deps/lockfile byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter`/source-of-truth touch.** Adds `docs/ios-native-migration/IOS_17E_ENGINE_CONSUMPTION_PLAN_V1.md`; Allowed-change §19.2 (engine-package parity goldens + tests). **Source-of-truth impact: none (no engine logic, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **iOS-17e-1** | **Per-exercise e1RM engine port + function-level parity — the FIRST progression-cluster engine slice, PURE logic only, NOT wired into the decision output (that is 17e-5).** Faithfully ports the per-exercise estimated-one-rep-max functions from `src/engines/e1rmEngine.ts` — `getExerciseRecordPoolId` (by-exercise record pool id) / `estimateOneRepMaxForExercise` / `buildE1RMProfile` / `getE1RMConfidence` — and every private helper they need (roundToHalfKg·JS-Math.round / parseRir / epley / isWorkSet / matchesExercise / isCurrentQualityCandidate / median / nearestCandidate / buildEstimate / collectCandidates) plus the cross-module dependencies they read: `engineUtils` number/isCompletedSet/setWeightKg/completedSets, `sessionHistoryEngine` filterAnalyticsHistory/isAnalyticsSession, `sessionBackfillToleranceEngine` checkSessionBackfill (its OWN date-string math only — `zero : Date`, no clock), and `replacementEngine.hasInvalidExerciseIdentity` (reuses the SR-2 `ReplacementEngine.isSyntheticReplacementExerciseId` / `.validateReplacementExerciseId`), into a new pure `IronPathTrainingDecision.E1RMEngine` (`E1RMEngine.swift`). Consumes `[TrainingSession]` history (a §11 clean input); PURE — no IO, no randomness, no decision-output wiring. **5 NEW** `e1rm-engine/*` goldens (`progressive-overload` median_recent current + high best / `plateau-stall` flat / `insufficient-history` single_recent forced-low / `low-quality-filtered` low-confidence branches + `filterAnalyticsHistory` exclusion of a test-flagged + a back-filled session / `pool-confidence-probes` all `getExerciseRecordPoolId` + `getE1RMConfidence` branches) GENERATED via `scripts/generate-parity-goldens.mjs` → `generateE1RMEngine`, never hand-edited (§22); `E1RMEngineParityTests` re-runs the PORTED functions on each golden's echoed engineInput (history + probe inputs) and **COMPUTE-ASSERTS** the produced `E1RMProfile` / estimate / pool-id / confidence equal the golden item-by-item. The 27 pre-existing parity goldens regenerate byte-identically (`generated 32 fixture(s); 5 changed`) — zero `--diff-filter=M`, the 5 new goldens additive (`--diff-filter=A`); **the 17e-0 progression decision goldens + the cold-start goldens are byte-unchanged (no decision-output / existing-golden touch — the new functions are NOT wired in)**. The parity count guards (`parityFixturesContract`, `parityFixturesGenerationConsistency`, `iosBootstrapParityStillGreen`, the `iosLocal*`/`iosNative*` `--check` guards) bumped 27→32 in sync. **SPM-auto-included, `project.pbxproj`/deps/lockfile byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter`/source-of-truth touch; the 17e-0 decode-only scaffold + `TrainingDecisionE1RMTrend` global-trend boolean are untouched.** Adds `docs/ios-native-migration/IOS_17E1_PER_EXERCISE_E1RM_PORT_V1.md`; extends §19.2 (engine-package pure logic + parity goldens); amends §27. **Source-of-truth impact: none (no engine-output change, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **iOS-17e-2** | **adaptiveFeedbackEngine performance-lookup port + function-level parity — the SECOND progression-cluster engine slice, PURE logic only, NOT wired into the decision output (that is 17e-5).** Faithfully ports the performance-lookup functions from `src/engines/adaptiveFeedbackEngine.ts` — `findLastPerformance` / `findPreviousPerformance` / `findRecentPerformances` / `buildAdaptiveState` — plus the private helpers they read (`performanceValue` / `hasPainFlag`·painRegex / `issuesForExercise` via `ISSUE_FROM_PATTERN`) and the `PerformanceSnapshot` type, into a new pure `IronPathTrainingDecision.AdaptiveFeedbackEngine` (`AdaptiveFeedbackEngine.swift`). **Reuses (does NOT re-port) the 17e-1 `E1RMEngine.completedSets`/`number`/`setWeightKg`** so `completedSets` semantics stay identical across engines; `enrichExercise` is deliberately NOT ported (every site wrapping an exercise before `issuesForExercise` is a no-op — `issuesForExercise` reads only `baseId||id`). Consumes `[TrainingSession]` history (a §11 clean input) + the caller's `screening.adaptiveState.issueScores` seed (the ONLY screening field `buildAdaptiveState` reads); PURE — no IO, no randomness, no decision-output wiring. **Zero `: Date`:** the TS `buildAdaptiveState` `lastUpdated: new Date()…` wall-clock stamp is INJECTED via a `today` parameter (the generator substitutes `parityMeta.deterministicClockIso`, mirroring `generateDataRepair` omitting the clock-derived `createdAt`). **4 NEW** `adaptive-feedback/*` goldens (`performance-drop` drop branch + baseline moduleDose / `pain-accumulation` both `hasPainFlag` branches·painFlag+Chinese note + multi-rule duplicate issue bump + boost/baseline / `improving-and-seed` improve branch + seeded issueScores merge + boost/taper / `lookup-edge` findLast/findPrevious-skip/findRecent-limit edge probes + no-op buildAdaptiveState) GENERATED via `scripts/generate-parity-goldens.mjs` → `generateAdaptiveFeedback`, never hand-edited (§22); `AdaptiveFeedbackEngineParityTests` re-runs the PORTED functions on each golden's echoed engineInput (history + issueScores seed) and **COMPUTE-ASSERTS** the produced snapshots + `buildAdaptiveState` (issueScores / painByExercise / performanceDrops / improvingIssues / moduleDose / lastUpdated) equal the golden item-by-item. The 32 pre-existing parity goldens regenerate byte-identically (`generated 36 fixture(s); 4 changed`) — zero `--diff-filter=M`, the 4 new goldens additive (`--diff-filter=A`); **the 17e-0 progression decision goldens + the 17e-1 e1rm goldens + the cold-start goldens are byte-unchanged (no decision-output / existing-golden touch — the new functions are NOT wired in)**. The parity count guards (`parityFixturesContract`, `parityFixturesGenerationConsistency`, `iosBootstrapParityStillGreen`, the `iosLocal*`/`iosNative*` `--check` guards) bumped 32→36 in sync. **SPM-auto-included, `project.pbxproj`/deps/lockfile byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter`/source-of-truth touch; the 17e-0 decode-only scaffold + `TrainingDecisionE1RMTrend` global-trend boolean are untouched.** Extends §19.2 (engine-package pure logic + parity goldens); amends §27 (§11 engine boundary unchanged). **Source-of-truth impact: none (no engine-output change, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **iOS-17e-3** | **progressionRulesEngine progressive-suggestion port + function-level parity — the THIRD progression-cluster engine slice, PURE logic only, NOT wired into the decision output (that is 17e-5).** Faithfully ports the progressive-suggestion functions from `src/engines/progressionRulesEngine.ts` — `makeSuggestion` / `shouldUseTopBackoff` / `buildSetPrescription` — and every private helper they read (`averageRir` / `qualityRank` / `averageTechniqueQuality` / `hitRepCeiling` / `firstSetBelowFloor` / `rirAllowsProgress` / `roundToUnit` / `roundLoad` / `progressionIncrement` / `summarizeRir` / `summarizeTechnique` / `recentPoorTechniqueCount` / `applyFineTuneIfDataRich`) plus the `Suggestion` / `ExerciseForProgression` types, into a new pure `IronPathTrainingDecision.ProgressionRulesEngine` (`ProgressionRulesEngine.swift`). **Reuses (does NOT re-port) the 17e-2 `AdaptiveFeedbackEngine.findRecentPerformances`/`findPreviousPerformance`+`PerformanceSnapshot` and the 17e-1 `E1RMEngine.number`/`setWeightKg`** so history-lookup + set-math semantics stay identical across engines. Consumes `templateExercise` + `[TrainingSession]` history (a §11 clean input); PURE — no IO, no randomness, no decision-output wiring. **Zero `: Date`:** the ONE clock leak — `makeSuggestion`'s call to `buildSetWeightFineTune` (setWeightFineTuneEngine.ts, which reads the wall clock because it is invoked WITHOUT an asOfDate) — is **DEFERRED golden-neutral** (setWeightFineTuneEngine is NOT ported here); every 17e-3 fixture anchors its history outside the live 8-week window so the TS `fineTune` returns the `insufficient_history` fallback and `applyFineTuneIfDataRich` is a no-op, making the engine output exactly the legacy decision-tree baseline (mirrors the `TrainingDecisionExercisePrescription` fineTune-trust-override deferral; the live e1RM-trend projection ports in a later slice). **6 NEW** `progression-suggestion/*` goldens (`no-history-baseline` first-session baseline + conservative + isolation rangeNote + non-topBackoff conservative prescription + shouldUseTopBackoff/buildSetPrescription probes / `increase-double-top` hit-ceiling-twice → +increment / `hold-stable` default hold / `backoff-volume-drop` volume-regression backoff / `backoff-technique-streak` poor-technique-streak backoff + regression+replacement hints + 技术较差 summary / `top-backoff-compound` conservative high-fatigue top+backoff prescription) GENERATED via `scripts/generate-parity-goldens.mjs` → `generateProgression`, never hand-edited (§22); `ProgressionRulesEngineParityTests` re-runs the PORTED functions on each golden's echoed engineInput (templateExercise + history) and **COMPUTE-ASSERTS** the produced `Suggestion` (weight/reps/lastSummary/targetSummary/note) + `shouldUseTopBackoff` + `SetPrescription` (top/backoff weight+reps+summary) equal the golden item-by-item, plus a `fineTuneNeutrality` assertion (every fixture's TS fineTune fallbackReason == `insufficient_history`) pinning the deferral premise. The 36 pre-existing parity goldens regenerate byte-identically (`checked 42 fixture(s); 0 changed`) — zero `--diff-filter=M`, the 6 new goldens additive (`--diff-filter=A`); **the 17e-0 progression decision goldens + the 17e-1 e1rm goldens + the 17e-2 adaptive-feedback goldens + the cold-start goldens are byte-unchanged (no decision-output / existing-golden touch — the new functions are NOT wired in)**. The parity count guards (`parityFixturesContract`, `parityFixturesGenerationConsistency`) bumped 36→42 in sync. **SPM-auto-included, `project.pbxproj`/deps/lockfile byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter`/source-of-truth touch; the 17e-0 decode-only scaffold + `TrainingDecisionE1RMTrend` global-trend boolean are untouched.** Adds `docs/ios-native-migration/IOS_17E3_PROGRESSION_SUGGESTION_PORT_V1.md`; extends §19.2 (engine-package pure logic + parity goldens); amends §27 (§11 engine boundary unchanged). **Source-of-truth impact: none (no engine-output change, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **iOS-17e-4** | **setWeightFineTuneEngine + loadFeedbackEngine port + function-level parity — the FOURTH progression-cluster engine slice, PURE logic only, NOT wired into the decision output (that is 17e-5).** Faithfully ports `buildSetWeightFineTune` from `src/engines/setWeightFineTuneEngine.ts` (+ private helpers `epley` / `roundToPlate` / `parseRir` / `matchesExercise` / `isWorkSet` / `linearSlope` / `dateToWeekIndex` + the `SetWeightFineTuneInput`/`Result` types) into a new pure `IronPathTrainingDecision.SetWeightFineTuneEngine` (`SetWeightFineTuneEngine.swift`), and `collectLoadFeedback` / `upsertLoadFeedback` / `buildLoadFeedbackSummary` / `getLoadFeedbackAdjustment` from `src/engines/loadFeedbackEngine.ts` (+ `sortRecent`/`normalizePoolId` + the `LoadFeedback`/`LoadFeedbackValue`/`LoadFeedbackAdjustment`/`LoadFeedbackSummary` types) into a new pure `IronPathTrainingDecision.LoadFeedbackEngine` (`LoadFeedbackEngine.swift`). **Reuses (does NOT re-port) the 17e-1 `E1RMEngine.getExerciseRecordPoolId`/`epley`/`number`/`parseRir`** (the fine-tune engine's own `parseRir` is byte-identical, shared verbatim). Consume `[TrainingSession]` history (a §11 clean input); `loadFeedback`/`dataFlag` ride in the Domain `_unknown` open bag, and `upsertLoadFeedback` returns a NEW `TrainingSession` value (immutable transform — NO write path / `CanonicalSessionWriter`). PURE — no IO, no randomness, no decision-output wiring. **Zero `: Date`:** the TS `buildSetWeightFineTune` wall-clock fallback (`input.asOfDate ? new Date(input.asOfDate) : new Date()`) is the ONE impure branch — the pure port REQUIRES an explicit `asOfDate` (the same `nowIso` §11 clean input threads through; a `nil`/unparseable date returns the `insufficient_history` fallback, the wall-clock branch is intentionally not reproduced, mirroring the MesocycleWeekResolver/EffectiveTrainingPhase explicit-reference-date precedent); every parity fixture passes an explicit `asOfDate` so the goldens are byte-deterministic. **7 NEW** goldens: 4 `set-weight-fine-tune/*` (`upward-trend` uncapped + non-noisy projection / `downward-capped` lower-clamp / `noisy-trend` `noisy_trend` flag + median-trim + upper-clamp + rir>4/reps-out-of-range/out-of-window filters / `insufficient-history` < MIN_SAMPLES sparse branch + 0-sample + targetReps-clamp + baseExerciseId-membership + windowWeeks-override probes) + 3 `load-feedback/*` (`collect-summary` collect ordering/dataFlag+exerciseId filter/slice + summary counts/dominant-tie/total + adjustment / `adjustment-branches` conservative + slightly_aggressive + normal-default / `upsert` insert / insert-with-note / replace-keep-others-append) GENERATED via `scripts/generate-parity-goldens.mjs` → `generateSetWeightFineTune`/`generateLoadFeedback`, never hand-edited (§22); `SetWeightFineTuneEngineParityTests` + `LoadFeedbackEngineParityTests` re-run the PORTED functions on each golden's echoed engineInput and **COMPUTE-ASSERT** the produced `SetWeightFineTuneResult` (suggestedWeightKg + basis, EXACT Double `==` incl. non-round e1RM doubles) / collect+summary+adjustment + upsert array equal the golden item-by-item. The 42 pre-existing parity goldens regenerate byte-identically (`checked 49 fixture(s); 0 changed`) — zero `--diff-filter=M`, the 7 new goldens additive (`--diff-filter=A`); **the 17e-0…17e-3 progression/e1rm/adaptive/progression-suggestion goldens + the cold-start goldens are byte-unchanged (no decision-output / existing-golden touch — the new functions are NOT wired in; that is 17e-5)**. The parity count guards (`parityFixturesContract`, `parityFixturesGenerationConsistency`) bumped 42→49 in sync. **SPM-auto-included, `project.pbxproj`/deps/lockfile byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter`/source-of-truth touch; the 17e-0 decode-only scaffold + `TrainingDecisionE1RMTrend` global-trend boolean are untouched.** Extends §19.2 (engine-package pure logic + parity goldens); amends §27 (§11 engine boundary unchanged). **Source-of-truth impact: none (no engine-output change, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **iOS-17e-5** | **Wire the progression cluster into the decision output + flip the history goldens to compute-assert — the iOS-17e CLOSURE slice (closes 17e-0…17e-5).** The core slice (`buildTrainingDecisionFromCleanInput`) now folds the history-driven e1RM trend (`TrainingDecisionE1RMTrend.isE1rmTrendUp`, derived from the cleaned performed sets) — via the SAME branch order as `trainingDecisionEngine.ts:1985-1999, 2079-2087` — into the top-level **`progressionMode`** (already wired) and the **NEW `weeklyAdjustment`** projection (`TrainingDecisionModes.buildWeeklyAdjustment` → direction / magnitudePct / blockedBy / appliesFromIsoDate; added to `TrainingDecisionCoreSlice` + `WeeklyAdjustment` gains a compute-side memberwise init). **Closes the loop** the 17e-0 history goldens pinned: the 3 history goldens (`progressive-overload` / `plateau-stall` / `insufficient-history`) flip from **decode-only → compute-assert** (`TrainingDecisionProgressionGoldenParityTests`: rebuilds each fixture's engine input from synthetic rising/flat/too-short weighted history, runs the real engine, asserts `progressionMode` + `weeklyAdjustment` + `perExercise` + the full decision frame == golden, field-by-field). The 3 ids live in a SEPARATE compute-assert roster (`TrainingDecisionGoldens.progressionIds`) — NOT folded into the cold-start `allIds`/`expandedIds` (those drive the per-id-input cold-start suites). `TrainingDecisionCoreSliceParityTests` additionally compute-asserts `weeklyAdjustment` on all 9 cold-start fixtures. **Zero-drift invariants held: ALL goldens regenerate byte-identically (`git status` on `tests/fixtures/parity/golden` empty — the 10 cold-start AND the 3 history goldens unchanged; `--diff-filter=M` zero), proving cold-start output is unaffected (no history → no e1RM trend → no weekly increase) and the history goldens were already correct (ideal: byte-unchanged, only the test mode flips).** Reuses 17e-1~4 verbatim (no re-port, no semantic change). UI: `TrainingDecisionSummaryView` adds a read-only `weeklyAdjustment` row (mirror of engine output; §15). **SPM-auto-included, `project.pbxproj`/deps/lockfile byte-unchanged, zero `: Date`, no IO; pure engine — NO write path / `CanonicalSessionWriter`/source-of-truth touch; e1RM trend stays GLOBAL (SR-4 caveat unchanged).** Amends §11 (closed-loop adaptive-output note) + §27. **Source-of-truth impact: none (engine now CONSUMES recorded sets read-only to adapt output; output stays parity-pinned + reproduces TS; §8/§11 write semantics unchanged). Data-safety impact: none.** |
| **iOS-17e-6a** | **Make the ported fineTune projection LIVE + pin the `asOfDate` injected-clock contract — re-opens iOS-17e to retire the 17e-3 golden-neutral stub.** `ProgressionRulesEngine.makeSuggestion` (Swift + the TS `src/engines/progressionRulesEngine.ts` it ports) drops the hard-coded `insufficient_history` fineTune stub and calls the already-ported `SetWeightFineTuneEngine.buildSetWeightFineTune` (17e-4) **for real**, threading an injected `asOfDate` (the §11.2 clock). TS `makeSuggestion` gains an **additive optional `asOfDate`** (production callers omit it → wall clock, behaviour unchanged); the Swift `makeSuggestion(_:_:asOfDate:)` defaults to `nil`, which reproduces the old stub exactly (nil → `insufficient_history` → legacy baseline), so the 6 existing `progression-suggestion/*` goldens stay **byte-identical**. ⚠️ **`asOfDate` CONTRACT** pinned in both source headers (`ProgressionRulesEngine` / `SetWeightFineTuneEngine`): `nil` SILENTLY degrades to the legacy path — a LIVE wiring MUST pass `nowIso` (silent regression, not a crash). **2 NEW** `progression-suggestion/fine-tune-{uptrend-applied,legacy-respect}-v1` goldens pass `asOfDate = deterministicClockIso` over recent in-window history so the live `applyFineTuneIfDataRich` body (**±10% clamp / 2.5-round / legacy-respect**) is actually exercised; `ProgressionRulesEngineParityTests` gains a fineTune-LIVE group that compute-asserts the data-rich suggestion + buildSetPrescription, asserts `fineTuneNeutrality` is data-rich (samplesUsed 9, not insufficient), and PROVES the injected clock is load-bearing (`uptrend-applied` = 62.5 live vs 65 nil/legacy). GENERATED via `scripts/generate-parity-goldens.mjs` → `generateProgression` (now threads `input.asOfDate`), never hand-edited (§22); the 49 pre-existing goldens regenerate byte-identically (`checked 51 fixture(s); 0 changed` — zero `--diff-filter=M`, the 2 new additive `--diff-filter=A`). Count guards bumped 49→51 in sync (`parityFixturesContract` own-list + `parityFixturesGenerationConsistency` + `iosBootstrapParityStillGreen` + the iosLocal*/iosNative* `--check` guards). **FIX-3 correction — the 17e-6a "6b" record was architecturally wrong.** It claimed 6b would "wire the per-exercise next-weight/next-reps suggestion into the TrainingDecision per-exercise FRAME" by expanding the sparse `CleanTrainingDecisionInput.templateExercises`. That is a **pseudo-requirement, now CLOSED**: `makeSuggestion` is **NOT** part of TrainingDecision (**0 callers** in `trainingDecisionEngine.ts`) — its only consumers are the **un-ported** `sessionBuilder`/`recommendationDiff` chain (`recommendationDiffEngine.ts:74` / `sessionBuilder.ts:76`). The iOS `TrainingDecision` per-exercise prescription is supplied by `TrainingDecisionExercisePrescription.buildWorkingSetTargets` (the `exercisePrescriptions → workingSetTargets` chain, `trainingDecisionEngine.ts:1966-1983`, ported in iOS-4B5), **never** by `makeSuggestion`. `makeSuggestion`/`fineTune`/`loadFeedback` (17e-3/4/6a) are thus **parity-pinned libraries with no iOS production consumer — exactly as in TS, not a defect**. A `sessionBuilder`-style "suggest next weight from history" surface, if ever wanted, is a **separate track that ports the `sessionBuilder`/`recommendationDiff` chain**, NOT a `CleanTrainingDecisionInput` expansion. **SPM-auto-included, `project.pbxproj`/deps/lockfile byte-unchanged, zero `: Date`, no IO; pure engine — no write path / `CanonicalSessionWriter`/source-of-truth touch; 17e-1~4 reused without semantic change (TS `makeSuggestion` only gains the additive optional arg).** Amends §11 (fineTune-live + `asOfDate` contract note) + §27. **Source-of-truth impact: none (§8/§11 write semantics unchanged). Data-safety impact: none.** |
| **AN-1** | **Leaf-analytics engines port + function-level parity — the FIRST analytics/insights-track slice (AN-1…6 start), PURE read-only logic only, NOT wired into any UI (that is AN-6).** Faithfully ports the three leaf analytics engines from `src/engines/` — `computeTrainingStreak` (`trainingStreakEngine.ts`) / `computeRecentPRDeltas` (`recentPRDeltaEngine.ts`) / `computeWeeklyMuscleBalance` (`weeklyMuscleBalanceEngine.ts`) — and every private helper they read (computeRunLength / prevWeekKey / prevMonthKey; bestSet / collectObservations / pickBest / exerciseKey; the muscleContribution-vs-primary allocation) into new pure `IronPathTrainingDecision.TrainingStreakEngine` / `RecentPRDeltaEngine` / `WeeklyMuscleBalanceEngine`, with the byte-identical shared helpers (`safeDate` NOON-normalize / `isAnalyticsSession` completed≠false / UTC `startOfWeekUtc`·`weekKey`·`monthKey` integer civil-calendar math / `getPrimaryMuscles` / `setVolume`) centralised ONCE in `AnalyticsEngineSupport`. **Reuses (does NOT re-port) the already-ported `E1RMEngine.number` / `setWeightKg` / `isCompletedSet` / `completedSets`**; the analytics `safeDate` / `isAnalyticsSession` are DELIBERATELY distinct from `E1RMEngine`'s (noon-vs-midnight normalize; + `completed≠false`), so they are faithfully ported fresh — not a re-port of an existing helper. Consume `[TrainingSession]` history (a §11 clean input) + injected options; PURE — no IO, no randomness, no decision-output wiring. **Zero `: Date`:** "今天" is the injected `options.nowIso` (the TS `\|\| new Date()` / `?? Date.now()` wall-clock fallbacks are intentionally NOT reproduced; all date math is integer civil-calendar arithmetic, never `Date()` / `Calendar`). **3 NEW** goldens (one per metric, each a `cases` array: training-streak no-history / active-week / carry-last-week / broken-streak / filtering+months · recent-pr-delta up-down-flat / new+skip / sort-new-first+limit / window-boundary+pickBest-tie · weekly-muscle-balance no-data / balanced / imbalanced-over+under / muscleContribution+secondary+filtering) GENERATED via `scripts/generate-parity-goldens.mjs` → `generateTrainingStreak` / `generateRecentPRDelta` / `generateWeeklyMuscleBalance`, never hand-edited (§22); `TrainingStreakEngineParityTests` / `RecentPRDeltaEngineParityTests` / `WeeklyMuscleBalanceEngineParityTests` re-run the PORTED functions on each case's echoed engineInput (history + options) and **COMPUTE-ASSERT** the produced result == golden item-by-item (streak counts / ordered PR-delta list / muscle-balance entries, EXACT Double `==`). The 51 pre-existing parity goldens regenerate byte-identically (`generated 54 fixture(s); 3 changed`) — zero `--diff-filter=M`, the 3 new goldens additive (`--diff-filter=A`); existing decision / SR / e1rm / 17e goldens byte-unchanged. The parity count guards (`parityFixturesContract` own-list + `parityFixturesGenerationConsistency` + `iosBootstrapParityStillGreen` + the `iosLocal*`/`iosNative*` `--check` guards) bumped 51→54 in sync. **SPM-auto-included, `project.pbxproj`/deps/lockfile byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter`/source-of-truth touch; existing engines/goldens untouched.** Adds `docs/ios-native-migration/IOS_AN1_LEAF_ANALYTICS_METRICS_PORT_V1.md`; extends §19.2 (engine-package pure logic + parity goldens); amends §27. **Source-of-truth impact: none (no engine-output change, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **AN-1b** | **`roundToFixed` JS-`toFixed` fidelity fix + AN-1 boundary fixtures — a correctness + coverage follow-up to AN-1; PURE / read-only, no UI, no write path, no engine-OUTPUT change.** AN-1 implemented `AnalyticsSupport.roundToFixed` as `(value * p).rounded() / p` — a multiply-then-round that DIVERGES from JS `Number(value.toFixed(digits))` on `.XX5` ties + IEEE-754-representation cases (the `value * p` multiply rounds across a `.5` boundary the true value sits below: `2.675`→`2.68` not `2.67`, `0.15`→`0.2` not `0.1`, `1.555`→`1.56` not `1.55`, `-2.675`→`-2.68` not `-2.67`); the three analytics engines round real data (`deltaKg` / `deltaPercent`=`(cur-prev)/prev*100` / `effectiveSets` / `share`=`eff/total*100`) that easily lands on `.XX5`, and AN-1's fixtures merely AVOIDED ties so the bug was latent. Now `roundToFixed` faithfully reproduces ECMAScript `Number.prototype.toFixed` (ES2023 §21.1.3.3) with **pure decimal-string semantics, never `.rounded()`**: take the magnitude's EXACT decimal expansion (`String(format: "%.340f", …)` — a finite double terminates; 340 ≫ the ≤~110 frac digits any analytics magnitude needs), round that string half-away-from-zero at `digits` places (carry-propagated, ties→larger `n` on the magnitude per the spec), then `Double(_:)`-parse back (= JS `Number(string)`, both `strtod`-correctly-rounded) → **bit-identical to `Number(value.toFixed(digits))` for every finite double**. A new `AnalyticsRoundToFixedCrossValidationTests` pins `roundToFixed` against JS ground truth (`Number(x.toFixed(d))` from Node) over 69 rows (`.XX5` ties / IEEE-754 cases / negatives / digit widths 0…3 / realistic deltaPercent·share·effectiveSets intervals), EXACT `==` — catching any regression to a multiply-then-round approximation. **8 NEW boundary cases in 3 NEW fixture files** (`{training-streak,recent-pr-delta,weekly-muscle-balance}/*-boundary-cases-v1`, one per metric, run through the SAME three generators) pin the audit's "green-but-empty" coverage debt — SEPARATE files (NOT edits of the AN-1 fixtures) because the `--diff-filter=MD` parity guards sanction pure ADDITIONS but flag any modify of an existing committed golden (§22). Each case makes its branch be truly executed by a golden (several carry a divergent rounding value the OLD `roundToFixed` would mis-compute, so they double as fidelity pins): training-streak `finished-at-precedence-sunday-week` (the `finishedAt ?? startedAt ?? date` precedence + full-ISO→noon `safeDate` branch + non-Monday `weekStartDayOfWeek=0`) + `cross-year-month-carry` (`prevMonthKey` year-underflow `2026-01`→`2025-12`) · recent-pr-delta `both-new-and-equal-delta-tie` (NaN-tie + equal-`deltaKg` 0-tie → JS-stable order) + `pickbest-full-equality-tie` (equal weight AND reps → first-seen wins) + `delta-rounding-half-tie` (`deltaKg=5.55`, old → `5.56`) · weekly-muscle-balance `non-focus-insertion-and-half-tie` (non-focus 小腿/前臂 via `effectiveSets>0` + Map insertion-order tie + `effectiveSets=2.67`, old → `2.68`) + `single-focus-count-lt2` (`focusEntries.count<2` gate) + `threshold-12-boundary` (shares 62/38 → deviations EXACTLY ±12 → inclusive overworked/underworked edges). GENERATED via `scripts/generate-parity-goldens.mjs` (registered in `FIXTURE_IDS` + `GENERATORS`), never hand-edited (§22); the 3 Swift `*ParityTests` gain a `…ForBoundaryCases` compute-assert. **Zero golden drift:** the fidelity fix is Swift-only (goldens come from TS native `toFixed`, already correct), so the 54 pre-existing goldens ALL regenerate byte-identically and the 3 boundary goldens are pure ADDITIONS (`generated 57 fixture(s); 3 changed`; `git diff --diff-filter=MD origin/main -- tests/fixtures/parity` empty); the new tie cases FAIL the old Swift `roundToFixed` and PASS the fixed one. The parity fixture-count guards (`parityFixturesContract` + `parityFixturesGenerationConsistency` + `iosBootstrapParityStillGreen` + the ten `iosLocal*`/`iosNative*` `--check` guards) bumped **54 → 57** in sync. **SPM-auto-included, `project.pbxproj`/deps/lockfile byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter`/source-of-truth touch; already-ported helpers reused (no re-port).** Adds `docs/ios-native-migration/IOS_AN1B_ROUNDTOFIXED_FIDELITY_V1.md`; extends §19.2; amends §27. **Source-of-truth impact: none (no engine-output change, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **AN-2** | **plateauDetectionEngine port + function-level parity — the analytics/insights-track per-exercise plateau slice (AN-1…6); PURE read-only logic only, NOT wired into any UI (that is AN-6).** Faithfully ports `detectExercisePlateau` (`src/engines/plateauDetectionEngine.ts:299`) + EVERY private helper it reads (`signal` / `unique` / `exerciseIds` / `exerciseMatches` / `sessionDateKey` / `relevantSessions` / `relevantExerciseSets` / `firstExerciseName` / `buildTechniqueSummaryFromSets` / `isLoadFeedbackSummary` / `normalizeLoadFeedback` / `e1rmValues` / `buildSessionPerformances` / `isStableCompletion` / `countFlatSessions` / `isE1rmFlat` / `buildLimitActions` / `statusTitle` / `confidenceFromEvidence` / `summaryByStatus`) + the output/input types (`PlateauStatus` / `PlateauSignal` / `PlateauDetectionResult` / `DetectExercisePlateauParams`) into a new pure `IronPathTrainingDecision.PlateauDetectionEngine`. **Reuses (does NOT re-port) the already-ported `E1RMEngine.number` / `setWeightKg` / `completedSets` / `filterAnalyticsHistory`** (the engine's only real cross-module call) + `LoadFeedbackEngine.LoadFeedbackValue`. The `e1rmProfile` / `loadFeedback` external inputs are genuine TS runtime UNIONS consumed by duck-typing (`'recentValues' in profile` / `'counts' in value && 'adjustment' in value` / `Array.isArray` / `typeof === 'string'`) — kept as raw `JSONValue?` and structurally re-discriminated, never forced into a static type. **TYPE-ONLY (consumed subset, no engine logic) ports** of `TechniqueQualitySummary` (trainingLevelEngine.ts:41) / `Partial<EffectiveVolumeSummary>` (training-model.ts:1258) / `PainPattern` (training-model.ts:1028) — mirroring the `SmartReplacementEngine.SmartReplacementPainPattern` precedent; **NO `trainingLevelEngine` / `effectiveSetEngine` logic is pulled in (that is AN-3 / AN-5).** Consumes `[TrainingSession]` history (a §11 clean input) + an exerciseId + the optional external summaries; PURE — no IO, no randomness, no decision-output/UI wiring. **Zero `: Date`:** the engine never reads the wall clock; every date comparison is over the session's OWN date strings (`localeCompare`-descending reproduced with `>` over ASCII ISO keys, the same precedent as `E1RMEngine.collectCandidates`). **2 NEW** goldens (each a `cases` array): `plateau-detection/plateau-status-cases` (one case per `PlateauStatus`: none / possible_plateau / plateau / fatigue_limited / technique_limited / volume_limited / load_too_aggressive / insufficient_data — full status-arbitration precedence) + `plateau-detection/plateau-boundary-cases` (sets<5 vs sessions<3 insufficient · count<4 low-confidence none · flatE1rm-via-recentValues + e1rmCount≥5 high confidence · current/best & bare e1rmKg e1rm shapes · painPatterns-param fatigue · load-feedback-good signal · techniqueQualitySummary-param override · `_unknown` canonicalExerciseId match · loadFeedback array / summary-object / record-of-values shapes) GENERATED via `scripts/generate-parity-goldens.mjs` → `generatePlateauDetection`, never hand-edited (§22); `PlateauDetectionEngineParityTests` re-runs the PORTED `detectExercisePlateau` on each case's echoed engineInput (history + optional summaries) and **COMPUTE-ASSERTS** the full `PlateauDetectionResult` (status / title / summary / confidence + ordered signals + suggestedActions) == golden. The 57 pre-existing parity goldens regenerate byte-identically (`generated 59 fixture(s); 2 changed`) — zero `--diff-filter=M`, the 2 new goldens additive (`--diff-filter=A`); existing decision / SR / e1rm / 17e / AN-1 goldens byte-unchanged. The parity count guards (`parityFixturesContract` own-list + `parityFixturesGenerationConsistency` + `iosBootstrapParityStillGreen` + the `iosLocal*`/`iosNative*` `--check` guards) bumped 57→59 in sync. **SPM-auto-included, `project.pbxproj`/deps/lockfile/`Package.swift` byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter`/source-of-truth touch; already-ported helpers reused (no re-port); only the consumed TYPE subset of trainingLevel/effectiveSet ported.** Adds `docs/ios-native-migration/IOS_AN2_PLATEAU_DETECTION_PORT_V1.md`; extends §19.2 (engine-package pure logic + parity goldens); amends §27. **Source-of-truth impact: none (no engine-output change, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **AN-3** | **effectiveSetEngine (analytics-consumed subset) + analytics.ts dashboard port + function-level parity — the analytics/insights-track effective-set + dashboard slice (AN-1…6); PURE read-only logic only, NOT wired into any UI (that is AN-6/AN-7).** Faithfully ports the effective-set functions analytics CALLs — `evaluateEffectiveSet` / `countEffectiveSets` / `getMuscleContribution` / `buildEffectiveVolumeSummary` (`effectiveSetEngine.ts:7/104/116/141`) + the `clampScore` / `emptyMuscleSummary` helpers + the `EffectiveSetResult` / `EffectiveVolumeSummary` types — and ALL seven analytics.ts dashboard exports — `buildMuscleVolumeDashboard` / `buildExerciseTrend` / `trendStatus` / `buildPrs` / `buildWeeklyReport` / `buildAdherenceReport` / `CORE_TREND_EXERCISES` (`analytics.ts:112/152/178/188/293/406/84`) + every private helper they read (`ratio` / `analyticsHistory` / `incrementReason` / `mostCommonReason` / `setCountForExercise` / `supportPlannedFromBlock` / `recordQualityForSet` / `combineRecordQuality` / `completedHighQualitySets` / `roundOne` / `getVolumeStatus` / `buildVolumeNotes`) — into new pure `IronPathTrainingDecision.EffectiveSetEngine` / `AnalyticsDashboardEngine`. **Reuses (does NOT re-port) the already-ported `E1RMEngine.number` / `isCompletedSet` / `completedSets` / `hasInvalidExerciseIdentity` / `getExerciseRecordPoolId` / `buildE1RMProfile` / `isAnalyticsSession` (= the `analyticsHistory` dataFlag predicate) / `safeDateMs` (widened private→internal so the injected-clock weekly window reuses the exact `new Date(value)` parse), `AnalyticsSupport.getPrimaryMuscles` / `setVolume` / `jsMathRound`, `ExerciseLibrary.formatExerciseDisplayName` (= `formatExerciseName`)**; the small `engineUtils` helpers no prior engine needed (`getSecondaryMuscles` / `sessionVolume` / `sessionCompletedSets` / `exerciseVolume`) are ported IN PLACE. OUT OF SCOPE (not CALLed by the analytics track / UI-DOM / non-injectable clock): `makeCsv` / `downloadText` / `buildMonthStats` / `buildRecentSessionBars` + the `effectiveSetExplanationEngine` re-exports. **Zero `: Date`:** the only wall clock — `buildWeeklyReport`'s `new Date()` 7-day window — is made deterministic by a minimal BACKWARD-COMPATIBLE TS seam (`buildWeeklyReport(history, bodyWeights, options?: { nowIso?: string })`, the AN-1 `options.nowIso \|\| new Date()` / iOS-17e-6a injected-asOfDate precedent; the live UI path — the 2-arg call — is byte-unchanged and still wall-clock); the parity generator injects the deterministic clock, and the Swift port reproduces the window as exact UTC ms arithmetic (`now − 7·86_400_000`) over `E1RMEngine.safeDateMs`, never `Date()` / `Calendar`. **7 NEW** goldens (each a `cases` array): `effective-set/{evaluate-cases,volume-summary-cases}` (evaluateEffectiveSet every flag/score/confidence branch · buildEffectiveVolumeSummary byMuscle/confidence-buckets/corrective+functional skip/dataFlag+dateRange filter/rounding/invalid-identity reason + countEffectiveSets + getMuscleContribution probes) + `analytics/{muscle-volume-dashboard,exercise-trend,prs,weekly-report,adherence-report}-cases` (status four bands + target≤0 + weekStart vs slice(0,7) + sort · topSet/slice(0,6)/low-quality filter + trendStatus 4 states + CORE_TREND_EXERCISES · four PR metrics + quality + sort+slice(0,8) · injected-clock 7-day window + focus + latestWeight · rates/suggestions/confidence/skipped + supportPlannedFromBlock) GENERATED via `scripts/generate-parity-goldens.mjs`, never hand-edited (§22); `EffectiveSetEngineParityTests` / `AnalyticsDashboardEngineParityTests` re-run the PORTED functions on each case's echoed engineInput and **COMPUTE-ASSERT** the result == golden item-by-item (EffectiveSetResult / EffectiveVolumeSummary byMuscle+reasons / dashboard rows / trend+status / PrItems / weekly-report string / AdherenceReport, EXACT Double `==`). The 59 pre-existing parity goldens regenerate byte-identically (`generated 66 fixture(s); 7 changed`) — zero `--diff-filter=M`, the 7 new goldens additive (`--diff-filter=A`); existing decision / SR / e1rm / 17e / AN-1 / AN-2 goldens byte-unchanged. The parity count guards (`parityFixturesContract` own-list + `parityFixturesGenerationConsistency` + `iosBootstrapParityStillGreen` + the `iosLocal*` / `iosNative*` `--check` guards) bumped 59→66 in sync. **SPM-auto-included, `project.pbxproj` / deps / lockfile / `Package.swift` byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter` / source-of-truth touch; already-ported helpers reused (no re-port); only the analytics-CALLed effectiveSet subset ported.** Adds `docs/ios-native-migration/IOS_AN3_EFFECTIVE_SET_ANALYTICS_PORT_V1.md`; extends §19.2 (engine-package pure logic + parity goldens); amends §27. **Source-of-truth impact: none (no engine-output change; the `buildWeeklyReport` `options.nowIso` seam is an OPTIONAL injected-clock param, production path byte-unchanged, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **AN-4** | **sessionDetailSummaryEngine (sessionQuality-consumed subset) + sessionQualityEngine port + function-level parity — the analytics/insights-track session-quality slice (AN-1…6); PURE read-only logic only, NOT wired into any UI (that is AN-7).** Faithfully ports `buildSessionQualityResult` (`sessionQualityEngine.ts:122`) + every private helper it reads (`clamp` / `roundScore` / `isRecordedCompletedSet` / `hasRecordedRir` / `levelLabel` / `dataFlagLabel` / `isLoadFeedbackSummary` / `normalizeLoadFeedback` / `makeSignal` / `unique`) + the output/input types (`SessionQualityLevel` / `SessionQualitySignal` / `SessionQualityResult` / `LoadFeedbackInput` / `BuildSessionQualityParams`) into a new pure `IronPathTrainingDecision.SessionQualityEngine`; AND the TWO `sessionDetailSummaryEngine.ts` functions it CALLs — `groupSessionSetsByType` (`:154`) / `buildWorkingOnlySession` (`:218`) + their private helpers (`setTypeText` / `exerciseIdentity` / `parseWarmupExerciseId` / `classifySet`) + the `WORKING_SET_TYPES` / `SUPPORT_SET_TYPES` constants + the grouping types (`SessionSetCategory` / `SessionSetEntry` / `SessionExerciseSetGroup` / `GroupedSessionSets`) — into a new pure `IronPathTrainingDecision.SessionDetailSummaryEngine`. **Reuses (does NOT re-port) the already-ported `EffectiveSetEngine.buildEffectiveVolumeSummary` (AN-3) / `E1RMEngine.{completedSets,isCompletedSet,number,setWeightKg,hasInvalidExerciseIdentity}` / `AnalyticsSupport.jsMathRound`**; the only newly-needed `engineUtils` helper `isIncompleteSet` (`engineUtils.ts:95`) is ported IN PLACE beside its sibling `E1RMEngine.isCompletedSet`. The `loadFeedback` (`LoadFeedback[] \| LoadFeedbackSummary \| LoadFeedbackSummary[] \| Record<string,…> \| null`) + `effectiveSetSummary` (`Partial<EffectiveVolumeSummary> \| null`) params are genuine TS runtime UNIONS consumed by duck-typing (`Array.isArray` / `'feedback' in item` / `'counts' in v && 'adjustment' in v` / `Object.values`; JS `\|\|` truthy override) — kept as raw `JSONValue` and structurally re-discriminated, never forced into a static type (the AN-2 `normalizeLoadFeedback` precedent). OUT OF SCOPE (NOT CALLed by sessionQuality): `buildSessionDetailSummary` + `getSessionWarmupSets` / `getSessionWorkingSets` / `getSessionSupportSets` + `withStrictCompletionState` + the display helpers + the `SessionDetailSummary` type + the `effectiveSetExplanationEngine` / `unitConversionEngine` / `i18n/formatters` cross-calls (`BuildSessionQualityParams.unitSettings` is also UNUSED by the engine and omitted). **Zero `: Date`:** every ported function is clockless — no `new Date()` / `Date.now()` in their paths; every set-state / identity / scoring decision is over the session's OWN fields, never `Date()` / `Calendar`; the open-bag `type` rewrites (`{ ...set, type:'warmup' }` / `type \|\| 'straight'` / focusWarmup `done: set.done === true`) mirror the TS object spreads via encode→set-key→decode on the full JSON shape. **2 NEW** goldens (each a `cases` array): `session-quality/quality-cases` (buildSessionQualityResult level/signal/confidence/cap branches: insufficient_data early-return + high/medium/low + every positive+issue signal + score caps + confidence bands) + `session-quality/grouping-and-input-cases` (groupSessionSetsByType classification — working `type`/`setType`/`stepType`/inferred · warmup `type`/`isWarmup`/`:warmup:`-id · support+corrective→uncategorized + warmup type-rewrite — · buildWorkingOnlySession reconstruction · focusWarmup merge+dedup · number-form `sets` plannedWorkingSets · supportExerciseLogs passthrough+skipped · normalizeLoadFeedback array/summary-object/record-of-values/session.loadFeedback · painPatterns match · provided-effectiveSetSummary override + earlyEndReason cap · dataFlag test cap) GENERATED via `scripts/generate-parity-goldens.mjs` → `generateSessionQuality`, never hand-edited (§22); `SessionQualityEngineParityTests` re-runs the PORTED functions on each case's echoed engineInput and **COMPUTE-ASSERTS** the full `SessionQualityResult` (level / score / title / summary + ordered positives/issues/nextSuggestions / confidence) == golden PLUS `grouped` / `workingOnly` structural probes pinning the two CALLed `sessionDetailSummaryEngine` functions directly. The 66 pre-existing parity goldens regenerate byte-identically (`generated 68 fixture(s); 2 changed`) — zero `--diff-filter=M`, the 2 new goldens additive (`--diff-filter=A`); existing decision / SR / e1rm / 17e / AN-1 / AN-2 / AN-3 goldens byte-unchanged. The parity count guards (`parityFixturesContract` own-list + `parityFixturesGenerationConsistency` + `iosBootstrapParityStillGreen` + the ten `iosLocal*` / `iosNative*` `--check` guards) bumped 66→68 in sync. **SPM-auto-included, `project.pbxproj` / deps / lockfile / `Package.swift` / `package.json` byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter` / source-of-truth touch; already-ported helpers reused (no re-port); only the sessionQuality-CALLed sessionDetailSummary subset ported.** Adds `docs/ios-native-migration/IOS_AN4_SESSION_QUALITY_PORT_V1.md`; extends §19.2 (engine-package pure logic + parity goldens); amends §27. **Source-of-truth impact: none (no engine-output change, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **AN-5** | **painPatternEngine (trainingLevel-consumed subset) + trainingLevelEngine port + function-level parity — the analytics/insights-track auto-training-level slice (AN-1…6); PURE read-only logic only, NOT wired into any UI (that is AN-7).** Faithfully ports `buildPainPatterns` (`painPatternEngine.ts:58`) — the ONLY painPattern function trainingLevel CALLs — + every private helper it reads (`recentNormalSessions` / `painSeverityFromSet` / `sessionSortKey` / `toTime` / `excludedFlags`) + the FULL canonical `PainPattern` / `PainSuggestedAction` types (`training-model.ts:1028/151` — this slice is the FIRST full port; the AN-2 `PlateauDetectionEngine.PainPattern` / `SmartReplacementPainPattern` partial subsets stay as-is) into a new pure `IronPathTrainingDecision.PainPatternEngine`; AND `buildTrainingLevelAssessment` (`trainingLevelEngine.ts:150`) / `buildTechniqueQualitySummary` (`:94`) / `formatAutoTrainingLevel` (`:78`) + every private helper they read (`clampScore` / `signal` / `levelLabels` / `confidenceFromSessionCount` / `uniqueExerciseIds` / `buildFallbackE1RMProfiles` / `buildFrequencyScore`) + the output/input types (`AutoTrainingLevel` / `TrainingLevelSignal` / `ReadinessForAdvancedFeatures` / `TrainingLevelAssessment` / `Params`) into a new pure `IronPathTrainingDecision.TrainingLevelEngine`. **Reuses (does NOT re-port) the already-ported CALLed engines `AnalyticsDashboardEngine.buildAdherenceReport` (AN-3) / `E1RMEngine.buildE1RMProfile` (17e-1) / `EffectiveSetEngine.buildEffectiveVolumeSummary` (AN-3) / `E1RMEngine.{completedSets,number,filterAnalyticsHistory}` / `AnalyticsSupport.{daysFromCivil,civilFromDays,weekdayFromDays,jsMathRound}` (AN-1 civil) / `AnalyticsDashboardEngine.jsNumberString`, and REUSES the AN-2 `PlateauDetectionEngine.TechniqueQualitySummary` TYPE (identical shape — `buildTechniqueQualitySummary`'s return type, NOT redefined).** NOT ported (red line, only the CALLed subset): `getExercisePainPattern` (`painPatternEngine.ts:142` — read by `exercisePrescriptionEngine`, NOT trainingLevel); `e1rmConfidenceScore` (`trainingLevelEngine.ts:119` — DEAD in the TS source: defined but never CALLed, and the sole reader of `EstimateConfidence`, so neither is ported); `TrainingCalendarData` is modelled **TYPE-ONLY** (the single `weeklyFrequency[].sessionCount` field `buildFrequencyScore` reads — the calendar ENGINE is NOT ported), matching the real call sites (`sessionBuilder` / `trainingDecisionContext` never pass it → the own-history week-bucketing `||` fallback is the live path). The optional external summaries (`painPatterns` / `techniqueQualitySummary` / `calendarData` / `e1rmProfiles` / `effectiveVolumeSummary` / `adherenceReport`) are kept as optional `Params` fields; nil ⇒ compute from history (the faithful `\|\| compute` short-circuit). **Zero `: Date`:** `painPattern`'s `toTime` (`new Date(value).getTime()` epoch ms) is reproduced as integer civil-day + time-of-day arithmetic over `AnalyticsSupport.daysFromCivil`, and `buildFrequencyScore`'s `new Date(\`${date}T00:00:00\`).getDay()` Monday-week bucketing is reproduced with `weekdayFromDays` / `civilFromDays` — the week-bucket KEY is only a Map grouping handle (never output), so the resulting per-week counts → `average`/`score` are timezone-independent; the `frequency.average.toFixed(1)` reason interpolation uses a FAITHFUL `toFixed(1)` (round-half-away on the EXACT decimal — NOT `String(format:"%.1f")`, which rounds half-to-even: `2.25`→"2.3" vs "2.2"), proven by the `calendar-frequency-override` case (4.25 → "4.3"). **2 NEW** goldens (each a `cases` array): `pain-pattern/aggregation-cases` (buildPainPatterns full suggestedAction matrix — exercise watch/substitute/deload · area watch/deload/seek_professional · multi-area `' / '` combined + severityAvg-desc/frequency-desc stable sort · painSeverityFromSet painSeverity>0 + note `/sharp\|刺痛\|剧烈/`→4 / `/ache\|酸\|不适/`→2 / default 2 · 30-day lookback exclusion · maxSessions slice + explicit currentDate anchor · test+excluded dataFlag filter · empty) + `training-level/assessment-cases` (buildTrainingLevelAssessment all five level bands — unknown count 0/≤2 · beginner · novice_plus · intermediate · advanced — + every signal name/score/confidence/reason + highPain/poorTechnique/lowAdherence/unstableFrequency limitations & downgrades + readinessForAdvancedFeatures + nextDataNeeded + the painPatterns/techniqueQualitySummary/calendarData provided-vs-computed short-circuit, each case echoing a buildTechniqueQualitySummary + formatAutoTrainingLevel probe) GENERATED via `scripts/generate-parity-goldens.mjs` → `generatePainPattern` / `generateTrainingLevel`, never hand-edited (§22); `PainPatternEngineParityTests` / `TrainingLevelEngineParityTests` re-run the PORTED functions on each case's echoed engineInput and **COMPUTE-ASSERT** the full result == golden (ordered `[PainPattern]` · `TrainingLevelAssessment` level/confidence/readiness/ordered signals/limitations/nextDataNeeded + technique-summary + level-label probes). The 68 pre-existing parity goldens regenerate byte-identically (`generated 70 fixture(s); 2 changed`) — zero `--diff-filter=M`, the 2 new goldens additive (`--diff-filter=A`); existing decision / SR / e1rm / 17e / AN-1 / AN-2 / AN-3 / AN-4 goldens byte-unchanged. The parity count guards (`parityFixturesContract` own-list + `parityFixturesGenerationConsistency` + `iosBootstrapParityStillGreen` + the ten `iosLocal*` / `iosNative*` `--check` guards) bumped **68 → 70** in sync. **SPM-auto-included, `project.pbxproj` / deps / lockfile / `Package.swift` / `package.json` / `.claude` byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter` / source-of-truth touch; already-ported helpers + AN-2 `TechniqueQualitySummary` type reused (no re-port); only the trainingLevel-CALLed painPattern subset ported, `TrainingCalendarData` type-only.** Adds `docs/ios-native-migration/IOS_AN5_PAIN_PATTERN_TRAINING_LEVEL_PORT_V1.md`; extends §19.2 (engine-package pure logic + parity goldens); amends §27. **Source-of-truth impact: none (no engine-output change, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **AN-5b** | **recommendationConfidenceEngine + volumeAdaptationEngine port + function-level parity — the two AN-6 `intelligenceSummary` leaves (`trainingIntelligenceSummaryEngine.ts:225` recommendationConfidence + `:246` buildVolumeAdaptationReport) that were the only AN-6 leaves still unported; PURE read-only logic only, NOT wired into any UI (that is AN-6/AN-7).** Faithfully ports `buildRecommendationConfidence` (`recommendationConfidenceEngine.ts:220`) + every private helper it reads (`clampScore` / `reason` / `getExerciseIds` / `exerciseMatches` / `relevantSessions` / `relevantSets` / `buildTechniqueSummaryFromSets` / `isLoadFeedbackSummary` / `normalizeLoadFeedback` / `resolveCurrentE1rm` / `countRecentEdits` / `hasRecentReplacement` / `hasMixedUnitsWithSparseHistory` / `levelFromScore` / `levelLabel`) + the output/input types (`RecommendationConfidenceLevel` / `RecommendationConfidenceReason` / `RecommendationConfidenceResult` / `BuildRecommendationConfidenceParams`) into a new pure `IronPathTrainingDecision.RecommendationConfidenceEngine`; AND `buildVolumeAdaptationReport` (`volumeAdaptationEngine.ts:268`) + every private helper it reads (`clamp` / `normalizeText` / `decisionLabel` / `normalizeWeeklyRows` / `isLoadFeedbackSummary` / `normalizeLoadFeedback` / `hasPainRisk` / `qualitySummary` / `evidenceRatio` / `volumeIsLow` / `volumeIsNearTarget` / `volumeIsHigh` / `increaseDelta` / `decreaseDelta` / `confidenceFor` / `buildDecision`) + the output/input types (`VolumeAdaptationDecision` / `MuscleVolumeAdaptation` / `VolumeAdaptationReport` / `BuildVolumeAdaptationReportParams` + internal `NormalizedMuscleVolume`) into a new pure `IronPathTrainingDecision.VolumeAdaptationEngine`; AND `formatMuscleName` (`i18n/formatters.ts:219`) + its `lookupLabel` string-path / `normalizeDisplayKey` / `MUSCLE_LABELS` (grep-confirmed NO package equivalent — `AnalyticsDashboardEngine` sets `muscleName = muscleId` verbatim; the `normalizeDisplayKey` / `regexReplaceAll` privates follow the per-engine `ReplacementEngine` / `SmartReplacementEngine` convention). **Reuses (does NOT re-port) `E1RMEngine.{number,completedSets,filterAnalyticsHistory}` / `LoadFeedbackEngine.{tooHeavy,tooLight,good}` (17e-4) / `AnalyticsDashboardEngine.{roundOne,jsNumberString}` (AN-3) / `ExerciseLibrary.hasChineseText`, and REUSES the AN-2 `PlateauDetectionEngine.TechniqueQualitySummary` + `PlateauDetectionEngine.EffectiveVolumeSummary` consumed-subset TYPES (typealiased — identical shapes, NOT redefined), the AN-5 canonical `PainPatternEngine.PainPattern` / `PainSuggestedAction`, and the AN-4 `SessionQualityEngine.SessionQualityResult` (+ its signals).** NOT ported (red line): `completedTrainingSets` (`recommendationConfidenceEngine.ts:70` — DEAD in the TS source: defined but never reached from `buildRecommendationConfidence`); the AN-6 `trainingIntelligenceSummaryEngine` top-level + UI wiring. The optional external inputs `e1rmProfile` (`E1RMProfile \| EstimatedOneRepMax`) / `loadFeedback` (`LoadFeedbackInput`) / `recentEdits` (`RecentEditInput`) / `weeklyVolumeSummary` (`WeeklyVolumeItem[] \| { muscles }`) / `effectiveSetSummary` (`Partial<…>`, read as `byMuscle?.[muscleId]`) / `adherenceReport` (`Partial<AdherenceReport>`) are genuine TS runtime UNIONS consumed by DUCK-TYPING — kept as raw `JSONValue?` and structurally re-discriminated (the AN-2 precedent), never forced into a static type; `trainingLevel` is kept as a raw `String?` (only `=== 'unknown'` / `'beginner'` compared). **Zero `: Date`:** neither engine reads the wall clock — recommendationConfidence's only ordering is `relevantSessions`' `localeCompare`-descending over the session's OWN `finishedAt \|\| startedAt \|\| date` keys (reproduced with `>` over ASCII ISO, the `PlateauDetectionEngine` precedent); volumeAdaptation consumes only opaque summaries; `clampScore` reuses `AnalyticsSupport.jsMathRound`, `roundOne` reuses `AnalyticsDashboardEngine.roundOne`. **2 NEW** goldens (each a `cases` array): `recommendation-confidence/assessment-cases` (buildRecommendationConfidence — level bands forced-low-≤1-session / high / medium-plain + every reason branch history sparse/stable/building · technique missing/stable/poor · rir missing/complete/incomplete · pain-pattern/no-pain · load-feedback volatile/stable-good-dominant/stable-other/total-0 · e1rm high-quality/low-confidence/medium/absent · effective-sets stable/weak · recent-replacement · recent-edits + 92 cap · mixed-units-sparse · training-baseline + the pain 74 cap + every missingData string + loadFeedback array/summary/record-of-values + recentEdits number/array + session.loadFeedback exerciseId-filter + no-exerciseId match-all) + `volume-adaptation/report-cases` (buildVolumeAdaptationReport — every decision insufficient_data/hold/decrease/increase/maintain + confidence bands + formatMuscleName mapped/unmapped→未标注肌群/row.muscleName-override/CJK-passthrough/byMuscle-lookup + weeklyVolumeSummary array vs {muscles} + muscle-alias key + multi-muscle summaryParts join) GENERATED via `scripts/generate-parity-goldens.mjs` → `generateRecommendationConfidence` / `generateVolumeAdaptation`, never hand-edited (§22); `RecommendationConfidenceEngineParityTests` / `VolumeAdaptationEngineParityTests` re-run the PORTED functions on each case's echoed engineInput and **COMPUTE-ASSERT** the full result == golden (RecommendationConfidenceResult level/score/title/summary + ordered reasons + missingData · VolumeAdaptationReport ordered muscles + summary). The 70 pre-existing parity goldens regenerate byte-identically (`generated 72 fixture(s); 2 changed`) — zero `--diff-filter=M`, the 2 new goldens additive (`--diff-filter=A`); existing decision / SR / e1rm / 17e / AN-1 / AN-2 / AN-3 / AN-4 / AN-5 goldens byte-unchanged. The parity count guards (`parityFixturesContract` own-list + `parityFixturesGenerationConsistency` + `iosBootstrapParityStillGreen` + the ten `iosLocal*` / `iosNative*` `--check` guards) bumped **70 → 72** in sync. **SPM-auto-included, `project.pbxproj` / deps / lockfile / `Package.swift` / `package.json` / `.claude` byte-unchanged, zero `: Date`, no IO; pure engine — no write path, no `CanonicalSessionWriter` / source-of-truth touch; already-ported helpers + AN-2 `TechniqueQualitySummary` / `EffectiveVolumeSummary` types + AN-5 `PainPattern` + AN-4 `SessionQualityResult` reused (no re-port); only these two engines + `formatMuscleName` ported.** Adds `docs/ios-native-migration/IOS_AN5b_RECOMMENDATION_VOLUME_PORT_V1.md`; extends §19.2 (engine-package pure logic + parity goldens); amends §27. **Source-of-truth impact: none (no engine-output change, no write path; §8/§11 semantics unchanged). Data-safety impact: none.** |
| **AN-6** | **trainingIntelligenceSummaryEngine TOP-LEVEL port + function-level parity — the top aggregator that CLOSES the analytics/insights engine layer (AN-1…6 done); PURE read-only logic only, NOT wired into any UI (that is AN-7).** Faithfully ports `buildTrainingIntelligenceSummary` (`src/engines/trainingIntelligenceSummaryEngine.ts:204`) + every private helper it reads (`unique` / `isNormalSession` / `getExerciseIds` / `exerciseLabelFromHistory` / `selectExerciseIds` / `plateauIsImportant` / `plateauInsight` / `confidenceInsight` / `volumeInsight` / `addAction` / `buildActions`) + the output/input types (`TrainingIntelligenceSummary` / `Action` / `BuildTrainingIntelligenceSummaryParams`) to `TrainingIntelligenceSummaryEngine.swift` in `IronPathTrainingDecision`. CONSUMES (no re-port) the already-ported AN-1~5 leaves — `SessionQualityEngine.buildSessionQualityResult` (AN-4) · `RecommendationConfidenceEngine.buildRecommendationConfidence` + `VolumeAdaptationEngine.buildVolumeAdaptationReport` + `.formatMuscleName` (AN-5b) · `PlateauDetectionEngine.detectExercisePlateau` (AN-2) · `E1RMEngine.filterAnalyticsHistory` + `.hasInvalidExerciseIdentity` (17e/SR) · `ExerciseLibrary.formatExerciseDisplayName` (SR-1) — and re-emits their results plus derived keyInsights/recommendedActions; the opaque external inputs (`effectiveSetSummary` / `loadFeedback` / `painPatterns` / `e1rmProfiles` / `weeklyVolumeSummary`) stay raw `JSONValue` and convert to each sub-engine's typed subset AT THE CALL SITE (the same duck-typing the TS asserts, the AN-2/5b precedent). 1 NEW generated fixture `intelligence-summary/summary-cases-v1` (11 cases) which `TrainingIntelligenceSummaryEngineParityTests` COMPUTE-ASSERTs full-summary (sessionQuality? + recommendationConfidence[] + plateauResults[] + volumeAdaptation + keyInsights + recommendedActions) == golden; parity fixture-count guards bumped **72 → 73** in sync; no existing golden changed (additive). PURE / zero `: Date` (the only date reads are inside the reused `filterAnalyticsHistory`, over the session's OWN date strings); no write path / no `CanonicalSessionWriter` touch; SPM-auto-included (no `project.pbxproj` touch). Adds `docs/ios-native-migration/IOS_AN6_INTELLIGENCE_SUMMARY_PORT_V1.md`; extends §19.2 (engine-package pure logic + parity goldens); amends §27. **Source-of-truth impact: none. Data-safety impact: none.** |
| **AN-7** | **Insights UI surface — the analytics/insights-track CLOSURE slice: wires the AN-1…6 engine layer into the FIRST read-only UI (closes the analytics track).** Adds a READ-ONLY 训练洞察 block to the 今日 surface (`TodayRootView`) that renders PR / 趋势 / 平台期 / 连续打卡 / 肌群平衡 / 智能摘要 from the already-ported, parity-pinned engines — `TrainingStreakEngine.computeTrainingStreak` (AN-1) · `RecentPRDeltaEngine.computeRecentPRDeltas` (AN-1) · `WeeklyMuscleBalanceEngine.computeWeeklyMuscleBalance` (AN-1) · `AnalyticsDashboardEngine.buildExerciseTrend`/`.trendStatus`/`.coreTrendExercises` (AN-3) · `TrainingIntelligenceSummaryEngine.buildTrainingIntelligenceSummary` (AN-6, which internally runs AN-2 plateau detection + AN-4/5/5b leaves). New pure orchestration/presentation `IronPathTrainingDecision.resolveTrainingInsightsState` + `TrainingInsightsSummary` (`TrainingInsightsReadPath.swift`) mirror the Today read path's `resolveTodayReadinessState` + `TodayReadinessSummary` (additive presentation, §19.2): they CALL the engines (never recompute/change them) and format `SurfaceRow`s the thin SwiftUI layer renders verbatim. **Input is DERIVED FROM the DataHealth clean view, never raw AppData** (§10/§11): the thin app-layer `TrainingInsightsModel` reads the SAME canonical store read-only, builds the clean view via `buildCleanAppDataView` (the §10 chokepoint), and the resolver feeds the engines `cleanView.cleanedHistory` only (the SR-4 loose-params precedent — these engines take their own params, not the branded `CleanTrainingDecisionInput`). **Order bridge:** canonical `history` is OLDEST-FIRST (`appendingHistorySession` appends to the end) but the analytics engines mirror `analytics.ts`'s NEWEST-FIRST convention (`history.slice(0,8).reverse()`, `trendStatus` reads `slice(0,2)` as recent), so the resolver reverses `cleanedHistory` to newest-first before feeding them. Honest states (§15.4): missing/first-launch/no-history → empty (the readiness empty state already covers the no-data case, so the insights block renders nothing); unreadable → degrade (document left untouched); a per-section gap (no in-window PR / no core-lift trend / no important plateau) → an honest "数据不足" placeholder, never fabricated. **Source-of-truth impact: none** — pure read-only display, no `CanonicalSessionWriter`/write-path touch, no AppData mutation. **Data-safety impact: none.** Zero `: Date` (the injected `now` derives every engine's `nowIso`, mirroring the Today path); no analytics engine logic changed and **no golden touched** (zero parity drift); SPM-auto-included + edits only `ios/IronPath/TodayRootView.swift` (no `project.pbxproj` / `Package.swift` / dependency touch). Adds `docs/ios-native-migration/IOS_AN7_INSIGHTS_UI_SURFACE_V1.md`; amends §27. |
| **AN-8** | **Sort-stability fix + insights read-path tests — analytics-track DEBT cleanup (post-AN-7); PURE faithfulness fix + test backfill, NO new engine, NO engine-output semantics change, NO write path.** Closes two medium debts the analytics audit flagged in merged code. **(1) AN-3 sort stability (provable-faithfulness gap):** `AnalyticsDashboardEngine` mirrors `analytics.ts`, whose sorts rely on JS `Array.prototype.sort` being STABLE (ES2019); the Swift port used bare `Array.sort(by:)` in FOUR places (`buildMuscleVolumeDashboard:264` status/remaining · `buildPrs:423` date-desc · `buildAdherenceReport:672`/`:685` count-desc) and one comment falsely asserted `stable (Swift ≥5)`. Swift's `sort(by:)` is NOT contractually stable, so the port was green only by an unspecified implementation detail. Adds an `internal static stableSorted<T>(_:_:)` (three-way comparator, ORIGINAL-INDEX tie-break — the `SmartReplacement`/`PainPattern`/`RecentPRDelta` precedent), routes all four sorts through it, and corrects the false/imprecise comments. **Honest scope (verified, not assumed):** on Apple Swift 6.3.2 `Array.sort` is EMPIRICALLY stable, so (a) the four conversions are byte-neutral → every existing golden regenerates byte-identically (zero drift, confirming none passed by introsort coincidence) and (b) a fixture where a BARE `.sort` visibly reorders ties cannot be built — the gap closed is CONTRACTUAL, not an observed divergence. 1 NEW additive golden `analytics/adherence-report-tie-cases-v1` (a pure skippedExercises count tie pinning the JS insertion order through `slice(0,5)`, generated via `generateAdherenceReport`, never hand-edited) makes the tie-break non-vacuous + regression-guards a future stdlib change; asserted by `testBuildAdherenceReportTieCaseParity` + the focused `AnalyticsDashboardSortStabilityTests` (the tie-break RULE is load-bearing). **(2) AN-7 insights read path had zero tests:** adds `TrainingInsightsReadPathTests` covering the resolver branches (`.missing→.empty` / `.unreadable→.unavailable` / `.loaded(empty)→.empty` / `.loaded→.ready`), the OLDEST→NEWEST history order bridge (`:150`, a load-bearing trend test: reversed→`推进中` vs un-reversed→`回落`), and every formatting helper branch (`num` / `prValue` / `isImportantPlateau` / `plateauStatusLabel`) — pure backfill, no engine change, no bug surfaced. Parity fixture-count guards bumped **73 → 74** in sync (`parityFixturesContract` own-list + `parityFixturesGenerationConsistency` + `iosBootstrapParityStillGreen` + the ten `iosLocal*`/`iosNative*` `--check` guards). PURE / zero `: Date` / no `CanonicalSessionWriter` / no write path; **`project.pbxproj` / `Package.swift` / `package.json` / lockfile / `.claude` byte-unchanged**; SPM-auto-included. Adds `docs/ios-native-migration/IOS_AN8_SORT_STABILITY_INSIGHTS_TESTS_V1.md`; amends §27. **Source-of-truth impact: none (only a sort-stability guarantee added; engine output unchanged). Data-safety impact: none.** |
| Next (proposed) | iOS-17e engine consumption of performed sets — **CLOSED through the cluster port + wire-in: 17e-0 parity scaffold + 17e-1 per-exercise e1RM + 17e-2 performance-lookup + 17e-3 progression-suggestion + 17e-4 fine-tune/load-feedback port + 17e-5 wire-into-decision-output & history-golden compute-assert flip + 17e-6a fineTune-LIVE + `asOfDate` contract ALL landed (above)**; **iOS-17e-6b** is **CLOSED as a pseudo-requirement (FIX-3)**: "wiring the next-weight/next-reps suggestion into the TrainingDecision per-exercise FRAME" mis-attributed `makeSuggestion` — a `sessionBuilder`/`recommendationDiff` function with **0 TrainingDecision callers** — to TrainingDecision; the iOS per-exercise prescription already comes from `buildWorkingSetTargets` (iOS-4B5), so **no `CleanTrainingDecisionInput` expansion is required** (see the iOS-17e-6a row + the §11 17e-6a note). Any `sessionBuilder`-style next-weight surface is a separate, independent track (porting the un-ported `sessionBuilder`/`recommendationDiff` chain). Widget **W-2** (more widget families / Lock Screen — now unblocked: all four canonical **read-for-display** surfaces have landed (#437–#440), so a real-data widget snapshot can be sourced). All five iOS-17S tabs now render real canonical AppData **read-only**, with in-place **edit-write** on 我的 (EDIT-1/2/3) and 计划 (EDIT-4) through the one §8.3 `performGatedMutation`; further edit slices may extend that boundary to additional fields. |

> Milestone facts here are descriptive context; the **rules in §1–§26 are binding.**

---

## 28. Final Architecture Decision Record

**ADR-0001 — Adopt the IronPath Master Technical Architecture as the binding engineering contract.**

- **Date:** 2026-05-30
- **Status:** Accepted
- **Context:** IronPath is mid-migration from a mature React/Vite/TS PWA to native iOS SwiftUI. Work is increasingly AI-assisted (Claude/Codex). Without hard boundaries, the dominant risks are scope creep into cloud/auth/persistence, source-of-truth corruption, and accidental full-restore/data-loss.
- **Decision:** Adopt this document as the canonical, top-level contract. Native iOS stays **local-first** (on-device JSON files via Foundation only); the SwiftUI app layer stays **thin**; logic lives in **Swift packages**; `IronPathDomain.AppData` is the **source of truth**, protected by **open-bag preservation, schema/timestamp guards, and parity goldens**; the **Focus-history store stays decoupled from AppData**; **draft restore stays in-memory** until the **DataHealth full-restore gate** is met; **TrainingDecision** consumes only a **`CleanTrainingDecisionInput`**; cloud/auth/CloudKit/backend/sync are **deferred behind gates** (HealthKit read-only body weight + workout history were ungated by HK-1/HK-2 per §17/§18 — imported workouts are derived/display-only, never canonical/engine input; HK-3 ungated a bounded workout write-back — a user-triggered, idempotent, native-only EXPORT, device-local — while other-type write-back and standalone metrics stay gated); all changes follow the **validation + guard + branch/PR** standards; AI agents must **read this first and stop on conflict.**
- **Consequences:**
  - *Positive:* predictable, safe, incremental migration; strong data-safety guarantees; AI tasks are bounded and auditable; merges are gated by real checks.
  - *Negative / accepted cost:* some desirable features (cloud sync, accounts, full restore) are intentionally slower because they require an explicit architecture task that amends this document.
- **Amendment rule:** any change to a boundary in this document must be made **in the same PR** that implements the corresponding code change, and must pass the validation + guard standards.

---

*End of canonical document. If your task is not consistent with the above, stop and escalate before writing code.*
